# Branch dispositions — the ONLY branch-disposition authority for this repository

Created by phase GOV-3 on 2026-09-04 (OWNER_DECISION D-LEDGER, applied default). Before this file
existed, no record in the repository said what should happen to any branch; branch disposition
lived in one person's head and in one AI session's private memory. **This file is the authority.
Session memory is not.** Operating rules: `.claude/skills/thenijobs/references/merge.md` (rows,
claims, worktree lifecycle) and `references/promote.md` (the `## Promotions` table). Checked by
`scripts/governance/validate-branch-dispositions.mjs` (warnings only; read them).

## The rules

- **A branch absent from this file has NO disposition and MUST NOT be merged.** Absence is not
  neutral. Find out before merging it.
- **Only the owner changes a disposition.** A session may ADD a row for a branch it just created.
  A session may never CHANGE another branch's `Status` cell, not even to correct it. Flag and ask.
- **CLAIM BEFORE YOU START — the row is a lock, not a receipt.** See the claim protocol.
- **Phase branches merge into `develop`, never into `main`** (OWNER_DECISION D-BRANCH,
  2026-09-04). `staging` and `main` move only by fast-forward promotion with the owner's word in
  the same message, recorded in `## Promotions`. A merge commit or a direct commit on `staging` or
  `main` is a defect.
- **Nothing in this file authorizes a deploy.** `main` = production on Vercel (see the `main` row).
  The agent never runs `firebase deploy`, `vercel --prod`, or any equivalent.

## Claim protocol — read this before creating a branch

1. **Re-read the `## Branches` table.** Check for a row whose `Why` cell overlaps the work you are
   about to start, whatever its `Status`.
2. **If an overlapping row exists and is `ACTIVE`, STOP and ask the owner.** Two sessions on one
   scope is the failure mode, not a merge conflict to resolve later.
3. **Collision check by command, not memory:** `git worktree list` · `git branch --list
   'thenijobs/*'` · the `ACTIVE` rows below. A row on an unmerged branch is invisible from
   `develop`, so all three are mandatory.
4. **Add your row as the FIRST commit on the new branch**, `Status` = `ACTIVE`, `SHA` = the base
   commit, `Why` naming the files or directories you expect to touch and the collision check you
   ran. Commit it with a pathspec: `git commit -- docs/active/BRANCH_DISPOSITIONS.md`.
5. **Then do the work.** Expand the `Why` cell with findings as you go; commit ledger edits
   immediately, never leave the ledger dirty.
6. **Committing directly to `main` is a defect under this model**, not a phase. If it ever happens,
   add a row naming the commit so the next session can see it.
7. On merge, the bookkeeping commit on `develop` sets `Status` = `MERGED_DEVELOP`, fills `SHA` with
   the branch tip, and `Merged into` with the merge commit. No placeholder may survive the merge.

## Status tokens

| Token | Meaning |
|---|---|
| `ACTIVE` | claimed, unmerged; the `SHA` cell is the base at claim time until the bookkeeping commit |
| `MERGED_DEVELOP` | contained in `develop`; `Merged into` = the `--no-ff` merge commit on `develop` |
| `ENVIRONMENT` | `develop`, `staging`, `main` themselves; moved only by merge (develop) or fast-forward promotion |
| `PRESERVED_REFERENCE` | holds commits absent from `develop`; **never merge without a new owner decision**; never delete |
| `SUPERSEDED_BY:<branch>` | replaced by another branch; kept as history |
| `ARCHIVED_TAG:<tag>` | tagged and retired |

## Branch model (OWNER_DECISION D-BRANCH, 2026-09-04, applied default — owner pushes)

`thenijobs/<phase>` → `--no-ff` merge into `develop` (inside the develop worktree) → fast-forward-only
promotion `develop` → `staging` → `main`, each promotion with the owner's word in the same message.
`main` never receives a merge commit or a direct commit. `develop` and `staging` were created at the
production tip `5b611114` by GOV-3 and exist locally until the owner pushes them.

## Branches

| Branch | SHA | Status | Decided | Decided by | Why | Merged into |
|---|---|---|---|---|---|---|
| `develop` | `5b611114` | ENVIRONMENT | 2026-09-04 | owner (D-BRANCH, applied default) | Created at the production tip `5b611114` by GOV-3 (`git branch develop main`). Integration + local end-to-end. Checked out in `/Users/saai_siddharth/Projects/Clients/Theni-Jobs-develop`. Unpushed until the owner pushes. | N/A |
| `staging` | `5b611114` | ENVIRONMENT | 2026-09-04 | owner (D-BRANCH, applied default) | Created at `5b611114` by GOV-3. Pre-production; moves only by `promote staging` (fast-forward). Unpushed until the owner pushes; a push may create a Vercel preview (D-DEPLOY). | N/A |
| `main` | `5b611114` | ENVIRONMENT | 2026-09-04 | owner (D-BRANCH) | Production pointer. Served by **Vercel** (verified 2026-09-04: `server: Vercel`, `x-vercel-id: bom1::…`, `www.thenijobs.com` CNAME → `vercel-dns`, the `/pricing` page carries a string introduced only in `5b611114`). A push to `main` is a production deploy if the Vercel Git integration is linked to `main` (INFERRED from a CDN cache fill 4 min after `pushedAt`; not visible from this machine — owner confirms in the Vercel dashboard). Firebase Hosting `thenijobs-9f01d.web.app` holds a stale CLI deploy from 2026-08-30 and serves no custom domain. Moves only by `promote main`. | N/A |
| `thenijobs/gov3-thenijobs-skill-and-branch-model` | `f8bb57b` | MERGED_DEVELOP | 2026-09-04 | GOV-3 (this phase) | **Merged 2026-09-04** into `develop` as `a0c08fd` inside `Theni-Jobs-develop` (5 commits `f1d3154..f8bb57b`; first-parent diff 21 files, all in scope; overlap 0; resurrection check forbidden=0 dark-theme=0 workflows=0). Gate at `f8bb57b` (no env file): typecheck 0 · lint 1 (12 ambient errors in 7 untouched `src/` files) · build 1 (environment: `auth/invalid-api-key` at page-data collection — a fresh checkout cannot build without `.env.local`) · secrets:src-literals 1 (S-6 `src/app/api/otp/call/route.ts`) · tracked-artefacts 0 · test/test:rules/secrets:bundle SKIP. Phase worktree removed after the four preconditions. Scope: build the single operating skill `.claude/skills/thenijobs/` (`SKILL.md`, `references/{cto,worker,security,feedback,uiux,merge,promote,run,ci,decisions,hazards}.md`, `scripts/{gate,surface,state,merge-develop,promote}.sh`), this ledger, `scripts/governance/validate-branch-dispositions.mjs`, root `CLAUDE.md` (short rules; keeps the `@AGENTS.md` include), `.gitignore` (`.claude/settings.local.json`, `.claude/worktrees/`). No `.prettierignore` (no prettier, no hooks in this repository — measured). Collision check at claim: `git worktree list` = primary checkout only (`main`); `git branch --list 'thenijobs/*'` = empty; no ledger existed. Worktree: `/Users/saai_siddharth/Projects/Clients/Theni-Jobs-gov3`. Never touches `src/`, the rules files, `firebase.json`, `vercel.json`, `next.config.ts`, or `package.json`. | `a0c08fd698772543051092b9bc439e6845e022e6` (2026-09-04, inside `Theni-Jobs-develop`) |
| `thenijobs/gov3b-validator-environment-rows` | `0c122dc` | MERGED_DEVELOP | 2026-09-04 | GOV-3b (this phase) | **Merged 2026-09-04** into `develop` as `71ad7ea` (2 commits `6bcc160..0c122dc`; first-parent diff 2 files; overlap 0; resurrection 0). Gate at `0c122dc` (no env file): typecheck 0 · lint 1 (the same 12 ambient errors) · build 1 (environment) · secrets:src-literals 1 (S-6) · tracked-artefacts 0; `eslint` on the validator 0; non-vacuity: a planted non-ancestor SHA on the `develop` row produced the warning. Worktree removed after the four preconditions. Scope: fix one validator defect found by GOV-3's own bookkeeping: `scripts/governance/validate-branch-dispositions.mjs` compared an ENVIRONMENT row's recorded SHA with the branch tip, which moves by design after every merge/promotion, producing a permanent false warning. Change: ENVIRONMENT rows record their creation SHA and the validator checks that it is an **ancestor** of the live tip (same rule as ACTIVE rows). Touches only that script and this row. Collision check at claim: `git worktree list` = primary + `Theni-Jobs-develop`; `git branch --list 'thenijobs/*'` = the merged GOV-3 branch only; no other `ACTIVE` row. Worktree: `/Users/saai_siddharth/Projects/Clients/Theni-Jobs-gov3b`. | `71ad7eaa13381f31fb3e490d14af19695466942c` (2026-09-04, inside `Theni-Jobs-develop`) |
| `thenijobs/rules1-default-deny-and-client-parity` | `7ee1961` | MERGED_DEVELOP | 2026-09-04 | owner ("go with the defaults", 2026-09-04) via phase RULES-1 | Default-deny Firebase rules plus coupled client parity (security.md S-1, S-2, S-3, S-11, S-13). Expected files: `firestore.rules`, `storage.rules`, `src/app/register-business/page.tsx`, `src/lib/firebase/firestoreServer.ts`, `src/lib/firebase/firestoreService.ts`, `src/app/company/[slug]/CompanyProfilePageClient.tsx`, `src/app/[companySlug]/CompanyLandingPageClient.tsx`, `src/app/company/register/page.tsx`, `src/components/home/WalkInDriveBanner.tsx`, `src/app/employer/jobs/page.tsx`, `src/app/employer/jobs/[id]/EmployerJobDetailClient.tsx`, `src/app/employer/candidates/page.tsx`, `src/app/portfolio/seeker/[id]/SeekerPortfolioClient.tsx`, `src/lib/firebase/errorService.ts`, this ledger, `.claude/skills/thenijobs/references/{decisions,security}.md`. Owner decisions applied: D-JOBSTATE resume→pending, D-SLUG `companySlugs` reservation, D-ERRORS authenticated-only, D-TESTS Playground checklist only (JDK 17 on this machine; firebase-tools needs 21). Never touches `src/app/api/**`, `next.config.ts`, `firebase.json`, `vercel.json`, `plans.ts`, `constants.ts`, `AuthContext.tsx`, `login/page.tsx`, `src/app/admin/**`. Collision check at claim: worktrees = primary + `Theni-Jobs-develop`; `thenijobs/*` = two merged branches; `ACTIVE` rows 0; `develop` `177ddd1` clean. Worktree: `/Users/saai_siddharth/Projects/Clients/Theni-Jobs-rules1`. Deploy of the rules is the owner's (`firebase deploy --only firestore:rules,storage --project thenijobs-9f01d`), never the agent's. **Built (2026-09-04):** commits `d65e334` (rules), `a32c839` (client parity, 15 files), `76b0f69` (hook deps), `6a4c0a0` (job-detail feedback); scope extended by `src/contexts/NotificationContext.tsx` (admin inbox subscribes to `userId == 'admin'`) and `src/lib/firebase/adminNotify.ts` (single admin-addressed notification) — recorded in run-state before the change. Evidence: gate after the last commit (typecheck 0 · lint = baseline 12 errors/758 warnings · build env-red · S-6 literal ambient · tracked-artefacts 0); 42 emulator probes (JDK 21, demo project) pass on the phase rules and 23 of them flip to ALLOW on the old rules (revert-and-watch); lanes security/uiux/feedback PASS(_WITH_FINDINGS), nothing blocking. Not verified: production behaviour until the owner deploys and runs the Playground after-check; export page-count parity (needs the owner's env file). **Merged 2026-09-04** into `develop` as `78addf2` inside `Theni-Jobs-develop` (6 commits `85f97ae..7ee1961`; first-parent diff 19 files, all in scope; overlap 0; resurrection forbidden=0 dark-theme=0 workflows=0). The rules are live in production only after the owner runs `firebase deploy --only firestore:rules,storage --project thenijobs-9f01d`. | `78addf23b45d9c5d8a868268c6b515e04b5dbbc1` (2026-09-04, inside `Theni-Jobs-develop`) |
| `thenijobs/uiux1-canonical-dashboard-system` | `af692a4` | MERGED_DEVELOP | 2026-09-05 | owner (2026-09-05, two answered questions: "Keep brand, modernise density" + "All dashboards, one long run") via phase UIX-1 | Canonical mobile-first dashboard system, then conversion of all 58 dashboard pages under `src/app/{admin,employer,seeker}/**`. **Measured problem at `af692a4`** (scanner, not assertion): 0 of 58 dashboard pages import any primitive from `src/components/ui/` — the existing `DataTable.tsx`, `StatsCard.tsx`, `EmptyState.tsx`, `Sidebar.tsx` have **zero** importers and are orphans of the abandoned `origin/feature/production-transformation-v1` design language (purple/cyan/glow, contradicting the live light `--vk-*` blue); 22,581 lines of hand-rolled dashboard markup; 12 pages render a raw `<table>` and **8 of them have no mobile card fallback**; 12 sites use a base `grid-cols-3..5` with no breakpoint prefix (broken below 640px). Owner decision: keep the existing brand (light `--vk-*` blue, Inter/Poppins, logo) and rebuild layout, spacing, type ramp, elevation, tables, toolbars and stats; **not** a re-brand. Expected files: NEW `src/components/dashboard/**`, NEW `src/lib/cn.ts`, `src/app/globals.css`, `src/app/{admin,employer,seeker}/layout.tsx`, the 58 dashboard pages, this ledger. Never touches `src/app/api/**`, `firestore.rules`, `storage.rules`, `database.rules.json`, `next.config.ts`, `firebase.json`, `vercel.json`, `package.json`, `src/lib/plans.ts`, `src/contexts/AuthContext.tsx`, `src/hooks/useAuth.ts`, or any Firestore query shape — **presentation only; no data contract may change**. Collision check at claim: `git worktree list` = primary (`main`) + `Theni-Jobs-develop` (`develop`) + `Theni-Jobs-fix1` (merged); `git branch --list 'thenijobs/*'` = 4 branches, all `MERGED_DEVELOP` or recorded; `ACTIVE` rows other than RULES-1 = 0. Worktree: `/Users/saai_siddharth/Projects/Clients/Theni-Jobs-uiux1`. | `64ff623` (2026-09-05, inside `Theni-Jobs-develop`) |
| `origin/feature/production-transformation-v1` (remote only) | `b31cb86` | PRESERVED_REFERENCE | 2026-09-04 | GOV-3 (recorded; disposition needs the owner) | Remote-only branch, last commit 2026-07-26 "Phase 1: Create unified design system tokens". At `5b611114` it is 99 commits ahead of and 44 behind `main` (`git rev-list --count`). Its content was never integrated. Never merge without an owner decision; if it is ever adopted, it enters through a phase branch into `develop`. No local branch exists, so the validator reports it as informational. | N/A |
| `thenijobs/fix1-employer-visibility-and-error-index` | `386b58b` | MERGED_DEVELOP | 2026-09-05 | owner ("start fixing all these bugs one by one", 2026-09-05) via phase FIX-1 | Four verified defects from the 2026-09-05 end-to-end audit, all contained and all fixable without an owner decision. (1) **Employers never see their messages** — `firestoreService.ts:422` writes `participants: [seekerId, companyId]` where `companyId` is a company DOCUMENT id, while `employer/messages/page.tsx:63` queries `array-contains user.uid`; the employer uid is never in that array so the list is permanently empty. Fixed by querying on `companyId` (works for existing documents, no migration) and by writing the owner uid into `participants` for new threads. (2) **Employers never get the new-application notification** — `firestoreService.ts:453` addresses it to `data.companyId` while `NotificationContext` filters `userId == uid`; the other four notification call sites correctly use `ownerId`/`postedBy`. (3) **Admin error monitoring is broken in production** — `errors where status == open order by lastOccurred desc` returns `400 FAILED_PRECONDITION` (probed against `thenijobs-9f01d` 2026-09-05); `firestore.indexes.json` declares no `errors` index at all. (4) **Candidate modal mutates React state directly** — `employer/candidates/page.tsx:666,669` assign to a `useState` value so a saved note or status change does not re-render; also two of the twelve ambient lint errors. Expected files: `src/lib/firebase/firestoreService.ts`, `src/app/employer/messages/page.tsx`, `src/app/employer/candidates/page.tsx`, `firestore.indexes.json`, this ledger. Never touches `src/app/api/**`, the rules files, `next.config.ts`, `vercel.json`, `package.json`, `constants.ts`, `plans.ts`. Lanes (surface.sh): security, uiux, feedback, skill, docs. Collision check at claim: `git worktree list` = primary + `Theni-Jobs-develop` + this one; `git branch --list thenijobs/*` = three merged branches; `ACTIVE` rows 0. Worktree: `/Users/saai_siddharth/Projects/Clients/Theni-Jobs-fix1`. Base `bc8014e` is the develop tip **after** reconciling the fast-forward model: `main` had received `6f34dc3`, `8f1973d` and `260ad42` directly, so `main` was not an ancestor of `develop` and every promotion was blocked; `main` was merged into `develop` as `bc8014e` (no conflicts) before this phase was cut. That merge also surfaced a finding: `260ad42` carries a `seekerProfiles` public-read clause (`isPortfolioPublic == true`) that was hand-written during the merge resolution on `main` and exists on no branch. Index deployment is the owner's (`firebase deploy --only firestore:indexes --project thenijobs-9f01d`), never the agent's. **Scope extended 2026-09-05, recorded before the change**, to `src/app/seeker/messages/page.tsx`: while confirming defect 1 I found the chat is broken in BOTH directions, not only the thread list. The two pages write and order by different timestamp fields — `applyToJob` and the seeker write `createdAt`, the employer writes `timestamp`; the seeker reads `orderBy('createdAt')`, the employer reads `orderBy('timestamp')`. Firestore `orderBy` excludes documents that lack the field, so a seeker never sees an employer reply and an employer never sees a seeker message or the system message. Fixing only the employer file would leave the feature half-broken, so both chat pages are standardised on `createdAt` and both sort client-side over `createdAt ?? timestamp`, which keeps existing messages of either shape visible without a data migration. **Built and merged 2026-09-05.** Commits `4514b55` (claim), `7a5386b` (scope), `cfa6945` (owner uid for threads and alerts), `169171d` (one chat time field, employer threads keyed on companyId), `59ce597` (candidate modal setter), `386b58b` (errors indexes). Evidence: gate after the last commit with the owner's env file sourced into the build only — typecheck 0 · lint 1 (10 errors, down from the baseline 12; the two removed are exactly this phase's `react-hooks/immutability` errors, warnings unchanged at 757) · **build 0 with real-firebase-config=yes** · secrets:bundle 0 · secrets:src-literals 1 (ambient S-6, `src/app/api/otp/call/route.ts`) · tracked-artefacts 0 · test/test:rules SKIP (no suite). Non-vacuity for the messaging fix: a Firestore **emulator** probe on demo project `demo-thenijobs-fix1` (JDK 21, never production) seeds one old-shaped and one new-shaped conversation plus four messages written the three ways the app writes them, and shows the old employer query returning only the new thread, the new `companyId` query returning both, `orderBy('createdAt')` dropping the employer's reply, `orderBy('timestamp')` dropping all three seeker and system messages, and the unordered client-sorted read returning all four in order — all five checks as expected. Probe kept at `~/.thenijobs/run/fix-2026-09/logs/fix1-chat-probe.mjs`. Lanes: security PASS_WITH_FINDINGS, uiux PASS, feedback PASS. **Findings carried out of the phase:** (a) under the RULES-1 default-deny rules a seeker still cannot create the employer's new-application notification — the rule allows a create only for one's own uid or the pseudo-recipient `admin`; `createNotification` now carries `companyId` so a follow-up rule can check the recipient against `companies/{companyId}.ownerId`; this is pre-existing, not a regression, because the old misaddressed write was equally denied. (b) `260ad42` on `main` carries a `seekerProfiles` public-read clause written during that merge resolution and present on no branch. (c) `useFirestore.useCollection` still ignores constraint changes; all 30 call sites without a `skip` guard use static constraints today, so nothing is currently broken by it — a latent hazard, not a live bug (this corrects the 2026-09-05 audit, which over-reported it). **Not verified:** no end-to-end run in a browser — the employer chat and the applicant notification are behind an authenticated employer account with real applications, and creating that would mean writing to `thenijobs-9f01d`. | `ee903a6` (2026-09-05, inside `Theni-Jobs-develop`) |
| `thenijobs/fix2-notification-rule-for-employer-alerts` | `1406a59` | MERGED_DEVELOP | 2026-09-05 | owner ("start fixing all these bugs one by one", 2026-09-05) via phase FIX-2 | Make the notification system actually work under the RULES-1 default-deny rules. Two defects, both verified by reading the rule against the code that exercises it. **(1) A seeker cannot alert the employer.** `firestore.rules` notifications `create` allows only `isAdmin()`, an employer/owner creating for anyone, a create for one's own uid, or `userId == 'admin'`. `applyToJob` runs as the job seeker and addresses the employer's uid, so the write is denied the moment the rules ship — FIX-1 (`cfa6945`) fixed the address but could not fix the permission, because `firestore.rules` was outside that phase. Fix: a fifth create branch that lets a seeker notify a company owner **only about an application they themselves made**, proven by the `applications` document rather than asserted — the notification must carry `applicationId` and `companyId`, the application's `seekerId` must equal the caller, its `companyId` must match, and the named company's `ownerId` must equal the recipient. Field-presence is checked first so `&&` short-circuits before any `get()`, and the branch is placed last so the existing paths match earlier and never pay for the reads. **(2) Nobody can mark a notification as read.** The `update` rule allows `affectedKeys().hasOnly(['isRead', 'readAt'])`, but the application writes `read` — `createNotification` writes `read: false` (`firestoreService.ts:601`), `markNotificationRead` writes `{ read: true }` (`:614`), `markAllNotificationsRead` queries `where('read','==',false)` (`:620`), `NotificationContext` counts `!n.read`, and `payment/verify/route.ts:220` writes `read`. Nothing in the tree writes `isRead`. So every 'mark as read' is denied once the rules ship and the unread badge never clears. Fix: allow `read` alongside `isRead` and `readAt`. Expected files: `firestore.rules`, `src/lib/firebase/firestoreService.ts` (add `applicationId` to the employer alert payload), this ledger. Never touches `src/app/api/**`, `storage.rules`, `database.rules.json`, `next.config.ts`, `firebase.json`, `vercel.json`, `package.json`, `src/app/**`. Lanes: security, rules, skill, docs. **Collision check at claim:** `git worktree list` = primary (`main`) + `Theni-Jobs-develop` (`develop` `af692a4`) + `Theni-Jobs-uiux1` + this one; `git branch --list 'thenijobs/*'` = 5. A second session is active: `thenijobs/uiux1-canonical-dashboard-system` was claimed at `4509e3e` on top of this same base while FIX-1 was being merged. Its row declares presentation-only work under `src/components/dashboard/**`, `src/lib/cn.ts`, `src/app/globals.css` and the 58 dashboard pages, and states it never touches `firestore.rules` or any Firestore query shape. Diffed to confirm rather than trusted: `git diff --name-only af692a4..thenijobs/uiux1-canonical-dashboard-system` = `docs/active/BRANCH_DISPOSITIONS.md` only. **No source-file overlap with this phase**; the ledger is the one shared file and merges by union. Surfaced to the owner because two concurrent sessions on one repository is the failure mode the claim protocol exists to catch. Worktree: `/Users/saai_siddharth/Projects/Clients/Theni-Jobs-fix2`. Rules and indexes deployment is the owner's (`firebase deploy --only firestore:rules,firestore:indexes --project thenijobs-9f01d`), never the agent's. **Built and merged 2026-09-05.** Commits `0da5bfc` (claim), `1406a59` (rules + payload). Gate after the last commit with the owner's env file sourced into the build only: typecheck 0 · lint 1 (10 errors / 757 warnings, byte-identical to the post-FIX-1 state, so no new red) · **build 0 with real-firebase-config=yes** · secrets:bundle 0 · secrets:src-literals 1 (ambient S-6) · tracked-artefacts 0 · test/test:rules SKIP. **Evidence — 11 rule checks against the Firestore AND Auth emulators** on demo project `demo-thenijobs-fix2` with real signed-in users (never production): the applicant may alert the owner of the company they applied to (ALLOW); a different seeker may not alert about someone else's application (DENY); the applicant may not name a company they did not apply to (DENY); may not address anyone but the owner (DENY); an alert with no `applicationId` falls through every branch (DENY); self-addressed and admin-addressed notifications still work (ALLOW); the recipient marks it read with `read` (ALLOW); `isRead` still accepted (ALLOW); the recipient may not rewrite the message (DENY); a stranger may not mark it read (DENY). **Revert-and-watch:** the identical probe against the pre-FIX-2 rules flips exactly the two fixes to DENY (checks 1 and 8) while all six denials stay DENY — the checks are not vacuous. Probe kept at `~/.thenijobs/run/fix-2026-09/logs/fix2-notif-probe.mjs`. **Regression argument for the other 30+ collections:** outside the `notifications` block the only change to `firestore.rules` is the added helper function; every other `match` block is byte-identical (diffed, not asserted). Lanes: security PASS_WITH_FINDINGS, rules PASS, skill/docs PASS. **Findings carried out of the phase:** (a) `isEmployerOrOwner()` dereferences `users/{uid}.role` without checking the document exists, so any signed-in user with no `users` document makes the notifications create rule raise a null-value evaluation error instead of returning false. It still fails closed and the emulator continues evaluating the remaining `||` branches, so nothing is permitted that should not be — but it makes legitimate denials surface as errors and would bite the anonymous-auth OTP path, which signs in before the user document exists. Worth its own small phase. (b) The new branch has no rate limit: a seeker with one real application can create that alert repeatedly. Rules cannot express rate limiting; bounded to their own application and their own company owner. **Not verified:** the 42 RULES-1 emulator probes were not re-run — they live in that phase's run-state, not in the tree (D-TESTS: no harness in the repository), so the byte-identical diff above is the regression argument rather than a re-run. No end-to-end browser run: the alert and the unread badge sit behind an authenticated employer account with real applications, and creating one would mean writing to `thenijobs-9f01d`. | `fdc8a39` (2026-09-05, inside `Theni-Jobs-develop`) |
| `thenijobs/perf0-headers-and-image-weight` | `283a4c2` | MERGED_DEVELOP | 2026-09-05 | owner ("start fixing all these bugs one by one" + "make the app blazing fast", 2026-09-05) via phase PERF-0 | Layer 0 of the performance plan, plus the security headers production does not send at all. **Measured 2026-09-05 against the live site, not asserted.** (1) `/_next/static/**` is correctly `public, max-age=31536000, immutable`, but **every file in `public/` is served `public, max-age=0, must-revalidate`** — `logo.png` (56 KB), `og-image.jpg` (49 KB) and `hero-bg.webp` (336 KB) are revalidated on every single page view. (2) **Production sends no security headers whatsoever**: no CSP, no HSTS, no `X-Content-Type-Options`, no `Referrer-Policy`, no `Permissions-Policy`. The block that exists in `firebase.json` applies only to the stale `thenijobs-9f01d.web.app` copy, which serves no custom domain — it has never protected the live site. (3) `public/images/eswaran-p.png` is **1.0 MB** and `anbarasan-s.png` is **424 KB**, both rendered on `/about` at a fraction of their pixel size. Approach: the CSP ships as **`Content-Security-Policy-Report-Only`**, not enforcing. An enforcing CSP written without an end-to-end run against a live site with real users could break Razorpay checkout, the Google sign-in popup, the Firebase channels and the Google Maps embeds in seven portfolio templates. Report-Only is risk-free, surfaces violations in the browser console, and the promotion path is documented in the file. The other five headers are enforced because each is measurably safe here: nothing in the tree uses geolocation, camera or microphone (grepped); `X-Frame-Options` is `SAMEORIGIN` rather than `DENY` because portfolio templates embed content and a stricter value buys nothing; HSTS carries no `includeSubDomains` and no `preload`, so it cannot strand a subdomain. The allowlist is derived from what the code actually reaches, not copied from `firebase.json`: `images.unsplash.com` (30 references in `sampleCompanies.ts`), `googleMapsEmbed` iframes in seven templates, `checkout.razorpay.com`, `lh3.googleusercontent.com` and `storage.googleapis.com` (from `next.config.ts` remotePatterns), and the Firebase identity, Firestore and storage origins. Expected files: `vercel.json`, `public/images/**`, `src/app/about/page.tsx`. Never touches `src/app/globals.css`, `src/components/**`, `src/app/{admin,employer,seeker}/**`, `firestore.rules`, `next.config.ts`, `package.json`. Lanes: security, config, uiux. **Collision check at claim:** worktrees = primary (`main`) + `Theni-Jobs-develop` (`378489c`) + `Theni-Jobs-uiux1` (second session, `7019e49`) + this one. The concurrent UIX-1 phase has touched 50 files; diffed rather than trusted — `git diff --name-only <base>..thenijobs/uiux1-canonical-dashboard-system` intersected with this phase's may-write list is **empty**. This phase's scope was chosen specifically to avoid every path UIX-1 claimed. Also verified that FIX-1's messaging logic survives on their branch (`messageMillis` present, `orderBy('timestamp')` gone). Worktree: `/Users/saai_siddharth/Projects/Clients/Theni-Jobs-perf0`. Nothing here deploys; headers take effect on the owner's next production deploy. **Built and merged 2026-09-05.** Commits `df2422c` (claim), `699c400` (images), `283a4c2` (headers). Gate after the last commit: typecheck 0 · lint 1 (10 errors / 757 warnings, byte-identical to the post-FIX-1 baseline, no new red) · **build 0 with real-firebase-config=yes** · secrets:bundle 0 · secrets:src-literals 1 (ambient S-6) · tracked-artefacts 0. **Images:** `eswaran-p` 1000 KB to 22 KB and `anbarasan-s` 424 KB to 10 KB, resized from 1024 px tall to 384 px wide (the portrait renders in a 96 CSS px box, so this still covers a 4x device pixel ratio) and re-encoded to WebP with alpha preserved. `width`, `height`, `loading` and `decoding` added. **`hero-bg.webp` deliberately left alone:** re-encoding measured a mean absolute error of 2.79 at q75, 2.50 at q80 and 2.02 at q85 out of 255, with peak per-channel differences of 35 to 50, because the file is already lossy and would be compressed twice. Saving 113 to 193 KB was not worth measurable generation loss on the most prominent image on the site; the correct fix is responsive sizes on the hero element, which needs the component and belongs to Layer 5. **Headers:** every `source` was checked to resolve to a file that exists in `public/`. The CSP allowlist was derived from the built output rather than copied from `firebase.json` — every external host in `out/` was extracted and matched against the directive it actually needs, which **found a real gap**: Firebase Auth refreshes ID tokens against `securetoken.google.com`, which is not a `googleapis.com` host and was therefore uncovered; `*.gstatic.com` was added for the reCAPTCHA assets phone auth pulls. 17 of 17 checked hosts now resolve to the correct directive. A `$comment` key was drafted into `vercel.json` and then removed: it is not a documented Vercel property and an unrecognised key risks failing the deployment, so the rationale lives here and in the commit messages instead. Lanes: security PASS_WITH_FINDINGS, config PASS, uiux PASS. **Findings carried out of the phase:** (a) `public/.DS_Store` exists on disk (8 KB); it is gitignored so Vercel never receives it, but it should not be in the directory. (b) The CSP is Report-Only and therefore protects nothing until promoted — the promotion procedure is in the commit message for `283a4c2`. (c) `hero-bg.webp` is 336 KB at 1915x821 and is served at that size to a 390 px phone; responsive sizes are the outstanding win. **Not verified:** the headers were not observed on a live response, because that requires a deployment and the agent never deploys. What was verified is that the configuration is valid JSON, that every cache `source` names a file that exists, and that the policy covers every origin the built bundle references. The owner confirms with `curl -sSI https://www.thenijobs.com/logo.png` after the next deploy. | `872b85c` (2026-09-05, inside `Theni-Jobs-develop`) |
| `thenijobs/sec1-remove-hardcoded-key-literals` | `1d0eeb4` | MERGED_DEVELOP | 2026-09-05 | owner ("start fixing all these bugs one by one", 2026-09-05) via phase SEC-1 | Remove the two hardcoded credential literals, and close the authentication bypass the first one was holding open. **The literal is not a stray constant, it is link one of a five-step chain.** `src/app/api/otp/call/route.ts:42` reads `process.env.TWOFACTOR_API_KEY || '<literal>'`, and that literal is byte-identical to the live `NEXT_PUBLIC_FIREBASE_API_KEY` (compared by SHA-256 on 2026-09-05, values never printed). Consequences, in order: (1) `apiKey` can never be falsy, so the `503 service unavailable` branch immediately below it is dead code; (2) the request to `2factor.in` is therefore made with a **Firebase key as the SMS vendor's API key**, which both fails and deposits a live Google credential in a third party's access logs; (3) when that call fails, `data` keeps its optimistic initial value `{ Status: 'Success', Details: 'voice_session_<timestamp>' }`, so **the route answers `success` with a synthetic session id it invented**; (4) `src/app/api/otp/verify/route.ts:47` accepts `123456` or `999999` for **any** session id beginning `test_session_` or `voice_session_`, and the prefix is caller-supplied, so the id need not even come from step 3; (5) `src/app/login/page.tsx` then calls `signInAnonymously` and writes `isVerified: true` onto the new user document. End to end: **a caller could obtain a phone-verified account with the code 123456 without any call ever being placed.** Mitigating and stated plainly: all seven `src/app/api/**` routes return 404 in the production static export, and `PHONE_LOGIN_ENABLED` is `false` in the login page, so this is not exploitable against the live site today. It becomes exploitable the moment `D-HOSTING` is resolved in favour of running the routes. That is exactly why it is worth fixing now rather than later. Fixes: `otp/call` reads the environment variable only, so the existing 503 branch becomes live, and the optimistic fallback is removed so a failed vendor call reports a failure instead of inventing a session. `otp/verify` only honours the fixed test codes when **no** provider key is configured, and rejects caller-supplied test prefixes outright when a key is present; the separate `if (!apiKey) return verified: true` branch, which accepted any code at all, is replaced with an explicit unavailable response. `adminUserService.ts` stops duplicating the Firebase config with literal fallbacks and reuses the already-initialised default app's own options, which removes the duplication as well as the literal. `gate.sh`'s `secrets:src-literals` check currently excludes `adminUserService.ts` by name; with the literal gone the exclusion is removed, so the check becomes strictly stronger and **goes green for the first time** in this programme. Expected files: `src/app/api/otp/call/route.ts`, `src/app/api/otp/verify/route.ts`, `src/lib/firebase/adminUserService.ts`, `.claude/skills/thenijobs/scripts/gate.sh`, this ledger. Never touches `firestore.rules`, `vercel.json`, `next.config.ts`, `package.json`, `src/app/{admin,employer,seeker}/**`, `src/components/**`, `src/app/globals.css`. Lanes: security, api, skill, docs. **Collision check at claim:** worktrees = primary (`main`) + `Theni-Jobs-develop` (`999f356`) + `Theni-Jobs-uiux1` (second session, `1eeb8ec`, now 50+ files) + this one. Diffed rather than trusted: the intersection of UIX-1's changed files with this phase's may-write list is **empty**. Worktree: `/Users/saai_siddharth/Projects/Clients/Theni-Jobs-sec1`. **This phase does not undo the exposure.** The repository is public and the history already carries these values in `2cbc780`, `45d9404`, `45ca048`, `acfd2e0` and `3fc1ef7`; deleting them from the tree does not delete them from git history. `D-SECRETS` remains open and is the owner's: restrict the Firebase web key in Google Cloud by HTTP referrer and by API, and rotate any non-Firebase key that was ever committed. **Built and merged 2026-09-05.** Commits `dcf2a1f`-equivalent claim then `1d0eeb4` (the fix). Gate after the last commit: typecheck 0 · lint 1 (10 errors / **756** warnings, one fewer than the 757 baseline because the now-unused `getApps` import went with the duplicated config) · **build 0 with real-firebase-config=yes** · secrets:bundle 0 · **secrets:src-literals 0 — green for the first time in this programme**, and the check is now stricter than when it was red, because the `adminUserService.ts` exclusion was removed with the literal · tracked-artefacts 0. `git grep -lE 'AIza[0-9A-Za-z_-]{35}' -- src scripts` on the merge result returns **0 files**. **Runtime evidence — the bypass was reproduced, then closed.** `next dev` was run inside the phase worktree on port 3100 with real `NEXT_PUBLIC_FIREBASE_*` values and a **dummy** `TWOFACTOR_API_KEY` (a placeholder string, never a real provider key), which is the configuration that matters: the bypass only shows itself when a provider key is present. Three requests, identical before and after: (A) `POST /api/otp/verify {sessionId:'voice_session_9999999999999', otp:'123456'}` — a session id invented by the caller — returned **`{success:true, verified:true}` HTTP 200** on the pre-fix code and **HTTP 400 'Invalid or expired session'** after. (B) the same with `test_session_…` and `999999` — **HTTP 200 verified** before, **HTTP 400** after. (C) `POST /api/otp/call {phone:'9876543210'}` with a key the vendor rejects returned **HTTP 200 with an invented `sessionId: 'voice_session_1788558397813'`** before, and **HTTP 502 'Could not place the verification call'** after. The before-state was produced by `git revert --no-commit HEAD` in the worktree with the dev server hot-reloading, then `git revert --abort`; `git status --porcelain` was 0 afterwards and the restored server was re-probed to confirm HTTP 400. So the checks are not vacuous: each one demonstrably passed on the broken code. Lanes: security PASS, api PASS, skill PASS, docs PASS. **Findings carried out of the phase:** (a) `src/app/api/otp/send/route.ts` still returns a `test_session_…` id when `TWOFACTOR_API_KEY` is unset. That is now consistent — verify only honours those ids when the key is also unset — but the SMS and voice paths remain asymmetric and deserve one small follow-up. (b) The login page still ends a verified OTP in `signInAnonymously` plus a self-asserted `isVerified: true` (`security.md` I10). This phase closed the forged-session route into that flow; it did not change the flow itself, which is `D-OTP` and needs an owner decision between 2Factor and the unused `AuthContext.sendPhoneOTP` Firebase phone auth. **Not verified / not undone:** the exposure itself. The repository is public and `2cbc780`, `45d9404`, `45ca048`, `acfd2e0` and `3fc1ef7` still carry these values in history. Removing a literal from the tree does not remove it from git. `D-SECRETS` stays open and is the owner's: restrict the Firebase web key in Google Cloud by HTTP referrer and by API, and rotate anything non-Firebase that was ever committed. Also unverified in production, because all seven routes 404 there — this fix matters when `D-HOSTING` is resolved. | `104fdc9` (2026-09-05, inside `Theni-Jobs-develop`) |
| `thenijobs/trust1-remove-fabricated-companies` | `5dae14f` | MERGED_DEVELOP | 2026-09-05 | owner ("start fixing all these bugs one by one", 2026-09-05) via phase TRUST-1 | **The live site publishes invented businesses as verified ones.** Verified 2026-09-05 against production, not inferred: `https://www.thenijobs.com/company/gk-clinic-chinnamanur` returns 200 and renders, client-side, `GK Clinic & Child Care Center` badged `PREMIUM` and `Authorized`, rated `4.9`, with `3 Open Jobs`, `2014 Established`, `11-50 Employees` and the phone number `+91 94432 18900`. A read-only Firestore query for each of the 13 slugs hardcoded into `generateStaticParams` returned **0 real companies** — none of them exists in the database. The data comes from `src/lib/sampleCompanies.ts`, which after `6f34dc3` still holds three complete inventions: `gk-clinic-chinnamanur`, `digital-theni-solutions` and `arasu-pandi-farm-services`. Two of them use the platform's own contact numbers (`SITE_CONTACT.phone1` and `phone2`), which is merely misleading. **The clinic does not**: it carries `+91 94432 18900` and `+91 98421 55678`, a named practitioner `Dr. G. Karthikeyan, MBBS, DCH`, five priced medical services, and two fabricated patient reviews attributed to named people. An invented healthcare provider with a named doctor and live phone numbers, presented as authorized on a directory that sells verification, is a different order of problem from a stale fixture. Reached by `src/app/company/[slug]/CompanyProfilePageClient.tsx:111` when Firestore finds nothing and again at `:153` inside the catch, so a transient query failure on a REAL company can also swap in an invented one. Same two paths in `src/app/[companySlug]/CompanyLandingPageClient.tsx`. Each fabricated company is published at three URLs because of the duplicate route families. The other 10 hardcoded slugs have no sample data, so they already render not-found; they exist only to generate 30 empty pages across the three families. Fix: delete the fallback from both client components on both paths, delete `sampleCompanies.ts` outright, and reduce the three hardcoded slug lists to `_fallback` alone, which `vercel.json` still needs. Real companies are unaffected: `generateStaticParams` gets them from `getAllCompanySlugsServer()`, and an unknown slug now reaches the not-found state the component already has. Expected files: `src/app/company/[slug]/CompanyProfilePageClient.tsx`, `src/app/[companySlug]/CompanyLandingPageClient.tsx`, `src/lib/sampleCompanies.ts` (deleted), `src/app/company/[slug]/page.tsx`, `src/app/companies/[slug]/page.tsx`, `src/app/[companySlug]/page.tsx`, this ledger. Never touches `src/components/**`, `src/app/globals.css`, `src/app/{admin,employer,seeker}/**`, `firestore.rules`, `vercel.json`. Lanes: security, uiux, feedback, seo, skill. **Collision check at claim:** worktrees = primary (`main`) + `Theni-Jobs-develop` (`6ff803b`) + `Theni-Jobs-uiux1` (second session) + this one; the intersection of UIX-1's changed files with this may-write list is **empty** (diffed). Worktree: `/Users/saai_siddharth/Projects/Clients/Theni-Jobs-trust1`. **Removing this from the tree does not remove it from the live site.** The three pages stay up until the owner deploys. They are not in the sitemap (checked: 0 of the 13 slugs appear), so they are not being actively promoted to search engines, but they are reachable and linkable today. **Built and merged 2026-09-05.** Commits: claim, then `5dae14f` (the removal). Gate after the last commit: typecheck 0 · lint 1 (10 errors / **755** warnings, two fewer than the 757 this programme started with, as the deleted file and its imports took their unused-variable warnings with them) · **build 0 with real-firebase-config=yes** · secrets:bundle 0 · secrets:src-literals 0 · tracked-artefacts 0. **Evidence is the export itself, counted before and after.** Company pages 119 to 106, `/companies/` 118 to 105, root-level 148 to 135 — exactly 13 fewer in each of the three URL families, 39 pages in total, and the export drops from 51 MB to 47 MB. Each of `gk-clinic-chinnamanur`, `digital-theni-solutions`, `arasu-pandi-farm-services` and `tata-consultancy-it-services-theni` was confirmed absent from `out/company/` after the build. Real companies are untouched: they come from `getAllCompanySlugsServer()`, which is unchanged. `grep -rnE "from '@/lib/sampleCompanies'|getSampleCompanyData\(" src` returns nothing; the only remaining occurrences of the name are the explanatory comments left at each site so the next reader knows why the fallback is absent. Lanes: security PASS, uiux PASS, feedback PASS (the not-found state already existed and is unchanged), seo PASS (39 fewer thin pages, none of which was in the sitemap), skill PASS. **Findings carried out of the phase:** (a) the three URL families remain, so every real company is still published three times — that is `SEO-1`, which is BLOCKED on one owner question. (b) `src/app/jobs/[id]/page.tsx` keeps a `'demo'` slug in `generateStaticParams` and `/employer/jobs/demo` is still generated; harmless placeholders rather than invented businesses, but they are the same species and worth a sweep. (c) The `_fallback` shell is retained deliberately because `vercel.json` rewrites unknown slugs to it. **Not verified, and important:** this does not take the three pages down. `https://www.thenijobs.com/company/gk-clinic-chinnamanur` still returns 200 and still renders the invented clinic until the owner deploys. Nothing the agent can do changes that; it is the one item in this programme where the delay between merge and deploy has a real-world cost, because the fabricated clinic carries a named practitioner and two phone numbers that are not the platform's own. | `4e74076` (2026-09-05, inside `Theni-Jobs-develop`) |
| `thenijobs/perf1-home-page-one-shot-reads` | `2168e9c` | MERGED_DEVELOP | 2026-09-05 | owner ("make the app blazing fast", 2026-09-05) via phase PERF-1 | **The home page holds four live Firestore subscriptions open to draw three numbers and a small strip.** Measured in a real browser on 2026-09-05, cold load of `www.thenijobs.com`: 4 requests to `firestore.googleapis.com/.../Listen/channel`. `StatsSection` calls `useRealtimeCount` three times, and that hook is implemented with `onSnapshot` over the whole matching set — so rendering the word 'Active Jobs' above a number streams every matching job document to the browser and then keeps a channel open for the life of the page. Two of those three calls are **the same query** (`companies where verificationStatus == 'verified'`), used once for 'Companies' and again for 'Verified Pages', so the page pays for it twice. `WalkInDriveBanner` adds the fourth through `useCollection`, also `onSnapshot`. None of it needs to be live. These are decorative counters and a three-item strip on a marketing page; nobody is watching them tick. `getCountFromServer` returns a count **without transferring any documents**, and one `getDocs` covers the strip. Expected files: `src/components/home/StatsSection.tsx`, `src/components/home/WalkInDriveBanner.tsx`, this ledger. **Deliberately does NOT touch `src/hooks/useRealtimeStats.ts` or `src/hooks/useFirestore.ts`.** Both are shared with the admin and employer dashboards, which the concurrent UIX-1 session is rewriting right now. Editing a shared hook underneath another session is precisely how two agents corrupt one repository, so the fix is made at the two call sites instead and the hooks are left for a later phase once UIX-1 lands. Lanes: uiux, feedback, skill. **Collision check at claim:** worktrees = primary (`main`) + `Theni-Jobs-develop` (`7582667`) + `Theni-Jobs-uiux1` (second session) + this one; `git diff --name-only <base>..thenijobs/uiux1-canonical-dashboard-system` intersected with this may-write list is **empty** (diffed, not assumed). Worktree: `/Users/saai_siddharth/Projects/Clients/Theni-Jobs-perf1`. Acceptance is a browser measurement, before and after, taken the same way: the home page must open **zero** Listen channels while showing the same numbers. **CORRECTION, 2026-09-05, before any code was changed.** The claim above was written from the audit and is half wrong; measuring it directly contradicted it on two points. (1) **`StatsSection.tsx` has zero importers.** `src/app/page.tsx` never renders it, so its three `useRealtimeCount` calls, including the duplicated companies query, do not execute on any page. The statement that the home page subscribes three times to draw counters is false. Loading the home page and searching for the 'Verified Pages' label found no such section. (2) **Four requests to `/Listen/channel` is not four subscriptions.** The Firestore SDK multiplexes every listener onto one webchannel and issues a handshake request followed by long-poll RPCs against the same endpoint, so the count of requests measures connection chatter, not the number of `onSnapshot` calls. Counting requests as subscriptions, which the 2026-09-05 audit did, overstates the problem. **What is actually true:** the only live subscription a signed-out visitor's home page creates is `WalkInDriveBanner`'s single `useCollection`, and `StatsSection` is dead code carrying a duplicated query. The revised scope is therefore smaller and still worth doing: convert the walk-in strip to a one-shot read, so the home page holds no persistent Firestore connection for a signed-out visitor, and delete the dead component rather than leaving a subscription-shaped trap for whoever wires it up next. Acceptance becomes: **zero `/Listen/channel` requests** on a cold home page, measured the same way before and after. **SECOND CORRECTION, after measuring: that acceptance criterion was also wrong.** A one-shot `getDocs` uses the *same* `/Listen/channel` endpoint as `onSnapshot` in the web SDK, so 'zero Listen requests' is unreachable and would not have meant anything. Counting requests, and then counting whether the request count grows over time, both failed to separate the two states: an idle long-poll does not reconnect inside a 30-second window, so the after-state and the reverted before-state both showed zero growth. **The metric that does separate them is how long a single request stays open.** A live `onSnapshot` parks a long-poll; a one-shot read completes in milliseconds. Measured in the browser against `next dev` in the phase worktree with real `NEXT_PUBLIC_FIREBASE_*` values, same page, same method: **before** (the fix reverted in the working tree with `git revert --no-commit HEAD`, hot-reloaded) — 6 Firestore requests, one of them **held open for 45,586 ms**, 1 request open beyond 10 s; **after** — 5 Firestore requests, longest **81 ms**, **0** open beyond 10 s, at a comparable page age of 50 s. The walk-in strip renders in both. That is the whole claim: a signed-out visitor's home page no longer parks a Firestore stream. **Built and merged 2026-09-05.** Commits: claim, correction, then `2168e9c`. Gate after the last commit: typecheck 0 · lint 1 (10 errors / 755 warnings, unchanged) · **build 0 with real-firebase-config=yes** · secrets:bundle 0 · secrets:src-literals 0 · tracked-artefacts 0. `StatsSection.tsx` was deleted: **zero importers**, confirmed by grep and by the fact that loading the home page and searching for its 'Verified Pages' label found nothing. It also issued the identical `companies where verificationStatus == 'verified'` query twice. An unused component holding three subscriptions is a trap for whoever wires it up next, not an asset. Lanes: uiux PASS, feedback PASS (no user-facing copy changed; the strip's loading and empty states are untouched), skill PASS. **Findings carried out of the phase:** (a) `useRealtimeCount` and `useCollection` are still `onSnapshot` over an entire matching set, and ten admin call sites still use them with no constraints at all — that is the real version of this problem and it belongs to a phase after UIX-1 lands, because both hooks and all those pages are in that session's scope. (b) The 2026-09-05 audit's claim of '4 Firestore realtime channels on the home page' should be read as '4 requests on one channel, one of which was a parked stream'. **Not verified:** the measurement is from `next dev`, not a production build, and from one desktop browser. The structural claim — no `onSnapshot` remains in the file — is exact and checkable in the diff; the timing numbers are indicative of the mechanism rather than a benchmark. | `b975c7e` (2026-09-05, inside `Theni-Jobs-develop`) |
| `thenijobs/sec2-tighten-open-write-paths` | `989daf4` | MERGED_DEVELOP | 2026-09-05 | owner ("start fixing all these bugs one by one", 2026-09-05) via phase SEC-2 | The four write paths RULES-1 left open (`security.md` S-12). Every caller was inventoried first, because a rule the application itself violates is a broken feature, not a fix. **(1) `counters/{id}` is `read, write: if isAuthenticated()`.** Any signed-in account can rewrite the THENIJOBS ID sequence, so two businesses could be issued the same registration number, or the counter could be reset. The only writer is `identityService.ts` at `:26` and `:111`, both the same transaction: `transaction.set(counterRef, { count: previous + 1, updatedAt })`, creating with `count: 1` when the document is absent. That shape is expressible as a rule, so the rule can allow exactly it. **(2) `activityLogs` accepts a create from anyone signed in.** The admin audit trail can be poisoned with entries attributed to other people. All ten callers pass either the acting user's own uid or an admin's uid, so requiring `userId == request.auth.uid` (or `isAdmin()`) matches every real caller. **(3) `conversations` and `conversations/{id}/messages` accept a create with no participant check.** Any signed-in user can open a thread naming other people, or post a message into someone else's thread. Conversations are created only by `applyToJob`, which sets `seekerId` to the caller and includes them in `participants`. Messages are written by both chat pages with `senderId = user.uid`, plus one system message written by `applyToJob` with `senderId: 'system'`. The message rule must therefore mirror the existing READ rule — participant, or the seeker, or the company owner, which is how an employer reaches a pre-FIX-1 thread whose `participants` holds a company id — and accept `senderId` of either the caller or `'system'`. **(4) `leads` accepts an UNAUTHENTICATED create** with only a three-field presence check and no size limit: an open funnel into every company owner's inbox. Two findings while inventorying it: **nothing in the tree creates a lead** — `grep` finds reads, a count, an update and no create anywhere in `src/` — so today the rule is pure attack surface for a feature with no client. And the three shapes disagree: the rule demands `name`, `employer/leads/page.tsx` reads `customerName`, `admin/leads/page.tsx` and `lib/types` read `contactName`. A `hasOnly` allowlist would be inventing a schema, so this phase bounds the damage instead: keep the required fields, cap the free text, cap the field count, and require the named company to actually exist. Expected files: `firestore.rules`, this ledger. Never touches `src/**`, `storage.rules`, `vercel.json`, `package.json`. If a tightening turns out to break a real caller, that is a scope extension recorded before the change, and it must avoid every path the concurrent UIX-1 session owns. Lanes: security, rules, skill. **Collision check at claim:** worktrees = primary (`main`) + `Theni-Jobs-develop` (`f772a39`) + `Theni-Jobs-uiux1` (second session, `ca1e64f`) + this one. UIX-1's changed-file list intersected with `firestore.rules` is **empty** (diffed). Worktree: `/Users/saai_siddharth/Projects/Clients/Theni-Jobs-sec2`. Acceptance is an emulator probe per rule that is **ALLOWED on today's rules and DENIED after**, plus the FIX-2 notification probe re-run unchanged to show the earlier work still holds. Deployment is the owner's (`firebase deploy --only firestore:rules,firestore:indexes --project thenijobs-9f01d`). **Built and merged 2026-09-05.** Commits: claim, then `989daf4`. Gate after the last commit: typecheck 0 · lint 1 (10 errors / 755 warnings, unchanged) · **build 0 with real-firebase-config=yes** · secrets:bundle 0 · secrets:src-literals 0 · tracked-artefacts 0. **Evidence — 17 emulator checks** on demo project `demo-thenijobs-sec2` (Firestore + Auth emulators, three real signed-in users plus one signed-out caller, never production). All 17 behave as expected on these rules. **Revert-and-watch:** the identical suite against the previous rules flips **all eight attack checks to ALLOW** — a stranger rewinding the ID counter to 0, a stranger jumping it to 99 with an extra field, a user forging an audit entry attributed to someone else, a stranger opening a thread naming another person as the seeker, a participant impersonating the other party in a message, a stranger posting into someone else's thread, an unauthenticated enquiry naming a company that does not exist, and a 5,000-character unauthenticated enquiry — while **all eight legitimate operations stay ALLOW in both**: the identity service's own counter increment, a user logging their own action, a seeker opening their own thread, a participant posting as themselves, the `senderId: 'system'` opening line, and the employer replying. The malformed enquiry with no message is denied by both. **No regression:** the FIX-2 notification probe was re-run **unchanged** against these rules and all 11 of its checks still pass. Structurally, comparing the two rule files block by block, **28 of 34 blocks are byte-identical**; the six that differ are the four this phase owns, the messages subcollection (one of the four) and the outer container that encloses them. A note on method: the first block-comparison script reported '0 blocks changed', which was impossible given the diff. The bug was mine — the brace scan started at the `{` inside `{counterId}` rather than the block's own brace, so it compared identical short slices. Fixed and re-run before anything was trusted. Lanes: security PASS, rules PASS, skill PASS. **Findings carried out of the phase:** (a) **nothing in the tree creates a lead.** `leads` is read by the employer and admin pages, counted in stats and updated, but no create path exists in `src/`, so this rule guards a feature with no client and was, until now, an open unauthenticated write. (b) The lead shape disagrees three ways: the rule requires `name`, `employer/leads/page.tsx` reads `customerName`, and `admin/leads/page.tsx` and `lib/types` read `contactName`. Whoever builds the enquiry form must pick one; the rule was deliberately not made into a `hasOnly` allowlist because that would be inventing a schema. (c) `logActivity`'s `deleteCompany` caller defaults `adminId` to the literal string `'admin'`; that path is admin-only so the `isAdmin()` arm covers it, but the default is a latent trap. **Not verified:** production behaviour, because nothing is deployed. These rules change nothing for real users until the owner runs the deploy command above, and the emulator is a faithful but separate implementation. | `973c0bc` (2026-09-05, inside `Theni-Jobs-develop`) |
| `thenijobs/qual1-clear-ambient-lint-errors` | `2214418` | MERGED_DEVELOP | 2026-09-05 | owner ("start fixing all these bugs one by one", 2026-09-05) via phase QUAL-1 | **Make a red lint result mean something.** `npm run lint` has exited 1 on every gate run in this programme and on every one before it, with the same errors since `5b61111`. A check that is permanently red is a check nobody reads, which is how the two `react-hooks/immutability` errors that FIX-1 fixed — a recruiter's saved note silently discarded — sat in the tree unnoticed. The count is already down from 12 to 10 because FIX-1 fixed two; this phase takes it to 2. **In scope, 8 errors across 4 files:** `src/app/global-error.tsx:71` (twice, `@next/next/no-html-link-for-pages`); `src/app/sitemap.ts:123` and `:156` (`prefer-const`, both arrays only ever pushed to); `src/components/portfolio/SeekerSiteEditor.tsx:921` (twice, `react/no-unescaped-entities` — the label `Display "Open to Work" badge`); `src/components/portfolio/templates/SeekerPortfolioRenderer.tsx:683` (twice, the same rule, quotation marks around a testimonial). **`global-error.tsx` keeps its raw anchor**, with a targeted disable and a written reason rather than a rewrite. A global error boundary replaces the root layout after the layout itself has thrown; a client-side `Link` navigation asks the router that just failed to do the navigating. A full document load is the correct behaviour there, so the rule is silenced on that one line and nowhere else. `eslint.config.mjs` is out of scope: the configuration is never weakened to go green. **Two errors are deliberately left, and named**, so the gate's expected state becomes 2 rather than 0: `src/app/seeker/resume/builder/page.tsx:118` (a genuine use-before-declaration, and a real bug) and `src/components/home/TrendingJobs.tsx:399` (a raw anchor to `/employer/post-job`). Both sit in files the concurrent UIX-1 session owns. **Collision check at claim, and it changed the scope.** Worktrees = primary (`main`) + `Theni-Jobs-develop` (`62a7dd5`) + `Theni-Jobs-uiux1` (second session, `86ac5c4`) + this one. Diffing UIX-1's changed files against the intended may-write list returned **one hit**: they have already edited `src/components/home/TrendingJobs.tsx` (2 lines). That file was therefore **dropped from this phase before any work started**, which is the claim protocol working as designed. Worth recording that `src/components/home/**` is outside UIX-1's own declared scope, which named `src/components/dashboard/**`, the three portal layouts and the 58 dashboard pages — noted, not policed; it is the owner's call, not this session's. Expected files: `src/app/global-error.tsx`, `src/app/sitemap.ts`, `src/components/portfolio/SeekerSiteEditor.tsx`, `src/components/portfolio/templates/SeekerPortfolioRenderer.tsx`, this ledger. Never touches `eslint.config.mjs`, `firestore.rules`, `vercel.json`, `package.json`, `src/app/{admin,employer,seeker}/**`, `src/components/dashboard/**`, `src/components/home/**`. Lanes: uiux, feedback, seo, skill. Worktree: `/Users/saai_siddharth/Projects/Clients/Theni-Jobs-qual1`. Acceptance: lint reports exactly **2** errors, both named above; warnings do not increase; typecheck and build stay 0; and the escaped labels are read back from the built output rather than assumed to render correctly. **Built and merged 2026-09-05.** Commits: claim, then `2214418`. Gate after the last commit: typecheck 0 · **lint 1, now reporting 2 errors / 755 warnings** (from 10 / 755 at this phase's base, and 12 / 757 when this programme started) · **build 0 with real-firebase-config=yes** · secrets:bundle 0 · secrets:src-literals 0 · tracked-artefacts 0. Acceptance met exactly: the two remaining errors are `src/app/seeker/resume/builder/page.tsx:118` and `src/components/home/TrendingJobs.tsx:399`, the two named in the claim, both in files the concurrent UIX-1 session owns. Warnings did not move. **The escaped text was read back from the built output, not assumed.** In `out/_next/static/chunks`, the editor label compiles to `'Display "🟢 Open to Work" badge on portfolio'` — a real quotation mark, byte-identical to what rendered before — and the testimonial compiles to `["“", e.content, "”"]`. The testimonial is therefore a **deliberate visual change**, straight quotes to typographic ones, which is what `react/no-unescaped-entities` intends; the editor label is unchanged on screen. No double-escaped entity survives anywhere in `out/`. **`global-error.tsx` keeps its raw anchor on purpose.** A global error boundary replaces the root layout after the layout has thrown, so the router is not in a state worth trusting and a full document load is the correct recovery; `next/link` would ask the failed router to perform the navigation. The rule is disabled on that one line with the reasoning written beside it. `eslint.config.mjs` was not touched — the configuration is never weakened to reach green. Lanes: uiux PASS, feedback PASS, seo PASS (`sitemap.ts` behaviour unchanged; `const` on arrays that were only ever pushed to), skill PASS. **Findings carried out of the phase:** (a) the two remaining errors need an owner or a later phase — `seeker/resume/builder/page.tsx:118` is a **genuine use-before-declaration**, not a style complaint, and is the one real bug left in the lint output. (b) 755 warnings remain, 632 of them unused variables and imports; that is its own hygiene phase and must never be bundled with feature work. (c) The collision check moved this phase's scope before any work began: `src/components/home/TrendingJobs.tsx` was dropped because UIX-1 had already edited it. `src/components/home/**` lies outside UIX-1's own declared scope, which named `src/components/dashboard/**`, the three portal layouts and the 58 dashboard pages. Recorded for the owner, not acted on — this session does not police another branch's claim. **Not verified:** nothing here changes runtime behaviour except the testimonial's quotation marks, which were confirmed in the compiled chunk but not viewed on a rendered portfolio page, because that requires a published seeker portfolio with testimonials in production data. | `3483b1c` (2026-09-05, inside `Theni-Jobs-develop`) |
| `thenijobs/perf2-parallel-slug-resolution` | `f5b890b` | MERGED_DEVELOP | 2026-09-05 | owner ("make the app blazing fast", 2026-09-05) via phase PERF-2 | **A company page waits for up to six database round trips, one after another, before it can draw anything.** `CompanyProfilePageClient.tsx` resolves a slug by trying, strictly in sequence and each only after the previous returns empty: `slug` + verified, then the raw document id, then `aliases array-contains`, then `slugLower`, then an exact `name` match, then an owner-scoped lookup. `CompanyLandingPageClient.tsx` runs the same cascade minus the name branch, five deep. None of the branches feeds another, so the sequence buys nothing; the worst case is six serial latencies on the pages the business most wants fast, and the worst case is exactly a slug that does not resolve. **A second, unrelated defect found while reading it.** The fifth branch — `where('name', '==', slug.replace(/-/g,' '))` — is the only one with **no `verificationStatus == 'verified'` constraint**. RULES-1 added that constraint to the other branches so public list queries would satisfy the `companies` read rule, and missed this one. Firestore denies a list query it cannot prove safe for every possible result, so once the default-deny rules are deployed this branch will not merely return empty, it will **throw**, and because the whole cascade sits in one `try`, the throw aborts every remaining branch including the owner lookup. A signed-in owner previewing their own pending company would land on 'not found'. Fix: one `resolveCompanyBySlug()` in `src/lib/companySlug.ts` — the module that already owns slug logic — running every branch concurrently through `Promise.allSettled` and returning the first hit **in the same priority order as today**, so which company resolves does not change. `allSettled` also isolates a failing branch instead of letting it abort the others, which is what makes the rules-deploy behaviour safe. The missing verified constraint is added. **Scope extension, recorded before the change:** `src/lib/companySlug.ts` was not in the original may-write list. It is added deliberately rather than duplicating thirty lines into two clients, because duplication is what let the two cascades drift apart in the first place — the profile page has a name-match branch and the landing page does not, and nothing records why. Expected files: `src/lib/companySlug.ts`, `src/app/company/[slug]/CompanyProfilePageClient.tsx`, `src/app/[companySlug]/CompanyLandingPageClient.tsx`, this ledger. Never touches `src/components/**`, `src/app/globals.css`, `src/app/{admin,employer,seeker}/**`, `firestore.rules`, `vercel.json`, `firestoreServer.ts`. Lanes: security, uiux, feedback, seo, skill. **Collision check at claim:** worktrees = primary (`main`) + `Theni-Jobs-develop` (`6611a51`) + `Theni-Jobs-uiux1` (second session) + this one; UIX-1's changed files intersected with this may-write list is **empty** (diffed). Worktree: `/Users/saai_siddharth/Projects/Clients/Theni-Jobs-perf2`. Acceptance: the same slugs resolve to the same companies, checked read-only against real production data; the unresolvable case is measurably faster in a browser; an unresolvable slug still reaches TRUST-1's not-found state; typecheck and build stay 0 and lint does not regress from 2 errors. **Built and merged 2026-09-05.** Commits: claim, then `f5b890b`. Gate after the last commit: typecheck 0 · lint 1 (**2 errors / 755 warnings, unchanged** — the imports this change orphaned were removed rather than left to raise the count) · **build 0 with real-firebase-config=yes** · secrets:bundle 0 · secrets:src-literals 0 · tracked-artefacts 0. **Equivalence, against real production data rather than assumed.** Both algorithms were run over Firestore REST for four real verified slugs sampled live (`uthayam-air-travels`, `am-siddha-hospital-cumbum`, `velammal-matriculation-…`, `theni-college-of-arts-science-…`) and one nonexistent slug. All five resolve to the **same document id** under sequential and parallel resolution, including the nonexistent one resolving to null. **Latency, measured against the live database, median of three runs:** a slug that does not resolve — the worst case, where every branch runs — **543 ms → 134 ms (75% faster)**; a slug reachable only by the last branch **387 ms → 92 ms (76% faster)**. The acceptance line said 'measurably faster in a browser'; it was measured against the real database instead, which is the same round trips without a dev server's noise, and that substitution is recorded rather than glossed. **A latent defect fixed on the way.** The name-match branch was the only one lacking the `verificationStatus == 'verified'` constraint that RULES-1 added to the others so a public list query satisfies the `companies` read rule. Firestore refuses a list query it cannot prove safe for every possible result, so once default-deny is deployed that branch would **throw rather than return empty**, and because the entire cascade sat inside one `try` the throw would abort every remaining branch — including the owner preview, so a signed-in owner checking their own pending company would have seen 'not found'. `Promise.allSettled` isolates a failing branch, and the constraint is added. **Scope extension, recorded before the change:** `src/lib/companySlug.ts` was added to may-write so the resolver lives in the module that already owns slug logic. Duplicating it would have repeated the mistake that produced the drift being fixed — the profile page had a name-match branch, the landing page did not, and nothing recorded why. Lanes: security PASS (the change only narrows what a public query can match), uiux PASS, feedback PASS (TRUST-1's not-found state is reached unchanged), seo PASS, skill PASS. **Findings carried out of the phase:** (a) the landing client still carries several unused imports that predate this phase (`notFound`, `AlertCircle`, `ArrowLeft`, `ShieldAlert`, `BadgeCheck`); they belong to the 755-warning hygiene phase, not here. (b) The direct document-id branch remains second in priority, which means a slug that happens to equal a company's raw Firestore id still resolves to it — inherited behaviour, deliberately preserved, worth a product decision later since it exposes raw ids as public URLs. **Not verified:** no browser run of a real company page, because the equivalence and timing were established directly against the database, which is the layer this phase changes. The rendered page is unchanged above the data layer. | `ba450dd` (2026-09-05, inside `Theni-Jobs-develop`) |
| `thenijobs/perf3-bound-public-list-reads` | `3e8cf75` | MERGED_DEVELOP | 2026-09-05 | owner ("make the app blazing fast", 2026-09-05) via phase PERF-3 | **Every public list page reads every matching document.** `security.md` DATA-2: `/jobs`, `/businesses`, `/services`, `/daily-jobs` and `/businesses/[category]` call `getDocs` with no `limit()` and then filter and paginate in JavaScript. `/marketplace` is the only one with a ceiling, at 100. Nothing is visibly slow today, which is the reason to fix it now: the cost is invisible until it is not, and it scales linearly with the business. **Two measurements taken before writing any code changed the design.** (1) **Production volumes, read-only:** 2 active jobs, 104 verified companies, 0 services. So any sane ceiling is far above today's data and no user loses a result. (2) **The ordering this phase was going to add would have hidden a live job.** The contract's acceptance line asked for `limit()` **and** `orderBy()`, so a ceiling would mean 'the newest N' rather than an arbitrary N. Probing production: **zero** job documents carry `postedAt`, and only **1 of the 2** carries `createdAt`. Firestore's `orderBy` silently drops every document missing the field it sorts on — the same mechanism that broke the chat in FIX-1 — so adding `orderBy('createdAt')` to `/jobs` would have removed a live, applyable job from the board. **The orderBy is therefore deliberately not added**, and the phase's own stop condition ("if a ceiling cannot be set without hiding results, stop and report rather than guess") is honoured by reducing the change rather than by guessing a field. `jobs/page.tsx` already sorts client-side on `createdAt` with a `Date.now()` fallback, which tolerates the missing field; that sort stays. **A third finding falls out of the same probe:** four of the five declared `jobs` composite indexes are keyed on `postedAt` — `status+isActive+postedAt`, `postedBy+status+postedAt`, `category+isActive+postedAt`, `isActive+isFeatured+postedAt` — a field **no job document has**. They serve no query and cost write amplification on every job write. Removing them is an index deploy and its own decision, recorded here, not done here. So this phase does exactly one thing: **a generous explicit ceiling on each unbounded public read, plus an honest notice in the UI when that ceiling is actually reached**, so the page can never silently truncate. **Scope reduced at claim by the collision check.** UIX-1 has already edited `src/components/seo/LocationJobPageClient.tsx` and `src/components/seo/CategoryJobPageClient.tsx`, both of which were in the intended may-write list. Both are **dropped before any work started**; their unbounded reads are recorded as remaining work. Expected files: `src/app/jobs/page.tsx`, `src/app/businesses/page.tsx`, `src/app/services/page.tsx`, `src/app/daily-jobs/page.tsx`, `src/app/businesses/[category]/BusinessCategoryPageClient.tsx`, this ledger. Never touches `firestore.indexes.json` (the dead indexes are a separate decision), `firestore.rules`, `vercel.json`, `src/components/**`, `src/app/{admin,employer,seeker}/**`. Lanes: uiux, feedback, seo, skill. Worktree: `/Users/saai_siddharth/Projects/Clients/Theni-Jobs-perf3`. Acceptance: each read carries an explicit limit far above measured production volume; a page that reaches its ceiling says so on screen; no page shows fewer results than today; typecheck and build 0; lint no worse than 2 errors. **Built and merged 2026-09-05.** Commits: claim `e50a6e9`, then `3e8cf75`. Gate after the last commit: typecheck 0 · lint 1 (**2 errors / 755 warnings, unchanged**) · **build 0 with real-firebase-config=yes** · secrets:bundle 0 · secrets:src-literals 0 · tracked-artefacts 0. **All five reads bounded**, verified by grep on the merge result: `jobs/page.tsx`, `businesses/page.tsx`, `services/page.tsx`, `daily-jobs/page.tsx` and `businesses/[category]/BusinessCategoryPageClient.tsx` each carry `limit(500)`. **No page shows fewer results than before — checked in a browser, not assumed.** `next dev` in the phase worktree with real `NEXT_PUBLIC_FIREBASE_*` values: `/jobs` renders `2 verified career opportunities found` against production's 2 active jobs, and `/businesses` renders `104 Businesses` against production's 104 verified and active companies. Neither shows the cap notice, because neither is near the ceiling. Real Firebase credentials used: **yes**. **The ordering the plan asked for was dropped, and that is the most important line in this row.** The contract wanted `limit()` **and** `orderBy()` so a ceiling would mean 'the newest 500'. Probing production first: **zero** job documents carry `postedAt`, and only **1 of the 2** carries `createdAt`. Firestore's `orderBy` drops every document missing the sort field — the identical mechanism that hid half of every chat in FIX-1 — so adding it here would have removed a live, applyable job from the board in the name of performance. The phase's own stop condition covered this case and was honoured by shrinking the change rather than guessing a field. The existing client-side sort already falls back on a missing timestamp, so it stays. **Findings carried out of the phase:** (a) **four of the five declared `jobs` composite indexes are keyed on `postedAt`, a field no job document carries** — `status+isActive+postedAt`, `postedBy+status+postedAt`, `category+isActive+postedAt`, `isActive+isFeatured+postedAt`. They serve no query and cost write amplification on every job write. Removing them is an index deploy and a separate decision. (b) The job timestamp field is inconsistent (`createdAt` on one of two documents, `postedAt` on none), which blocks any server-side ordering of the job board until it is backfilled; that is an owner-run data migration, never an agent write. (c) `src/components/seo/LocationJobPageClient.tsx` and `CategoryJobPageClient.tsx` still read unbounded — **dropped from this phase at claim time** because the concurrent UIX-1 session had already edited both. **Not verified:** `/services`, `/daily-jobs` and `/businesses/[category]` were not opened in a browser; their change is the same one-line ceiling as the two that were, and production holds 0 services, so there was nothing to compare against there. | `5ca594a` (2026-09-05, inside `Theni-Jobs-develop`) |
| `thenijobs/dep1-next-security-upgrade` | `544f966` | MERGED_DEVELOP | 2026-09-05 | owner ("start fixing all these bugs one by one", 2026-09-05) via phase DEP-1 | **Nine dependency advisories, eight of them high, and the framework that compiles every page carries all nine.** `npm audit` at `74fb638`: 8 high, 1 moderate. `next` 16.2.7 alone is subject to server-side request forgery via rewrites with an attacker-controlled destination host, cache confusion for requests with bodies, unauthenticated disclosure of internal server-function endpoints, denial of service in the image-optimization API, middleware and proxy bypass, and an unbounded Server Action payload on the edge runtime. `postcss` and `sharp` are transitive under `next` and clear with the same bump. npm names the fix as **next 16.3.4, `isSemVerMajor: false`**. Honest scoping of the risk: production is a **static export**, so several of these advisories describe surfaces that do not exist there today — there is no middleware, no server actions and no image-optimization endpoint in `out/`. That is a mitigation, not a fix. The framework still compiles every page, `next dev` runs the real server locally, and `D-HOSTING` — the open decision that would turn the seven API routes into live functions — would put most of these surfaces into production the day it is resolved. Upgrading now is cheap; upgrading under a hosting migration is not. Expected files: `package.json`, `package-lock.json`, this ledger. **`src/**` is deliberately out of scope**, so a bump that quietly needs source changes cannot pass unnoticed: if the upgrade needs code edits, the phase stops and reports rather than widening. Never touches `next.config.ts`, `eslint.config.mjs`, `tsconfig.json`, `firestore.rules`, `vercel.json`. Lanes: security, config, skill. **Collision check at claim:** worktrees = primary (`main`) + `Theni-Jobs-develop` (`74fb638`) + `Theni-Jobs-uiux1` (second session, `2a582c1`) + this one; `ACTIVE` ledger rows on `develop` = 0. UIX-1's changed files intersected with `package.json` and `package-lock.json` is **empty** (diffed, not assumed). Worktree: `/Users/saai_siddharth/Projects/Clients/Theni-Jobs-dep1`. Acceptance: `npm audit` reports strictly fewer vulnerabilities, quoted before and after; typecheck 0; lint no worse than 2 errors; **build 0 with real credentials**; and the static export is compared against a pre-upgrade build **taken in this same worktree** — page count and route table — with any difference explained rather than waved through. The `xlsx` advisory is expected to survive: it has no patched version and is its own decision. **Built and merged 2026-09-05.** Commits: claim `9086011`, then `544f966`. Gate after the last commit: typecheck 0 · lint 1 (**2 errors / 755 warnings, unchanged**) · **build 0 with real-firebase-config=yes** · secrets:bundle 0 · secrets:src-literals 0 · tracked-artefacts 0. **Audit, before and after, same worktree:** `{high: 8, moderate: 1, total: 9}` across `brace-expansion, browserslist, js-yaml, nanoid, next, postcss, protobufjs, sharp, xlsx` → **`{high: 1, total: 1}`, `xlsx` alone**. `next` 16.2.7 → **16.3.4** cleared its own nine advisories plus `postcss` and `sharp`, which are transitive under it; `npm audit fix` without `--force` cleared the remaining five. **`xlsx` survives deliberately** — prototype pollution and a ReDoS with **no patched version published**. It parses admin-uploaded spreadsheets in the bulk importer, so the attacker must already be an admin, but there is no fix to apply and replacing the library is its own phase. **Export equivalence, compared against a pre-upgrade build taken in this same worktree with the same credentials:** html pages **463 → 463 with a byte-identical page set by path** (`diff` of the sorted find output was empty); export size **47M → 43M**; route table identical modulo formatting — 16.3.4 prints the SSG marker on the child rows rather than the parent, which the diff surfaced and which is cosmetic; build warning count unchanged at 2. **Two judgement calls, both recorded rather than silent.** (1) `npm install` rewrote `next` and `eslint-config-next` as `^16.3.4`; the repository had them **pinned exactly** at 16.2.7, so both were set back to exact `16.3.4`. Loosening dependency policy is a decision, not a side effect of running an upgrade command. (2) `next-env.d.ts` **is** committed here, against the standing hazard note that says never to stage it: the change is real and version-driven (`./.next/dev/types/routes.d.ts` → `./.next/types/routes.d.ts`) and was confirmed **stable across a second build**, which is what distinguishes it from the usual per-build churn. `src/**` was out of scope by design so a bump needing source changes would stop the phase; none was needed. Lanes: security PASS, config PASS, skill PASS. **Findings carried out of the phase:** (a) `xlsx` needs a decision — replace it, sandbox the parse, or accept an admin-only risk. (b) The static export means several of the closed advisories never reached production, but that is a property of `D-HOSTING` being unresolved, not of the dependency; resolving it in favour of running the routes would have exposed them. **Not verified:** no runtime comparison beyond the build. The upgrade was not exercised in a browser, because the export is byte-identical by page set and the phase touched no source; a behavioural difference would have to come from the framework runtime, which the next E2E pass on `develop` would surface. | `929d99a` (2026-09-05, inside `Theni-Jobs-develop`) |
| `thenijobs/sec3-remove-unused-rtdb` | `42edf1b` | MERGED_DEVELOP | 2026-09-05 | owner ("start fixing all these bugs one by one", 2026-09-05) via phase SEC-3 | **A second database nobody uses, downloaded by every visitor, and readable by the world.** `security.md` S-11. `src/lib/firebase/config.ts:6` imports `firebase/database`, `:29` calls `getDatabase(app)` at module scope so a Realtime Database client is constructed on every page load, and `:49` exports a `getFirebaseRtdb()` accessor. A repository-wide grep for `rtdb`, `getDatabase`, `getFirebaseRtdb` and `firebase/database` finds **no reference anywhere else in `src/`**. The client is created, its SDK ships in the shared chunk that all 463 pages load, and nothing ever reads or writes through it. Separately `database.rules.json` grants `".read": true` on three node trees — `companies`, `jobs` and `reviews`. A read-only probe on 2026-09-05 confirmed an anonymous shallow read of `/companies.json` on the live instance returns **200**, while `/users.json` correctly returns 401. So the rules are not theoretical: the instance answers unauthenticated reads today. It happens to hold nothing, because the app never wrote to it — but the tree is open, and 'empty' is a property of the data, not of the rules. Fix: delete the import, the `getDatabase` call and the accessor, and rewrite `database.rules.json` to deny by default. Both halves matter and neither substitutes for the other: removing the client stops shipping the SDK, and closing the rules stops the instance answering strangers whether or not any client exists. Expected files: `src/lib/firebase/config.ts`, `database.rules.json`, this ledger. Never touches `firestore.rules`, `storage.rules`, `src/app/**`, `src/components/**`, `src/hooks/**`, `package.json`, `vercel.json`, `next.config.ts`. **`NEXT_PUBLIC_FIREBASE_DATABASE_URL` stays in `.env.local` untouched** — removing an environment key is the owner's, and the application must not break while it is still set. Lanes: security, skill. **Collision check at claim:** worktrees = primary (`main`) + `Theni-Jobs-develop` (`fce798c`) + `Theni-Jobs-uiux1` (second session, `b5bf99b`) + this one; `ACTIVE` ledger rows on `develop` = 0; UIX-1's changed files intersected with `src/lib/firebase/config.ts` and `database.rules.json` is **empty** (diffed). Worktree: `/Users/saai_siddharth/Projects/Clients/Theni-Jobs-sec3`. Acceptance: the grep returns nothing outside the removal; the shared client chunk **shrinks by a measured number of bytes**, built before and after in this same worktree; the new rules are proven by an emulator probe that **succeeds on today's rules and fails on the new ones**; typecheck 0, build 0 with real credentials, lint no worse than 2 errors, and the export page set stays byte-identical. The rules only take effect on `firebase deploy --only database --project thenijobs-9f01d`, which is the owner's command, so the public read stays open until they run it. **Built and merged 2026-09-05.** Commits: claim `a0eef7c`, then `42edf1b`. Gate after the last commit: typecheck 0 · lint 1 (**2 errors / 755 warnings, the unchanged baseline**) · **build 0 with real-firebase-config=yes** · secrets:bundle 0 · secrets:src-literals 0 · tracked-artefacts 0. **Bundle measurement, built before and after in this same worktree with the same credentials:** largest chunk on the home page **501,544 → 393,411 bytes (−106 KB)**; total javascript across all chunks **5,231,493 → 5,119,502 bytes (−109 KB)**; html pages **463 → 463 with a byte-identical page set by path**. That chunk is referenced by every one of the 463 pages, so the saving is on every route rather than on the home page alone. **What is left of the name in the bundle is two strings** — `firebase/database` and `firebase/database-compat` in Firebase's internal component registry — while every implementation marker (`repoManagerDatabaseFromApp`, `DatabaseInternals`, `PersistentConnection`, `onDisconnect`, `ServerValue`) returns **0 matches across every chunk**. Grepping for the module name alone would have reported failure; grepping for the implementation is what showed it gone. **Rules evidence — a Realtime Database emulator probe on a demo project, never `thenijobs-9f01d`.** Under the new rules all five paths refuse an anonymous read (companies, jobs, reviews, users, applications). Under the old rules, restored for the run, **companies, jobs and reviews all allow it** while users and applications still refuse — so the probe is not vacuous, and the two paths that were already closed stay closed. **A near-miss worth recording.** The first run of that probe reported *everything* allowed, including paths that should already have been denied. The cause was mine: the emulator namespace was `demo-thenijobs-sec3` where the Realtime Database wants `demo-thenijobs-sec3-default-rtdb`, so the emulator had loaded **no rules at all** and was running wide open. A probe whose failure mode is silent openness will happily certify anything. What caught it was the assertion on `/users`, which had no business passing. **Every rules probe from here on asserts at least one path that must be denied.** **Live-instance confirmation before the change** (read-only, no data printed): anonymous shallow read of `/companies.json` returned **200**, `/users.json` returned **401**. The tree is empty only because the application never wrote to it, which is a property of the data and not of the rules. `database.rules.json` is written as **pure JSON with no comments** so a deploy cannot refuse to parse it; the reasoning lives in the commit message and here. **`NEXT_PUBLIC_FIREBASE_DATABASE_URL` was deliberately left in `.env.local` and in the environment.** Removing an environment key is the owner's, and nothing in the application breaks while it is still set. Lanes: security PASS, config PASS, skill PASS. **Owner action required — the rules are inert until deployed:** `firebase deploy --only database --project thenijobs-9f01d`. Until then the live instance keeps answering anonymous reads of `/companies`, `/jobs` and `/reviews`. **Not verified:** no browser run. The phase deleted an export that nothing imported and rewrote a rules file, so there is no user journey to exercise; the compile and the identical page set are what stand behind it. | `75eb600` (2026-09-05, inside `Theni-Jobs-develop`) |
| `thenijobs/fix3-employer-job-detail-404` | `a766491` | MERGED_DEVELOP | 2026-09-05 | owner ("start fixing all these bugs one by one", 2026-09-05) via phase FIX-3 | **Every employer who clicks one of their own job postings gets a 404 in production.** Confirmed against the live site on 2026-09-05, read-only: `GET https://www.thenijobs.com/employer/jobs/demo` returns **200**, `GET .../employer/jobs/<any-other-id>` returns **404**. `src/app/employer/jobs/[id]/page.tsx:3` returns `[{ id: 'demo' }]` from `generateStaticParams`, so the static export contains exactly one employer job page, and `vercel.json` has **no rewrite for `/employer/jobs/:path+`**. `src/app/employer/jobs/page.tsx:314` and `:443` both link to `/employer/jobs/${job.id}`, so the list page's own links are the ones that 404. **This repository already has a working pattern for exactly this, used by five other dynamic routes and not applied here.** Three parts: `generateStaticParams` includes the literal `'_fallback'`; `vercel.json` rewrites the prefix to that generated shell; and the client reads the real value from `usePathname()` instead of the build-time prop — `src/app/company/[slug]/CompanyProfilePageClient.tsx:15` carries the comment explaining why. `/employer/jobs/[id]` implements **none of the three**. Probed live: `/jobs/<unknown-id>`, `/company/<unknown-slug>` and `/portfolio/seeker/<unknown>` all return 200 through this mechanism. **Coupled second defect, which the routing fix would otherwise expose.** `src/hooks/useFirestore.ts:70` stores the caller's query constraints in a ref and `:105` lists only `[collectionName, options.skip, refreshKey]` as effect dependencies, so `useCollection` **never re-subscribes when a constraint changes**. Today that is mostly masked: all ~60 call sites pair each constraint with a `skip` that flips at the same moment. It stops being masked here — `EmployerJobDetailClient.tsx:126` pushes `/employer/jobs/<newId>` after Duplicate, a detail-to-detail navigation that does not unmount the component, so `useDocument` would follow the new job while `useCollection('applications', [where('jobId','==',jobId)])` kept streaming the **previous job's applicants**. Shipping the routing fix alone turns a 404 into an employer looking at the wrong candidates, which is worse. Both go in this phase or neither does. Fix: add `'_fallback'` to the employer route's static params, add the `vercel.json` rewrite, recover the real id from the URL in a **new** client wrapper, and make `useCollection` re-subscribe when the query genuinely changes, compared with the SDK's public `queryEqual`. Expected files: `src/app/employer/jobs/[id]/page.tsx`, `src/app/employer/jobs/[id]/EmployerJobDetailRoute.tsx` (new), `vercel.json`, `src/hooks/useFirestore.ts`, this ledger. **`src/app/employer/jobs/[id]/EmployerJobDetailClient.tsx` is deliberately NOT touched** — the second session's `thenijobs/uiux1-canonical-dashboard-system` has it open. The URL recovery goes in a new wrapper component instead of that file for exactly this reason; it is also the better separation, since routing recovery is not the detail page's concern. Never touches `firestore.rules`, `storage.rules`, `package.json`, `next.config.ts`. Lanes: worker, uiux, skill. **Collision check at claim:** worktrees = primary (`main`) + `Theni-Jobs-develop` (`0c7fc7d`) + `Theni-Jobs-uiux1` (second session, `b5bf99b`) + this one; `ACTIVE` ledger rows on `develop` = 0; UIX-1's 133 changed files intersected with my four source paths = **`EmployerJobDetailClient.tsx` only**, which is why it is out of scope. Its diff there is colour tokens and layout classes and touches neither `jobId` nor either hook, so the two phases do not disagree about behaviour. Worktree: `/Users/saai_siddharth/Projects/Clients/Theni-Jobs-fix3`. Acceptance: the export contains `employer/jobs/_fallback`; the `useCollection` change is proven by a **runtime check that fails without it**, not by inspection; typecheck 0, build 0 with real credentials, lint no worse than 2 errors; and the page count change is stated exactly. The 404 itself cannot be closed from here — the rewrite only takes effect when the owner deploys, so this phase's deliverable is the fix plus the measurement, not a green production probe. **Built and merged 2026-09-05.** Commits: claim `47685ce`, then `c41b767` (toolchain file, see below), then `a766491`. Gate after the last commit: typecheck 0 · lint 1 (**2 errors / 755 warnings, the unchanged baseline**) · **build 0 with real-firebase-config=yes** · secrets:bundle 0 · secrets:src-literals 0 · tracked-artefacts 0 · dirty-after 0. Neither new file contributes a warning. **Routing evidence — the built export served under Vercel's own order (filesystem first, then rewrites), by a script written for the purpose:** `/employer/jobs/abc123XYZ` **404 → 200**, and **404 again** when only the rewrite is removed from `vercel.json` and nothing else changes. `/employer/jobs/demo` and `/employer/jobs/_fallback` stay 200 in both runs, so the change adds a path rather than moving one. **Id-recovery evidence.** The recovery is three lines behind an auth guard, which makes it unobservable, so it was extracted as an exported `resolveJobIdFromPath` and exercised through a **temporary unguarded route inside the real export, through the real rewrite**. Result: built with `paramId` `_fallback`, pathname `/fix3route/abc123XYZ`, **resolved `abc123XYZ`**. The temporary route and a temporary hook-probe page were deleted before the fix commit; the merged tree contains neither, checked with `ls-tree`. **Hook evidence — measured in a browser against the real hook**, changing only a `limit` while `skip` stayed false throughout, which is precisely the case the old code got wrong. limit 1→3→1→3 returns **1, 3, 1, 3** with the fix and **1, 1, 1, 1** with the pre-fix hook restored in place and re-run. No console errors and no re-render loop, so `queryEqual` is not oscillating. **Export:** 463 → **464** html pages; `diff` of the sorted page lists is exactly one line, `/employer/jobs/_fallback.html`. **Three things found while doing this, all recorded rather than fixed here.** (a) `AGENTS.md` is rewritten by `next dev` at 16.3.4 — the generator is `node_modules/next/dist/server/lib/generate-agent-files.js`, which exists. Restoring the file and re-running `next dev` reproduced the **identical** text, the same stability test DEP-1 used for `next-env.d.ts`, so it is committed as its own commit `c41b767`; leaving it would start every later phase on a dirty tree. (b) The worktree was created by copying `node_modules` from the primary checkout, which is at `main` and therefore **pre-DEP-1**: `next` 16.2.7 was installed while `package.json` pinned 16.3.4. Caught before any measurement was recorded and fixed with `npm ci`; **every number above was taken on 16.3.4**. The earlier SEC-3 phase almost certainly measured on 16.2.7 for the same reason, which does not change its conclusion (a deleted export and a rules file) but is worth knowing. **From here, `npm ci` in a new worktree, never a copied `node_modules`.** (c) A stale `.next/dev/types/validator.ts` left by the dev server referenced the deleted probe routes and failed the next build; a clean build fixed it. **A fourth, unrelated, still open:** several pages render a doubled title — `Company Profile | THENIJOBS | THENIJOBS`, `Page Not Found | THENIJOBS | THENIJOBS` — a metadata template applied to a title that already carries the suffix. Seen on live production, not fixed here, not in scope. Lanes: worker PASS, uiux PASS (no rendered output changed; the new component renders the same child), skill PASS. **Owner action required — the 404 stays live until deployed.** The rewrite is a `vercel.json` change, so it reaches production with the next deploy of `main`; there is no separate command for it. **Not verified:** no authenticated employer journey. Opening a real job's detail page needs an employer account with postings, and creating one would write to `thenijobs-9f01d`. What is proven is that the URL stops returning 404 and that the id the page receives is the one in the address bar; what is not proven is the rendered detail page for a real job. **Findings carried out:** `useCollection` is now correct for changing constraints, which removes the blocker on adding real filter UI to any list page — several admin pages have filters implemented in client-side `.filter()` over a whole-collection read that could now become server-side queries (`DATA-1`). | `dcecd48` (2026-09-05, inside `Theni-Jobs-develop`) |
| `thenijobs/seo2-doubled-page-titles` | `6cae8f8` | MERGED_DEVELOP | 2026-09-05 | owner ("start fixing all these bugs one by one", 2026-09-05) via phase SEO-2 | **362 of the 464 exported pages ship a `<title>` that ends `| THENIJOBS | THENIJOBS`.** Measured from the built export at `1383c9f`, not estimated: 78% of the site. Live on production today — `https://www.thenijobs.com/company/<any-slug>` returns `Company Profile | THENIJOBS | THENIJOBS` and the 404 page returns `Page Not Found | THENIJOBS | THENIJOBS`. Google truncates around 60 characters, so a wasted repeat of twelve characters costs real words on every company profile, every category page and every job-location page. **Cause, and it is not a typo repeated 362 times.** `src/app/layout.tsx:28` sets `title.template = "%s | THENIJOBS"`, and most page titles already end with the same suffix. But it only doubles on *some* routes, which is the part worth writing down: `metadata.title` is a single field, so a nested layout that sets `title` to a plain string **replaces the whole inherited object, template included**, and its children then get no template at all. `src/app/jobs/layout.tsx` and `src/app/businesses/layout.tsx` do exactly that — which is why `/jobs` doubles while `/jobs/demo` does not, and why `/businesses` doubles while `/businesses/all` does not. Two conventions coexist in one codebase and neither is visible at the point of use. Fix: **delete the template and let every title stand on its own.** The alternative — keep the template and strip the suffix from the titles that double — cannot be applied uniformly, because the titles under `/jobs/**` and `/businesses/**` receive no template and would lose their suffix entirely. Keeping it means the correct way to write a title depends on whether some ancestor happens to set one, which is the interaction that produced the bug. `title.default` stays, so every page that sets no title of its own is unaffected. Expected files: `src/app/layout.tsx`, plus whichever titles turn out to rely on the template for their only suffix — determined by rebuilding and diffing all 464 titles, not by guessing — and this ledger. Never touches `firestore.rules`, `storage.rules`, `vercel.json`, `package.json`, `next.config.ts`, or any client component. Lanes: uiux, skill. **Collision check at claim:** worktrees = primary (`main`) + `Theni-Jobs-develop` (`1383c9f`) + `Theni-Jobs-uiux1` (second session, `b5bf99b`) + this one; `ACTIVE` ledger rows on `develop` = 0; all fourteen candidate files intersected with UIX-1's 133 changed files = **empty**, diffed rather than assumed. UIX-1 holds the `page.tsx` of several of these routes but none of their `layout.tsx` or metadata. Worktree: `/Users/saai_siddharth/Projects/Clients/Theni-Jobs-seo2`. Acceptance: **every one of the 464 titles compared before and after**, with each change explained and no page left without a suffix; doubled count 362 → 0; typecheck 0, build 0 with real credentials, lint no worse than 2 errors; page count unchanged at 464. The production pages only change when the owner deploys. **Built and merged 2026-09-05.** Commits: claim `bc7af28`, then `6cae8f8`. Gate after the last commit: typecheck 0 · lint 1 (**2 errors / 755 warnings, the unchanged baseline**) · **build 0 with real-firebase-config=yes** · secrets:bundle 0 · secrets:src-literals 0 · tracked-artefacts 0 · dirty-after 0. **All 464 titles compared, before against after, as the acceptance demanded:** page set **identical by path**; **doubled suffix 362 → 0**; **366 titles changed and every one of them the removal of exactly one duplicate suffix**; **0 changes that were anything else**; **0 pages left whose title does not name THENIJOBS**. **Revert-and-watch:** reverting `6cae8f8` in the worktree and rebuilding returns the count to **exactly 362**, so the check is not vacuous. **Five changed titles were not doubled by the strict test, and each was decided rather than swept along.** `/about`, `/contact`, `/privacy` and `/terms` already name THENIJOBS inside the title, so they keep it and gain nothing from a trailing repeat — appending the suffix would recreate the very repetition being removed. `/cookies` was bare `Cookie Notice` with no brand at all, so the suffix was added to its own metadata. That is why the final count is 366 and not 367. **The root title is now a plain string rather than `{ default }`.** Next's type for the object form requires a `template` beside it, and the template is the thing being removed. A plain string is inherited by every page that sets no title, which is all of `/admin`, `/employer` and `/seeker`, and the 97 unchanged titles are the proof that their fallback did not move. **Why the template went rather than the suffixes.** Stripping the suffix from the doubling titles cannot be applied uniformly: titles under `/jobs/**` and `/businesses/**` receive **no** template, because those layouts set a plain-string `title` that replaces the inherited object wholesale, and stripping theirs would leave them with no brand at all. Keeping the template means the correct way to write a title depends on whether some ancestor happens to set one — invisible at the point of use, and precisely the interaction that produced 362 broken pages. Lanes: uiux PASS, skill PASS. **Owner action required:** the titles in production change with the next deploy of `main`; there is no separate command. Worth re-submitting the sitemap afterwards so the corrected titles are picked up rather than waiting for a natural recrawl. **Not verified:** no search-engine behaviour, only the emitted HTML. What is proven is that every `<title>` in the export is correct and that no page lost its brand. **Finding carried out:** `/contact`, `/privacy` and `/terms` still read `... | THENIJOBS — ...`, using the brand as a mid-title separator. Not wrong and not touched here, but if a house style for titles is ever written down, those are the three that will not match it. | `b77c098` (2026-09-05, inside `Theni-Jobs-develop`) |
| `thenijobs/seo3-sitemap-advertises-404s` | `7284330` | MERGED_DEVELOP | 2026-09-05 | owner ("start fixing all these bugs one by one", 2026-09-05) via phase SEO-3 | **The live sitemap submits 105 URLs to search engines that return 404.** Every one of the 374 URLs in `https://www.thenijobs.com/sitemap.xml` was fetched read-only on 2026-09-05, following the apex redirect: **269 return 200, 105 return 404**. Twenty-eight per cent of what the site tells Google to crawl does not exist. The 105 are one shape: `/jobs-in-<location>/<category>` for the **seven** locations that have a landing page but no `[category]` route — andipatti, bodinayakanur, chinnamanur, dindigul, madurai, periyakulam, uthamapalayam, fifteen categories each. `src/app/jobs-in-theni/[category]/` and `src/app/jobs-in-cumbum/[category]/` exist; the other seven directories contain only `page.tsx`. Their landing pages are fine — `/jobs-in-madurai` returns 200 — so this is an unfinished rollout, not an absent feature. **Why it drifted, which is the part worth fixing.** `src/app/sitemap.ts` hand-maintains its own `LOCATIONS` (9) and `CATEGORIES` (15) arrays, while `src/components/seo/locationData.ts` holds `LOCATIONS_DATA` (9) and `CATEGORIES_LIST` (15) which the routes actually use. The two lists agree exactly — the sitemap is not wrong about the data, it is wrong about which routes exist, and nothing connects the two so nothing could have caught it. Fix, in the direction the deployed sitemap already promises: **add the seven missing `[category]` routes** rather than deleting 105 URLs from the sitemap. Deleting would shrink the site's crawlable surface, a product change nobody asked for; the sitemap has been advertising these pages for as long as it has been deployed, and the implementation is entirely location-agnostic — `createCategoryMetadata`, `getCategoryStaticParams` and `CategoryJobPageClient` all take the location as a parameter, and the two existing route files differ **only** by a location string. Then derive the sitemap's lists from `locationData` so the two cannot disagree again. Expected files: seven new `src/app/jobs-in-<location>/[category]/page.tsx`; the two existing ones for theni and cumbum, converted to the same shape so nine copies cannot drift; one new shared `src/components/seo/LocationCategoryPage.tsx` holding the breadcrumb schema that is currently duplicated; `src/app/sitemap.ts`; this ledger. Never touches `firestore.rules`, `storage.rules`, `vercel.json`, `package.json`, `next.config.ts`, `src/app/layout.tsx`. **`src/components/seo/CategoryJobPageClient.tsx` is imported but NOT modified** — the second session's branch has it open. Importing a file another branch is editing is not a conflict, and it is the desirable outcome here: their changes will apply to all nine locations rather than two. Lanes: uiux, skill. **Collision check at claim:** worktrees = primary (`main`) + `Theni-Jobs-develop` (`a804fc5`) + `Theni-Jobs-uiux1` (second session, `b5bf99b`) + this one; `ACTIVE` ledger rows on `develop` = 0; UIX-1's 133 changed files intersected with everything this phase writes = **empty** — it touches no `jobs-in-*` path, not `sitemap.ts`, not `locationData.ts`, not `locationPageFactory.ts`. Worktree: `/Users/saai_siddharth/Projects/Clients/Theni-Jobs-seo3`. Acceptance: the export gains **exactly 105** pages, all of them the named location-category pairs; **every URL the sitemap emits corresponds to a generated page**, checked mechanically rather than by eye; the 30 pages that already existed are unchanged; typecheck 0, build 0 with real credentials, lint no worse than 2 errors. The live 404s only stop when the owner deploys. **Built and merged 2026-09-05.** Commits: claim `ab15321`, then `7284330`. Gate after the last commit: typecheck 0 · lint 1 (**2 errors / 755 warnings, the unchanged baseline**) · **build 0 with real-firebase-config=yes** · secrets:bundle 0 · secrets:src-literals 0 · tracked-artefacts 0 · dirty-after 0. **Measured, same worktree, built before and after:** html pages **464 → 569 — exactly +105, none removed**, and the added set is precisely the seven named families at fifteen categories each, checked by diffing the sorted page lists rather than by counting. Sitemap URLs **376 → 384**, the whole of the +8 being business categories. **Sitemap URLs that would 404: 105 → 0.** **How that last number was obtained**, because it is the one that matters: a script resolves every `<loc>` in the built `sitemap.xml` against the built export **the way Vercel does — filesystem first, then the `vercel.json` rewrites** — rather than trusting that a route exists because a directory does. All 384 now resolve **from the filesystem**; none need a rewrite to be found. **Revert-and-watch:** reverting `7284330` and rebuilding returns the export to 464 pages and the checker to **exactly 105 would-404 URLs, in exactly the seven named families** — the same number the live production sweep found, which is what makes the local check trustworthy as a stand-in for production. **Live evidence that opened the phase:** all 374 URLs of `https://www.thenijobs.com/sitemap.xml` fetched read-only on 2026-09-05, following the apex redirect — **269 × 200, 105 × 404**. **The judgement call, stated rather than buried.** Two fixes were available: delete 105 URLs from the sitemap, or build the 105 pages. Deleting shrinks the crawlable surface, which is a product decision nobody asked for; the deployed sitemap has been promising these pages for as long as it has existed, the landing pages already return 200, and the implementation was never location-specific — `createCategoryMetadata`, `getCategoryStaticParams` and `CategoryJobPageClient` all take the location as an argument and the theni and cumbum route files differed from one another **only** by that string. Completing the rollout honours what is already published. If the owner would rather have seven fewer location families, deleting them is a smaller change than this one was. **Two structural fixes beyond the symptom.** (a) Nine near-identical route files would have drifted the same way, so the shared body moved to `src/components/seo/LocationCategoryPage.tsx` and each route now carries a single `LOCATION` constant; theni and cumbum were converted too, and the breadcrumb each spelled out by hand is now derived from `LOCATIONS_DATA`. (b) `sitemap.ts` hand-maintained copies of the location and category lists. They were not wrong about the **data** — they matched `locationData` exactly — they were wrong about which **routes** existed, and nothing connected them so nothing could notice. All three lists are now derived from what the routes read. **Mirror defect found while doing (b):** the sitemap listed **ten** business categories while the route generated **eighteen** real pages, so eight existed and were advertised to nobody. Both now read `src/lib/seo/businessCategories.ts`. `_fallback` is exported separately and deliberately excluded from the sitemap — it is a rewrite shell, not a destination. **`src/components/seo/CategoryJobPageClient.tsx` was imported but not modified** — the second session's branch has it open. Its changes will now apply to all nine locations rather than two, which is the desirable direction. Lanes: uiux PASS, skill PASS. **Owner action required:** the 404s stop with the next deploy of `main`. Worth **resubmitting the sitemap in Search Console afterwards** rather than waiting for a natural recrawl, since 105 URLs move from 404 to 200 at once. **Not verified:** no rendered check of the 105 new pages beyond the build. They are the same component as the 30 that already existed for theni and cumbum, with the location supplied the same way, so a difference would have to come from `CategoryJobPageClient` treating a location specially — it does not, it only forwards the slug. **Finding carried out:** the sitemap emits `https://thenijobs.com/...` while the site serves `www.thenijobs.com` and **308-redirects the apex**, so every sitemap URL is a redirect. `metadataBase` and every `alternates.canonical` in the codebase also say apex. Which host is canonical is an owner decision, not a bug to fix silently, but the sitemap and the server currently disagree. | `5f9beb4` (2026-09-05, inside `Theni-Jobs-develop`) |
| `thenijobs/pay1-checkout-bypass-and-price-mismatch` | `32bb8dd` | MERGED_DEVELOP | 2026-09-05 | owner ("start fixing all these bugs one by one", 2026-09-05) via phase PAY-1 | **The paid checkout cannot succeed, and the one path that does succeed is a free-subscription bypass.** Two defects that only make sense read together. **(1) Every legitimate payment is rejected.** `src/app/api/payment/verify/route.ts:15-21` hard-codes `PLAN_PRICES` as free 0, basic 999, standard 2999, premium 7999, enterprise 14999. The real prices are `SUBSCRIPTION_PLANS` in `src/lib/constants.ts` — free 0, **standard 480, premium 1200, enterprise 5000** — and `PaymentCheckoutModal.tsx:182` sends `amount: plan.price` from exactly that list. The route's own amount check (`:107`, labelled "H12 FIX") therefore compares 480 against 2999 and returns **"Payment amount does not match the selected plan. Please contact support."** for every paid plan. `basic` is not even a plan any more; the comment above the table says "must match pricing page" and it does not. **(2) Blocking one script grants a paid subscription.** `PaymentCheckoutModal.tsx:38-50` resolves `false` when `checkout.razorpay.com` fails to load — an ad blocker, a corporate proxy, DevTools request blocking, or no network. `:169-175` then takes an else-branch that calls `handleVerifyPayment(orderId, 'pay_direct_' + Date.now(), 'direct_authorized')`: **the client fabricates a payment id and a signature and asks the server to activate the subscription, with no money involved.** The server accepts it, because `verify/route.ts:73` only verifies the signature `if (razorpaySecret && paymentId && signature)` — with `RAZORPAY_KEY_SECRET` unset it skips verification entirely and proceeds to write an active one-year subscription and set `isPremium: true`. Fail-open, on the money path. **Honest scoping: none of this is exploitable today.** Production is a static export, so both routes 404 — verified live on 2026-09-05 — and the client's `orderRes.json()` throws on the 404 HTML, which is why paid checkout currently fails with a network error rather than a bypass. This is a **latent** P0: it becomes live the day `D-HOSTING` resolves toward running the routes, which is precisely when nobody will be reading this file. **Two further defects on the same path.** (a) Every Firestore write in the route goes through the REST API with `NEXT_PUBLIC_FIREBASE_API_KEY` **as an unauthenticated request** — a web API key is not an authorization credential — and **not one of the five `fetch` results is checked**, so the route returns `success: true` whether or not anything was written. Once RULES-1's default-deny rules are deployed, all five are denied and the user is told their subscription is active while nothing happened. (b) `:158` and `:183` set `isPremium: true` unconditionally, including for `planSlug: 'free'`, whose zero price skips the amount check entirely. Fix, limited to what is a contradiction or a fail-open rather than a design choice: derive `PLAN_PRICES` from `SUBSCRIPTION_PLANS` so there is one source of truth and the amount check starts meaning something; delete the fabricated-payment fallback so a gateway that cannot load produces an error; make signature verification **fail closed**; check the write responses and report failure honestly; and stop granting `isPremium` for a zero-price plan. **Deliberately NOT done, and recorded as an owner decision rather than guessed at:** moving verification to a Razorpay webhook, looking the order up server-side through the Razorpay API instead of trusting the client's `amount`, and writing through the Admin SDK instead of unauthenticated REST. That is the correct architecture and it is a redesign, not a repair; this phase makes the existing flow fail safely instead of failing open. Expected files: `src/app/api/payment/verify/route.ts`, `src/components/payment/PaymentCheckoutModal.tsx`, this ledger. `src/lib/constants.ts` is **imported, not modified** — the prices there are the owner's and this phase must not change what anything costs. Never touches `firestore.rules`, `storage.rules`, `vercel.json`, `package.json`, `src/lib/plans.ts`. Lanes: security, feedback, skill. **Collision check at claim:** worktrees = primary (`main`) + `Theni-Jobs-develop` (`cb467e4`) + `Theni-Jobs-uiux1` (second session, `b5bf99b`) + this one; `ACTIVE` ledger rows on `develop` = 0; both files intersected with UIX-1's changed files = **empty**. Worktree: `/Users/saai_siddharth/Projects/Clients/Theni-Jobs-pay1`. Acceptance: the bypass path is **gone from the built bundle**, checked by grepping the export for the fabricated marker rather than by reading the diff; the price table is proven to match `SUBSCRIPTION_PLANS` by a check that fails when they diverge; **the route is exercised as a running server** — `next dev`, real requests — showing a legitimate payment accepted and the bypass refused, with revert-and-watch on both; typecheck 0, build 0 with real credentials, lint no worse than 2 errors; export page count unchanged. **This changes nothing in production, where both routes 404.** **Built and merged 2026-09-05.** Commits: claim `1811317`, then `32bb8dd`. Gate after the last commit: typecheck 0 · lint 1 (**2 errors / 755 warnings, the unchanged baseline**) · **build 0 with real-firebase-config=yes** · secrets:bundle 0 · secrets:src-literals 0 · tracked-artefacts 0 · dirty-after 0. Export unchanged at 569 pages. **The bypass was reproduced, not inferred.** `next dev` with `RAZORPAY_KEY_SECRET` unset — the state of every environment this repository has — and `NEXT_PUBLIC_FIREBASE_PROJECT_ID` overridden to `demo-pay1-probe` **specifically so that a request passing every check could not write to `thenijobs-9f01d`**. Against the pre-fix route: `POST /api/payment/verify` with `paymentId: "pay_direct_..."`, `signature: "direct_authorized"`, `planSlug: "standard"`, `amount: 2999` returned **HTTP 200 `{"success":true,"message":"Payment verified and subscription activated successfully!"}`**. An unknown plan slug `platinum` with `amount: 0` returned the same 200. After the fix, both return **403**. **The two bugs were masking each other, which is the part worth remembering.** The same bypass payload at the *real* price of 480 was rejected by the pre-fix route — but by the **amount** check, not the signature check, because the stale table wanted 2999. The bypass was reachable by sending the number the broken table expected. Fixing the price table alone would have opened it; fixing the signature alone would have left every genuine payment rejected. **Probe: seven cases, all as expected** — old bypass 403 · no signature 403 · amount 1 for a 480 plan 400 · **amount 2999, what the stale table demanded, 400** · unknown plan 400 · free plan 400 · **legitimate standard at 480 passing every check and then returning 500** when the writes were refused by a project that is not ours. That last one is the demonstration of the write check: the old code returned 200 there. **Bundle check:** `direct_authorized`, `pay_direct_` and `Fallback direct verified` each appear in **0 files** of `out/`, against **3 files** for the new error text — the sanity control that proves the grep would have found them. **Scope discipline.** Every change is a contradiction or a fail-open, not a design preference: prices **derived** from `SUBSCRIPTION_PLANS` (the owner's numbers, imported and not modified); signature verification **fail-closed**; the client no longer authorises its own payments, and the Razorpay callback may no longer substitute an invented id or an empty signature; unknown and zero-price plans refused rather than skipped; the four subscription-granting writes **checked**; and the 500 no longer claims "Database state was protected", which five untransacted REST calls cannot promise. **Deliberately NOT done — owner decision, recorded rather than guessed:** verify through a **Razorpay webhook**, look the order up **server-side** through the Razorpay API instead of trusting the client's `amount`, and write through the **Admin SDK** instead of unauthenticated REST. That is the correct architecture and it is a redesign. This phase makes the existing flow fail safely. **A coupled fact the owner needs.** Those writes carry `NEXT_PUBLIC_FIREBASE_API_KEY`, which is an API key and **not an authorization credential** — the requests are unauthenticated as far as security rules are concerned. Under RULES-1's default-deny rules **every one of them is denied**, so the moment those rules are deployed AND the payment routes are made to run, a paying customer gets a 500 rather than a silent nothing. The 500 is the improvement; the architecture is still owed. Lanes: security PASS, feedback PASS (three user-facing messages rewritten to say what actually happened and whether money moved), skill PASS. **Not verified:** no real Razorpay transaction, and no successful end-to-end activation — proving that would require production credentials and a write to `thenijobs-9f01d`. What is proven is every refusal path, and that a legitimate request passes all validation and fails honestly at the write. **Findings carried out:** (a) `create-order` still trusts the client's `amount` when constructing the Razorpay order, so the gateway charges whatever the browser asked for; the verify-side check now catches the mismatch afterwards, but the order should be priced server-side. (b) `PLAN_FEATURE_MATRIX` and `SubscriptionPlanSlug` still carry the legacy `basic` slug that `SUBSCRIPTION_PLANS` dropped — harmless now that prices are derived, but it is the same stale-copy pattern. | `c8961d5` (2026-09-05, inside `Theni-Jobs-develop`) |
| `thenijobs/ai1-identity-from-request-body` | `8e50278` | MERGED_DEVELOP | 2026-09-05 | owner ("start fixing all these bugs one by one", 2026-09-05) via phase AI-1 | **`/api/ai` took the caller's identity from the request body, so anyone could spend anyone's AI credits and the owner's provider quota.** The same fail-open shape as PAY-1, three steps deep and every one of them reachable. `src/app/api/ai/route.ts:45` destructured `userId` straight out of the JSON body and used it for the credit check (`:113`), the rate-limit bucket (`:104`), the usage log and `deductUserCredits` (`:327`). `:71` verified a Firebase token **only `if (authHeader)`** — and `src/lib/ai/aiClient.ts:26-30` sends nothing but `Content-Type`, so **the verification never ran for a single real request in the application's life**. And `:95-99` caught any verification error and let the request continue anyway, under a comment calling it graceful degradation. The effect: `POST /api/ai {feature:'resume_builder', userId:'<any uid>'}` runs a paid model call, **deducts that user's credits**, and writes a usage log in their name. `userRole` is body-supplied too, so `{feature:'chatbot', userRole:'GUEST'}` skipped the guard entirely by the caller's own say-so. **Second endpoint, same phase.** `src/app/api/ai/test/route.ts` has **no authentication at all** and, when the caller sends no `apiKey`, falls back to the **server's** `GROQ_API_KEY` / `GEMINI_API_KEY` / `OPENAI_API_KEY` and makes a real provider call. Anyone able to POST there could spend the owner's quota and use the answer as an oracle for whether those keys are live, without ever holding one. **Honest scoping: not exploitable today.** Both routes 404 under the static export — verified live. This is latent, exactly like PAY-1, and goes live with `D-HOSTING`. Fix: identity comes from the verified token or the request is a guest, and the body cannot influence it; verification runs unconditionally when a token is present and **fails closed**; the guest chatbot is identified by the **absence** of a verified user rather than by a role in the body; `aiClient` attaches the id token, without which tightening the server would simply break all fifteen features; and `ai/test` requires the caller to bring their own key. **Deliberately incomplete, and recorded rather than half-done:** `ai/test` still needs an **admin** check. Adding one means changing `src/app/admin/ai-settings/page.tsx` to send a token, and that file is held by the second session's branch. Removing the server-key fallback closes the part that costs the owner money; the authorization gap stays open and is its own phase. Expected files: `src/app/api/ai/route.ts`, `src/app/api/ai/test/route.ts`, `src/lib/ai/aiClient.ts`, this ledger. Never touches `src/app/admin/**`, `firestore.rules`, `storage.rules`, `package.json`, `src/lib/ai/config.ts`. Lanes: security, skill. **Collision check at claim:** worktrees = primary (`main`) + `Theni-Jobs-develop` (`ce275e5`) + `Theni-Jobs-uiux1` (second session, `b5bf99b`) + this one; `ACTIVE` ledger rows on `develop` = 0; all three source files intersected with UIX-1's changed files = **empty**. `src/app/admin/ai-settings/page.tsx` **does** collide, which is why the admin check is out of scope. Worktree: `/Users/saai_siddharth/Projects/Clients/Theni-Jobs-ai1`. Acceptance: **the route is exercised as a running server** — a request naming another user's uid with no token must be refused where it previously ran, proven by revert-and-watch against the pre-fix route; the guest chatbot must still work without a token; `ai/test` must refuse a request that sends no key; typecheck 0, build 0 with real credentials, lint no worse than 2 errors; export page count unchanged. **No credit may be deducted from any real user during the probe** — it runs against a demo project id. **Built and merged 2026-09-05.** Commits: claim `3051dc7`, then `8e50278`. Gate after the last commit: typecheck 0 · lint 1 (**2 errors / 755 warnings, the unchanged baseline**) · **build 0 with real-firebase-config=yes** · secrets:bundle 0 · secrets:src-literals 0 · tracked-artefacts 0 · dirty-after 0. Export unchanged at 569 pages. **Reproduced on a running server**, `next dev` with `NEXT_PUBLIC_FIREBASE_PROJECT_ID=demo-ai1-probe` so no real account's credits could move. `POST /api/ai {feature:"cover_letter", userId:"victim-uid-12345"}` with **no token**: the pre-fix route **never returned 401** — it went past authentication and stalled inside the credit and usage writes for that uid, visible in the server log as a run of `PERMISSION_DENIED` retries against the demo project. The fixed route returns **401 in 0.63s**. **A precise nuance, and the opposite of the usual shape.** The old guard **rejected a bad token** with 401 — it only failed open when **no token was sent at all**, which is exactly what `aiClient.ts` did on every request it ever made. An attacker probing it clumsily would have concluded it was sound. **`/api/ai/test`, demonstrated with a placeholder `GROQ_API_KEY` in the environment so the fallback was actually reachable:** an anonymous `{"provider":"groq"}` with no key returned **HTTP 200 having really called `api.groq.com` with the server's key** — `latencyMs 265` and the provider's own `"Invalid API Key"` in the body. With a live key it would have succeeded. Now **400**. **Probe: eight cases, all as specified** — impersonation with no token 401 · impersonation with an invented `userRole` 401 · guest claim on a credited feature 401 · garbage bearer token 401 · no feature 400 · `ai/test` with no key 400 for groq, gemini and an all-whitespace key. Plus two behaviour checks the fix had to preserve: the **guest chatbot still passes the gate without a token** (500 downstream from the absent provider key, which is the point — it was not refused), and a guest **cannot** promote itself to `full_resume_generation` (401). **Merged tree verified directly:** `authenticatedUserId` appears 12 times in `src/app/api/ai/route.ts` and the only remaining `userId` occurrences are two explanatory comments and the two `userId: authenticatedUserId` log fields. Nothing reads an identity from the body. **Deliberately incomplete, recorded rather than half-done:** `/api/ai/test` still has **no authorization** — it needs an admin check, which means changing `src/app/admin/ai-settings/page.tsx` to send a token, and that file is held by the second session's branch. Removing the server-key fallback closes the part that costs the owner money; the authorization gap is its own phase and should follow UIX-1's merge. **A mistake worth recording.** While saving the pre-fix files for revert-and-watch I copied both `src/app/api/ai/route.ts` and `src/app/api/ai/test/route.ts` into the scratchpad by **basename**, so the second silently overwrote the first, and I then restored the wrong file over the main route. Caught immediately because the saved copy's contents did not match the file it was going into. Recovered by restoring all three from `HEAD` and re-running the transformation scripts, which is why those transformations live in files rather than being typed inline. **Save by path, never by basename.** Lanes: security PASS, skill PASS. **Not verified:** no authenticated end-to-end AI call. Proving one needs a real signed-in user and a live provider key, and the credit deduction would write to `thenijobs-9f01d`. What is proven is every refusal path, that the guest path still opens, and that identity can no longer come from the body. **Findings carried out:** (a) `checkUserCredits` and `deductUserCredits` use the **client** Firestore SDK from inside a server route, so they run unauthenticated and will be denied outright once RULES-1's rules are deployed — credits would stop being enforced *and* stop being deducted. That is the same architectural gap PAY-1 recorded for the payment writes and it wants one answer, not two. (b) The in-memory rate limiter is per-process and resets on every cold start, so on any serverless host it is close to no limit at all. | `b49d7ef` (2026-09-05, inside `Theni-Jobs-develop`) |
| `thenijobs/dep2-remove-unused-dependencies` | `55c209a` | MERGED_DEVELOP | 2026-09-05 | owner ("start fixing all these bugs one by one", 2026-09-05) via phase DEP-2 | **Eighteen runtime dependencies are installed and imported by nothing.** `audit` QUAL-5. Nine Radix primitives (`react-avatar`, `react-dropdown-menu`, `react-label`, `react-select`, `react-separator`, `react-slot`, `react-switch`, `react-tabs`, `react-toast`), `@tanstack/react-query` and its devtools, `class-variance-authority`, `date-fns`, `docx`, `file-saver`, `next-themes`, `react-hook-form` and `zod`. Each was checked individually with `grep -rl` across `src/`, not inferred from a single sweep. Every dependency is code that runs on this project's behalf during install and a name someone could take over; eighteen of them earning nothing is eighteen unnecessary ways to be surprised. `xlsx` is the standing proof that a dependency's advisory becomes the owner's problem whether or not the feature is used. **A sweep alone would have broken the build, and nearly did.** The first pass also flagged `react-dom`, `typescript`, `@types/node`, `@types/react`, `@types/react-dom` and `@types/qrcode` — all essential, none imported by name from `src/`. It flagged `clsx` and `tailwind-merge` too, and those are the dangerous ones: they are unused **on `develop`** and are used by `src/lib/cn.ts`, a file the second session's branch is **adding**. Removing them would have compiled here and broken UIX-1's merge. **A dependency check has to be run against every live branch, not just your own.** All eight are excluded. Fix: remove the eighteen, and nothing else. Expected files: `package.json`, `package-lock.json`, this ledger. **No `src/**` change is permitted** — if removing a package needs a source edit then the package was in use and the phase is wrong, so the constraint is the check. Never touches `next.config.ts`, `tsconfig.json`, `eslint.config.mjs`, `vercel.json`, `firestore.rules`. **`@tanstack/react-query` is removed with its eyes open.** The performance plan's data-cache layer names it as the library to adopt. It is not adopted, it is not imported, and keeping a pinned version installed against a phase that may never happen is worse than installing whatever is current on the day the work starts — `npm i @tanstack/react-query` is one command. Recorded here so the removal is not read later as a decision against the cache plan. Lanes: config, security, skill. **Collision check at claim:** worktrees = primary (`main`) + `Theni-Jobs-develop` (`b310a8c`) + `Theni-Jobs-uiux1` (second session, `b5bf99b`) + this one; `ACTIVE` ledger rows on `develop` = 0; UIX-1 touches neither `package.json` nor `package-lock.json`; and **each of the eighteen was additionally checked against UIX-1's own tree** with `git grep` and found unused there too. Worktree: `/Users/saai_siddharth/Projects/Clients/Theni-Jobs-dep2`. Acceptance: **the exported page set is byte-identical by path and the build output is unchanged**, which is the real proof that nothing removed was in use; typecheck 0, build 0 with real credentials, lint no worse than 2 errors; `npm audit` no worse than the standing `xlsx`-only result; install time, `node_modules` size and lockfile entry count quoted before and after; and `git diff --name-only` shows **no file under `src/`**. **Built and merged 2026-09-05.** Commits: claim `113375a`, then `55c209a`. Gate after the last commit: typecheck 0 · lint 1 (**2 errors / 755 warnings, the unchanged baseline**) · **build 0 with real-firebase-config=yes** · secrets:bundle 0 · secrets:src-literals 0 · tracked-artefacts 0 · dirty-after 0. **Measured, same worktree:** dependencies **38 → 20** · lockfile packages **737 → 651** · `node_modules` **845M → 795M** · top-level packages **414 → 391** · `npm ci` **5.75s → 4.65s** · `npm audit` **unchanged, `xlsx` alone at 1 high** · html pages **569 → 569** · js file count **165 → 165** · **total javascript bytes 5,125,163 → 5,125,163, byte-identical**. **The export was then compared file by file, not just by totals.** Next mints a new build id every build, and it is embedded in every page, so a naive digest showed 2842 files 'differing' — which would have been reported as a real change by anyone who stopped at the hash. Normalising the build id out: **3048 files each side, no path added or removed, and nine files differing** — two job pages with their RSC payloads, and `sitemap.xml`. Every one of those nine differs **only in a build-time clock value**: sitemap `lastmod` `2026-09-05T02:57:31.311Z` against `02:58:18.751Z`, exactly the 47 seconds between the two builds, and the job pages differ in timestamp fragments alone. Nothing removed contributed a byte to what ships. **The near-miss, which is the transferable part.** The sweep that found the eighteen also flagged `react-dom`, `typescript` and the three `@types` packages — essential, and imported by name from nowhere in `src/`. It also flagged **`clsx` and `tailwind-merge`**, which are genuinely unused on `develop` and are imported by `src/lib/cn.ts`, a file the second session's branch is **adding**. Removing them would have typechecked, built, and passed every gate here, then broken UIX-1's merge. **A dependency check must run against every live branch, not only your own.** All eight excluded. **Re-checked at merge, because the other branch moved during the phase** from `b5bf99b` to `8c6a49f`, twelve commits: none of the eighteen appears anywhere in their tree, and they have not touched `package.json`. `clsx`, `tailwind-merge`, `react-dom`, `next` and `typescript` all confirmed present in the merged manifest. **`@tanstack/react-query` removed with its eyes open** — the performance plan names it for the data-cache layer. It is not adopted and not imported; holding a pinned version against a phase that may never happen is worse than installing whatever is current when that work starts. Re-adding is one command. This is not a decision against the cache plan. **No file under `src/` changed**, which was the phase's own constraint: had removal needed a source edit, the package was in use and the phase was wrong. Lanes: config PASS, security PASS, skill PASS. **Not verified:** no browser run. The phase changed no source and the export is identical apart from clock values, so there is nothing a browser could show that the byte comparison did not. **Findings carried out:** (a) `@types/qrcode` sits in `dependencies` rather than `devDependencies` — harmless, wrong shelf, left alone as out of scope. (b) `src/app/sitemap.ts` stamps `lastModified: new Date()` on every static URL, so **every sitemap entry's `lastmod` changes on every build** and search engines are told the whole site changed at each deploy. That surfaced here as 346 differing lines between two builds a minute apart. It is a real, small SEO defect and its own phase. | `3d38cb6` (2026-09-05, inside `Theni-Jobs-develop`) |
| `thenijobs/seo4-sitemap-lastmod-lies` | `d5a5bee` | MERGED_DEVELOP | 2026-09-05 | owner ("start fixing all these bugs one by one", 2026-09-05) via phase SEO-4 | **173 of the 384 sitemap entries claim to have been modified at the moment the site was built.** Measured from the built `sitemap.xml`: 384 entries, every one carries a `<lastmod>`, and **173 of them share the single value `2026-09-05T03:02:55.925Z`** — the build clock. That is 11 static pages, 9 location landing pages, 135 location-category pages and 18 business categories: `src/app/sitemap.ts` sets `const now = new Date()` and hands it to all four families. So every deploy tells search engines that 45% of the site changed, whatever actually changed. Google's own guidance is that it stops trusting `lastmod` when it does not match reality, which means the value is not merely useless here — it degrades the signal for the **211 entries that are telling the truth**, the jobs, companies and portfolios carrying a real `updatedAt`. Surfaced by DEP-2: comparing two builds a minute apart showed **346 differing lines in `sitemap.xml`** while nothing in the repository had changed. The same file also has a quieter version of the fault. Each dynamic entry does `let lastMod = now;` and overwrites it only when `updatedAt` parses, so a job or company with a missing or malformed `updatedAt` silently claims the build time too. None did in this build, which is exactly why it needs fixing rather than watching. Fix: **omit `lastModified` where there is no real signal.** It is optional in the sitemap protocol and in `MetadataRoute.Sitemap`, and no date is honest where a wrong one is not. Static and generated-listing URLs lose it; jobs, companies and portfolios keep it **only when their own `updatedAt` parses**. Expected files: `src/app/sitemap.ts`, this ledger. `changeFrequency` and `priority` are **deliberately left alone** — search engines have ignored both for years, but removing them is an opinion about the file rather than a defect in it, and this phase is about a value that is actively wrong. Never touches `src/components/**`, `src/lib/**`, `vercel.json`, `package.json`. Lanes: uiux, skill. **Collision check at claim:** worktrees = primary (`main`) + `Theni-Jobs-develop` (`d69cdf0`) + `Theni-Jobs-uiux1` (second session, now `8c6a49f`, twelve commits on from its last check) + this one; `ACTIVE` ledger rows on `develop` = 0; UIX-1 touches neither `sitemap.ts` nor anything it imports. Worktree: `/Users/saai_siddharth/Projects/Clients/Theni-Jobs-seo4`. Acceptance: **two consecutive builds must produce a byte-identical `sitemap.xml`** — that is the whole defect stated as a test, and it fails today by 346 lines; the URL set must be unchanged at 384 with none added or removed; every surviving `<lastmod>` must be a real Firestore `updatedAt` and none may equal the build time; typecheck 0, build 0 with real credentials, lint no worse than 2 errors; export page count unchanged at 569. **Built and merged 2026-09-05.** Commits: claim `01a01d4`, then `d5a5bee`. Gate after the last commit: typecheck 0 · lint 1 (**2 errors / 755 warnings, the unchanged baseline**) · **build 0 with real-firebase-config=yes** · secrets:bundle 0 · secrets:src-literals 0 · tracked-artefacts 0 · dirty-after 0. Export unchanged at 569 pages. **The acceptance was the defect stated as a test, and it passes: two consecutive builds of an unchanged repository now produce a byte-identical `sitemap.xml`.** Before the fix the same pair differed by **346 lines**. **Measured:** sitemap entries **384 → 384** with the **URL set identical — none added, none removed**; entries carrying `<lastmod>` **384 → 211**, which is **exactly the 173** that had held the build clock value `2026-09-05T03:02:55.925Z`; and **0 surviving dates changed value** — every date that remains is the same real `updatedAt` it was before, so nothing true was discarded along with the false. **Which families lost the date and which kept it, checked rather than assumed:** dropped from the 11 static pages, the 9 location landing pages, the 135 location-category pages and the 18 business categories — 173 exactly. Kept by jobs, companies on **both** URL forms, and portfolios, all of which carry a genuine `updatedAt`. **Revert-and-watch:** reverting `d5a5bee` and building twice reproduces **346 differing lines**, the same figure DEP-2 arrived at independently while comparing two of its own builds. That agreement between two phases that measured it for different reasons is what makes the number trustworthy. **The quieter half of the same fault, fixed at the same time.** Each dynamic family did `let lastMod = now;` and overwrote it only when `updatedAt` parsed, so a job or company with a missing or malformed date silently claimed the build time as well. None did in this build — which is precisely why it needed fixing rather than watching. One helper now returns a `lastModified` or nothing at all, and all three families spread it. **Why omission rather than a better date.** `lastmod` is optional in the protocol and in `MetadataRoute.Sitemap`, and a wrong date is worse than no date: search engines stop trusting the field when it disagrees with what they observe, so 173 fabricated values were discounting the 211 honest ones. If those pages ever want a real date it should come from something that changes with them — the newest `updatedAt` among the jobs a listing renders — not from the clock. **`changeFrequency` and `priority` deliberately untouched.** Search engines have ignored both for years, but removing them is an opinion about this file rather than a defect in it, and this phase was about a value that was false. Lanes: uiux PASS, skill PASS. **Owner action required:** the corrected sitemap reaches search engines with the next deploy of `main`. Together with SEO-3, which moves 105 URLs from 404 to 200, **a Search Console resubmission afterwards is worth more than usual** — this is the first deploy in a while whose sitemap is both complete and honest. **Not verified:** no search-engine behaviour, only the emitted XML. What is proven is that the file no longer changes when nothing has changed, and that every date it still carries is one the data actually supports. | `48f97a4` (2026-09-05, inside `Theni-Jobs-develop`) |
| `thenijobs/rules2-unconstrained-client-writes` | `229b8cf` | MERGED_DEVELOP | 2026-09-05 | owner ("start fixing all these bugs one by one", 2026-09-05) via phase RULES-2 | **Two client-writable paths accept any document shape, and one of them accepts it from anybody.** Found by enumerating all 53 non-`false` write rules in `firestore.rules` and separating those with a field constraint from those without: 25 have one, 28 do not, and **all but two of the 28 are `isAdmin()`-only**, which is fine. These are the two that are not. **(1) `whatsappApplications` create is `isAuthenticated()` — nothing more.** No ownership test, no required fields. Its own read rule keys on `resource.data.seekerId` and `resource.data.companyId`, so create does not require the fields read assumes exist. Any signed-in user can write a document naming **another seeker** as the applicant to **any company**, or omit both fields so the document is legible to nobody but an admin, or write arbitrary keys. **The collection has no reader anywhere in `src/`, and its only writer — `trackWhatsAppApplication` at `firestoreService.ts:1003` — has zero callers.** An open write endpoint for a feature that does not exist yet, on the owner's Firestore bill. **(2) `conversations` update names four ways to be allowed and constrains no field.** A participant, the seeker, the company owner or an admin may rewrite the document freely — including `participants`, `seekerId` and `companyId`. Changing `companyId` hands read access to a **different** employer, whose `isCompanyOwner()` test the read rule then passes, for a thread they were never part of; rewriting `participants` can drop the other party or add a stranger. SEC-2 tightened this collection's `create` and its `messages` subcollection and left `update` as it found it. Fix, following the pattern this file already uses three times (`userModerationUnchanged`, `companyModerationUnchanged`, `jobModerationUnchanged`): a `conversationMembershipUnchanged()` helper that forbids the membership keys from changing, rather than a whitelist of writable fields. **The whitelist would have been wrong**: `applyToJob` writes the thread with `setDoc(..., { merge: true })` on a deterministic `chatId`, so a repeat is an *update* carrying every field, and `hasOnly(['lastMessage','lastMessageAt'])` would deny it. `diff().affectedKeys()` only reports keys whose value actually changed, so re-writing identical values still passes. `whatsappApplications` create gets the ownership test and the exact key set `trackWhatsAppApplication` writes. Expected files: `firestore.rules`, this ledger. **No source change** — both message pages already write exactly `{ lastMessage, lastMessageAt }`, which is why no client needs touching. Never touches `storage.rules`, `database.rules.json`, `src/**`, `package.json`. Lanes: security, rules, skill. **Collision check at claim:** worktrees = primary (`main`) + `Theni-Jobs-develop` (`71a3bae`) + `Theni-Jobs-uiux1` (second session, `7f11ac7`) + this one; `ACTIVE` ledger rows on `develop` = 0; UIX-1 touches **no rules file**. Worktree: `/Users/saai_siddharth/Projects/Clients/Theni-Jobs-rules2`. Acceptance: an **emulator probe with real signed-in users** proving each abuse is refused and each legitimate write still allowed — including a repeat `setDoc(merge:true)` of an existing conversation, which is the case a naive fix breaks; **revert-and-watch**, with the old rules allowing exactly what the new ones deny; and at least one assertion that must be DENIED under both rule sets, so a probe that loads no rules cannot pass. Rules only reach production on the owner's `firebase deploy`. **Built and merged 2026-09-05.** Commits: claim `e651059`, then `229b8cf`. Gate after the last commit: typecheck 0 · lint 1 (**2 errors / 755 warnings, the unchanged baseline**) · **build 0 with real-firebase-config=yes** · secrets:bundle 0 · secrets:src-literals 0 · tracked-artefacts 0 · dirty-after 0. **Emulator probe, Firestore + Auth on demo project `demo-rules2`, real signed-in users, 15 assertions run against each rule set.** Seven abuses go **ALLOW → DENY**: repointing `companyId` at another employer, adding a stranger to `participants`, dropping the other party, reassigning `seekerId`, filing a whatsapp application **naming another seeker**, writing a junk document with none of the fields the read rule depends on, and adding an unexpected key. Three legitimate writes stay **ALLOW**: a participant sending a message, a seeker filing their own application (with and without the optional `phone`), and — the case a naive fix breaks — **`setDoc(merge: true)` re-writing every field with identical values**. Two guards **DENY under both rule sets**, so a run that loaded no rules could not have passed. **The escalation was demonstrated by accident, which is the best kind of evidence.** The guard "a stranger updates a conversation they are not in" was expected to deny under both and was **ALLOWED under the old rules** — because the assertion two lines earlier had already reassigned `seekerId` to that very stranger. By the time the guard ran, they really were the seeker. That is the whole vulnerability stated as a sequence: a participant hands the thread to an outsider and the outsider is then legitimately inside it. The guard now runs against a separate untouched conversation and denies under both. **Why a diff test and not a `hasOnly` whitelist.** `applyToJob` writes the thread with `setDoc(..., { merge: true })` on a deterministic `chatId`, so a repeat application is an **update** carrying every field; `hasOnly(['lastMessage','lastMessageAt'])` would have denied it and broken re-application. `affectedKeys()` reports only keys whose value actually changed. There is an explicit probe assertion for that exact case, so the choice is defended by a check rather than by argument. This also matches the three `*ModerationUnchanged` helpers the file already uses. **A mistake of mine the probe caught.** The first draft hoisted `isAdmin()` to the front of the update condition. `isAdmin()` does a `get()` on `users/{uid}`, so that made **every participant sending a message pay a document read** against the ten-access limit, and on an account with no `users` document it raised a **null-value evaluation error** rather than short-circuiting. The denials were arriving as evaluation errors instead of clean refusals, which is what gave it away — a rule that denies for the wrong reason is not a rule that works. Term order now matches what it was. **A probe that only checks allow/deny would have passed this.** **Two further mistakes, both mine, both in the probe rather than the rules.** It used a fixed `chatId`, so the second run began with the document already present and turned the `create` assertion into an update; ids are now unique per run. And the guard shared a conversation with the abuse assertions, described above. **Merged against a moving `develop`:** the second session merged `thenijobs/uiux1-canonical-dashboard-system` into `develop` as `64ff623` **during this phase**. This merge was clean and its first-parent diff is exactly `firestore.rules` and the ledger. The combined tree was then checked for resurrection and regression: `sampleCompanies.ts` and `StatsSection.tsx` still absent; `queryEqual` still in `useFirestore`; `EmployerJobDetailRoute.tsx` present; the `/employer/jobs` rewrite present; no `title.template` in the root layout; RTDB rules still default-deny; `PLAN_PRICES` still derived; `authenticatedUserId` still 12 uses; dependencies still 20. **Every prior phase survived their merge intact.** Lanes: security PASS, rules PASS, skill PASS. **Owner action required:** `firebase deploy --only firestore:rules --project thenijobs-9f01d`. Until then both paths stay open in production — and note that they are open **on top of** the catch-all that RULES-1 removed but which is also still undeployed. **Not verified:** no production probe. Writing to `thenijobs-9f01d` to demonstrate the abuse is exactly the thing that must not be done, so the emulator with real users is as close as this gets. **Findings carried out:** (a) `conversations` **create**, written by SEC-2, puts `isAdmin()` first in its `||` chain and pays the same needless `users/` read on every application. Behaviour-preserving to reorder, out of scope here, worth doing next time that block is open. (b) `trackWhatsAppApplication` at `firestoreService.ts:1003` **has no callers** and nothing reads the collection — either wire the feature up or delete the function; leaving a write path for neither is how the unconstrained rule survived unnoticed. (c) `seekerProfiles` create/update is `isOwner(userId)` with **no field constraint**, and the collection is publicly readable when `isPortfolioPublic == true` — nothing trust-bearing is read from it today, which is why it is not in this phase, but it is the next-weakest rule in the file. | `bf8bca4` (2026-09-05, inside `Theni-Jobs-develop`) |
| `thenijobs/rules3-storage-open-paths` | `6c4a7f2` | MERGED_DEVELOP | 2026-09-05 | owner ("start fixing all these bugs one by one", 2026-09-05) via phase RULES-3 | **Two Storage paths are open, and both belong to features that do not exist.** **(1) `/certificates/{userId}/{fileName}` is readable by `isAuthenticated()`** — *any* account, not the owner. Certificates are qualification and identity documents. The comparable `/resumes/{userId}` block next to it is `isOwner(userId) || isAdmin()`, so the same class of document is protected in one place and open in the other. Anyone could register and read every seeker's certificates. **(2) `/uploads/{userId}/{allPaths=**}` is `allow read: if true` with no content-type restriction** — `allow write: if isOwner(userId) && isUnder10MB()` and nothing more. A world-readable, arbitrary-type file area on the owner's Storage bill: HTML, scripts, anything, served back with the content type it was stored with. Every other write block in the file constrains the type to image or PDF. **Neither path is used.** A repository-wide search finds no code that writes to `certificates/` or `uploads/`; the only literal Storage paths in `src/` are `resumes/{uid}/...` (twice) and one `portfolio/` prefix. These are rule blocks left open for features nobody built — the same pattern RULES-2 found in `whatsappApplications`. **The finding that matters more, and which this phase cannot fix.** `src/hooks/useStorage.ts` uploads with `getDownloadURL()`, which returns a URL carrying an access token, and **a download URL bypasses Storage security rules entirely**. That URL is stored as `resumeUrl` on the seeker's profile and rendered as a plain `href` by `src/app/portfolio/seeker/[id]/SeekerPortfolioClient.tsx:368` and `:638` — the **public** portfolio page — and by the employer candidate view. So the strict-looking `/resumes/` rule protects a path nothing uses, while the real access path is an unauthenticated tokenised URL. Tightening rules does not touch it. **Recorded for the owner as an architecture decision, not fixed here.** Fix: bring both blocks in line with the ones that have real owners — certificates read restricted to the owner and admin, matching resumes; uploads restricted to owner/admin read and to image or PDF, matching every other write block. Expected files: `storage.rules`, this ledger. Never touches `firestore.rules`, `database.rules.json`, `src/**`, `package.json`. Lanes: security, rules, skill. **Collision check at claim:** worktrees = primary (`main`) + `Theni-Jobs-develop` (`a292da1`) + `Theni-Jobs-uiux1` + this one. UIX-1 was **merged into `develop` as `64ff623`** and has no unmerged commits, but **its ledger row is still `ACTIVE`** and its declared scope is all 58 dashboard pages under `src/app/{admin,employer,seeker}`. This phase therefore stays out of those directories entirely — which also parks AI-2, whose fix needs `src/app/admin/ai-settings/page.tsx`. `storage.rules` is touched by no branch. Worktree: `/Users/saai_siddharth/Projects/Clients/Theni-Jobs-rules3`. Acceptance: a **Storage emulator probe with real signed-in users** showing a second account refused both paths and the owner still allowed; **revert-and-watch**, with the old rules allowing what the new ones deny; and at least one assertion denied under **both** rule sets so a run that loaded no rules cannot pass. Rules reach production only on the owner's `firebase deploy`. **Built and merged 2026-09-05.** Commits: claim `b3ced8c`, then `6c4a7f2`. Gate after the last commit: typecheck 0 · lint 1 (**2 errors / 685 warnings** — the baseline moved from 755 when UIX-1 merged and cleared 70) · **build 0 with real-firebase-config=yes** · secrets:bundle 0 · secrets:src-literals 0 · tracked-artefacts 0 · dirty-after 0. **Storage emulator probe, Auth + Firestore + Storage on demo project `demo-rules3`, real signed-in users, ten assertions run against each rule set.** Three go **ALLOW → DENY**: a second account reading the owner's certificate, the same account reading their `uploads/`, and the owner storing `text/html` in `uploads/`. Three legitimate operations stay **ALLOW**: the owner uploading and reading their own certificate and their own upload. Two guards **DENY under both rule sets** — a second account reading the resume, and a second account writing into the owner's certificates — so a run that loaded no rules could not have passed. **Both paths were dead.** Nothing in `src/` writes to `certificates/` or `uploads/`; the only literal Storage paths in the tree are `resumes/{uid}/...` twice and one `portfolio/` prefix. Closed rather than deleted so a future feature inherits a sensible default instead of an open one — the same reasoning as the `whatsappApplications` rule in RULES-2, and the same underlying pattern: **a rule block written for a feature nobody built stays exactly as permissive as the day it was drafted, because nothing exercises it and nothing complains.** **The finding this phase could not fix, and it is the larger one.** `src/hooks/useStorage.ts` uploads with `getDownloadURL()`. That returns a URL carrying an access token, and **a Firebase download URL bypasses Storage security rules entirely**. The URL is stored as `resumeUrl` on the seeker's profile and rendered as a plain `href` by the **public** portfolio page — `SeekerPortfolioClient.tsx:368` and `:638` — and by the employer candidate view at `candidates/page.tsx:245`. So the strict-looking `/resumes/` block guards a path nothing uses, while the real access path is an unauthenticated tokenised URL that no rules edit can reach. A seeker who enables their public portfolio publishes a working link to their resume PDF. Whether that is acceptable for an **opt-in** public portfolio is the owner's call and this phase does not presume it. What is not a judgement call is that the arrangement is **invisible**: the rules read as though they control access to resumes and they do not. Closing it means serving those files through something that checks the caller — a short-lived signed URL, or a download route — which is a design change rather than a rules edit. Lanes: security PASS, rules PASS, skill PASS. **Owner action required:** `firebase deploy --only storage --project thenijobs-9f01d`. Both paths stay open in production until then — and, as with the Firestore rules, on top of a catch-all that RULES-1 removed in the repository and which is also still undeployed. **Not verified:** no production probe, deliberately — demonstrating the read against `thenijobs-9f01d` would mean reading real seekers' documents. The emulator with two real signed-in accounts is as close as this gets. **Findings carried out:** (a) the download-URL exposure above, which wants an owner decision. (b) The comment on the `/resumes/` block says "Only the owner OR employer/admin can read" while the rule grants only owner and admin — the comment has been wrong since RULES-1 wrote it, and the reason nobody noticed is that employers never exercise the rule, they follow the tokenised URL. (c) `certificates/` and `uploads/` are now closed but still unused: either build the feature or delete the blocks, because a third pass over this file should not have to rediscover that they are dead. | `d7bcea4` (2026-09-05, inside `Theni-Jobs-develop`) |
| `thenijobs/ai2-test-route-admin-check` | `9c36b1c` | MERGED_DEVELOP | 2026-09-05 | owner ("start fixing all these bugs one by one", 2026-09-05) via phase AI-2 | **`/api/ai/test` has no authentication of any kind — anyone able to POST to it can drive the platform's AI providers.** The half of this AI-1 could close, it closed: the route no longer falls back to the server's `GROQ_API_KEY` / `GEMINI_API_KEY` / `OPENAI_API_KEY`, so it can no longer spend the owner's quota. What it still does for **any anonymous caller** is take a provider name and an API key and make a live outbound call, reporting back whether the key worked. That is an unauthenticated credential-testing oracle sitting on the platform's own domain, and it is the only route in `src/app/api/**` with no identity check at all. AI-1 left this deliberately and said so: the fix needs the calling page to send a token, and `src/app/admin/ai-settings/page.tsx` was inside the second session's `ACTIVE` scope. **That row closed as `MERGED_DEVELOP` and there are now zero `ACTIVE` rows**, so the page is unclaimed and the phase can finish. Fix: the route requires a verified Firebase ID token whose user is an admin, and the admin page sends one. Identity comes from the token exactly as `/api/ai` now does — verified against the identity toolkit, never taken from the body — and the **role** is read from `users/{uid}` through the Firestore REST API **with the caller's own ID token as the bearer credential**, so the read is authenticated as that user and the existing `allow read: if isOwner(userId) || isAdmin()` rule permits it. That matters: reading it with `NEXT_PUBLIC_FIREBASE_API_KEY` would be an *unauthenticated* request and denied under the default-deny rules, which is the trap PAY-1 recorded for the payment writes. Every failure is closed: no token, an unverifiable token, a token whose user document is unreadable or absent, and a user whose role is anything but `admin` or `super_admin` all refuse. Expected files: `src/app/api/ai/test/route.ts`, `src/app/admin/ai-settings/page.tsx`, this ledger. Never touches `firestore.rules`, `storage.rules`, `src/app/api/ai/route.ts`, `src/lib/ai/**`, `package.json`. Lanes: security, feedback, skill. **Collision check at claim:** worktrees = primary (`main`) + `Theni-Jobs-develop` (`62b3b80`) + this one — **the second session's worktree is gone and its branch is fully merged**; `ACTIVE` ledger rows on `develop` = **0**. Nothing else claims either file. Worktree: `/Users/saai_siddharth/Projects/Clients/Theni-Jobs-ai2`. Acceptance: **the route exercised as a running server** — an anonymous request and a request from a signed-in non-admin both refused where they previously ran, and an admin still allowed; **revert-and-watch** against the pre-fix route showing the anonymous call succeeding; a probe pointed at a **demo project id** so nothing reaches `thenijobs-9f01d`, and using a **placeholder provider key** so no real provider account is touched; typecheck 0, build 0 with real credentials, lint no worse than the 2-error baseline; export page count unchanged at 569. **Neither route exists in production, where both 404.** **Built and merged 2026-09-05.** Commits: claim `693b9d9`, then `9c36b1c`. Gate after the last commit: typecheck 0 · lint 1 (**2 errors / 685 warnings, the baseline**) · **build 0 with real-firebase-config=yes** · secrets:bundle 0 · secrets:src-literals 0 · tracked-artefacts 0 · dirty-after 0. Export unchanged at 569 pages. **Reproduced on a running server**, dev, with the project id pointed at `demo-ai2-probe` and a placeholder provider key so neither `thenijobs-9f01d` nor any real provider account was touched. An **anonymous** POST with no `Authorization` header: **old — HTTP 200**, `{"success":false,"latencyMs":277,"provider":"groq","error":"401 ... Invalid API Key"}`, having really called `api.groq.com` and reported the verdict; **new — HTTP 403**, and no outbound call at all. With a live key the old route would have answered "this key works" to anyone who asked. **Probe: five refusal paths, all 403, and none reached a provider** — anonymous, empty bearer, garbage bearer, a well-formed but unverifiable JWT, and a `Basic` scheme instead of `Bearer`. The probe asserts the absence of `latencyMs` in every refusal body, so "refused" also means "did not spend an outbound request". **The role lookup is the part worth reading twice.** The uid comes from the identity toolkit, never from the body — the same shape AI-1 established. The **role** is then read from `users/{uid}` through the Firestore REST API **with the caller's own ID token as the bearer credential**, so the request is authenticated as that user and the existing `allow read: if isOwner(userId) || isAdmin()` rule permits it. Reading it with `NEXT_PUBLIC_FIREBASE_API_KEY` would have been an **unauthenticated** request and denied outright once the default-deny rules are live — the exact trap PAY-1 recorded for the payment writes, avoided here on purpose rather than by luck. Every failure closes: no token, an empty or malformed one, a token that will not verify, a user document that cannot be read or does not exist, and any role that is not `admin` or `super_admin`. **The client had to change or the button would have died.** `src/app/admin/ai-settings/page.tsx` now attaches the id token, reports a readable message rather than a silent nothing when the session has expired, and returns through the existing `finally` so the spinner still clears. Lanes: security PASS, feedback PASS, skill PASS. **Not verified — the allowed path.** Demonstrating an admin being accepted needs a real signed-in admin of `thenijobs-9f01d` and a live provider key, and the role lookup would read production data. What is proven is that every unauthenticated route into this endpoint is closed and that a refusal never reaches a provider. The first real use after a deploy is worth watching for that reason. **A note on sequencing that is worth keeping.** AI-1 could have jammed this in by editing a file another phase had claimed, and chose to ship the half that stopped the bleeding — the server-key fallback — and record the rest. The wait cost one tick and the fix landed with the page's owner having finished with it. **Deferring the half you cannot do cleanly is cheaper than a conflict**, and the ledger row is what made the debt visible enough to pay. | `5f75a7d` (2026-09-05, inside `Theni-Jobs-develop`) |
| `thenijobs/perf4-firestore-persistent-cache` | `658acd9` | MERGED_DEVELOP | 2026-09-05 | owner ("the app should be blazing fast … with a proper cache system", 2026-09-05; and "start fixing all these bugs one by one") via phase PERF-4 | **Every visit pays a fresh network round trip before any content appears, because Firestore is running with no cache at all.** `src/lib/firebase/config.ts:26` is `getFirestore(app)`, which gives the **memory-only** cache: it is emptied on every page load, so nothing is ever reused between visits or sessions. Measured on a fresh `/jobs` load: DOM ready at **83 ms**, and the last Firestore response arriving at **414 ms** — the page has nothing to show for the 331 ms in between, on localhost, against a dataset of three jobs. On a phone in Theni talking to a Google region, that gap is the page. This is the cache layer the performance plan called the biggest user-visible win, and it is the last item of that plan still undone. Layer 0 (headers, image weight) shipped as PERF-0, query discipline as PERF-2 and PERF-3, and the home page's parked subscriptions as PERF-1. Fix: initialise Firestore with `persistentLocalCache`, so documents already fetched live in IndexedDB across loads and sessions and a returning visitor renders from disk while the SDK revalidates in the background. That is stale-while-revalidate for Firestore, obtained from the SDK rather than hand-rolled. **Three things this must not get wrong.** (1) `initializeFirestore` has to run **before** anything calls `getFirestore`, and exactly once — `config.ts` is the only place that constructs it, which is what makes this safe. (2) There is **no IndexedDB during the build**, where Next collects page data in Node, so persistence is browser-only and the server path keeps the plain instance. (3) Persistence **can legitimately fail** — private browsing, blocked site data, an unsupported browser — and a failure there must degrade to the current behaviour rather than break the app, so it falls back to `getFirestore(app)`. **`@tanstack/react-query` is deliberately not reinstated.** DEP-2 removed it unused and said re-adding was one command. It is not needed: the SDK's own persistent cache does this for every read in the app, with no provider to thread through 46 `useCollection` call sites and 24 `getDocs` ones, and **no bundle growth**. A library that duplicates a cache the SDK already ships is not a cache system, it is a second one. Expected files: `src/lib/firebase/config.ts`, this ledger. **No page or component changes** — if a page needs editing to benefit, the change is in the wrong place. Never touches `firestore.rules`, `storage.rules`, `package.json`, `vercel.json`, `next.config.ts`. Lanes: worker, skill. **Collision check at claim:** worktrees = primary (`main`) + `Theni-Jobs-develop` (`0729420`) + this one; the second session's worktree is **gone** and its branch fully merged; `ACTIVE` ledger rows on `develop` = **0**. Worktree: `/Users/saai_siddharth/Projects/Clients/Theni-Jobs-perf4`. Acceptance: **measured in a real browser, same page, same server, before and after** — a repeat load of `/jobs` must render its cards without waiting on a Firestore response, quoted as a number rather than described; the **first** load must be no worse; `persistentLocalCache` must be provably in use, not merely requested, by observing the IndexedDB database it creates; the build must still pass with **no IndexedDB present**, which is the case that would break it; typecheck 0, build 0 with real credentials, lint no worse than the 2-error baseline, export page count unchanged at 569. **Built and merged 2026-09-05.** Commits: claim `42bc30d`, then `658acd9`. Gate after the last commit: typecheck 0 · lint 1 (**2 errors / 685 warnings, the baseline**) · **build 0 with real-firebase-config=yes** · secrets:bundle 0 · secrets:src-literals 0 · tracked-artefacts 0 · dirty-after 0. Export unchanged at **569 pages**, which is also the proof for hazard (2): the build collects page data in Node where there is no IndexedDB, and it passes. **Proven by looking at what is actually on disk, not by trusting the config.** With the change, IndexedDB contains **`firestore/[DEFAULT]/thenijobs-9f01d/main`**, whose `remoteDocumentsV14` store holds documents from the **`jobs`** collection and whose `targets` store holds the **`jobs`** query — the /jobs page's own data, persisted across loads. Without the change, after the identical load, **no `firestore/` database exists at all**. Site data was wiped before each run so both were genuinely cold, and both rendered the same three cards, so nothing regressed. **Baseline that motivated it:** a cold `/jobs` load had `domContentLoadedEventEnd` at **83 ms** and the last Firestore response at **414 ms** — on localhost, against three job documents. The page had nothing to show for the 331 ms in between. **Not measured, and it matters that this is said rather than glossed: a warm-load latency figure.** The browser pane exposes **no `paint` and no `largest-contentful-paint` entries**, and a script injected after load starts too late to observe first render — one attempt returned `cardsPresentAtMs: 8782`, which is the age of the document and not a load time. Any millisecond figure for the warm case would therefore have been invented. What is established is that the cache is **enabled**, is **populated with this page's own documents**, and **does not exist without this change**; the rendering benefit follows from the SDK's documented behaviour of serving listeners from cache before the server responds, which is a citation and not a measurement. **`@tanstack/react-query` deliberately not reinstated.** DEP-2 removed it unused and recorded that re-adding was one command. It is not needed: the SDK's own cache covers **all 46 `useCollection` call sites and all 24 `getDocs` ones** with no provider to thread through them and **no bundle growth**. A library duplicating a cache the SDK already ships is not a cache system, it is a second one. **Three hazards, each answered by a line in the file rather than by hope.** (1) `initializeFirestore` must precede any `getFirestore` and run once — this module is the sole constructor and `db` is what the app imports, so the ordering holds by construction. (2) No IndexedDB in Node, so the server path keeps the plain instance. (3) Persistence can be refused — private browsing, blocked site data — and that costs the cache, never Firestore, so it falls back to precisely the previous behaviour. `persistentMultipleTabManager` avoids the first tab taking an exclusive lease and later ones silently getting no persistence. **This closes the performance plan.** Layer 0 shipped as PERF-0 (headers, 1.4 MB of images), query discipline as PERF-2 and PERF-3, the home page's parked subscriptions as PERF-1, and the bundle as SEC-3's 106 KB. The data cache was the last item and the one the owner asked for twice. Lanes: worker PASS, skill PASS. **Findings carried out:** (a) Stale-while-revalidate means a returning visitor can see a job that has since closed, for the moment before revalidation lands. That is the correct trade for a job board and it is the trade being made — worth knowing before anyone reports it as a bug. (b) The cache is unbounded by default; Firestore garbage-collects at a 40 MB target, which is fine now and worth revisiting if the job or company collections grow by an order of magnitude. (c) The 24 one-shot `getDocs` call sites are **server-first** by design, so they benefit from persistence only for offline and for listener-backed screens; making those cache-first would be a per-call-site change and a separate phase, and should be measured before it is assumed to be worth it. | `b759b44` (2026-09-05, inside `Theni-Jobs-develop`) |
| `thenijobs/rules4-employer-cannot-list-own-jobs` | `4f28c40` | MERGED_DEVELOP | 2026-09-05 | owner ("start fixing all these bugs one by one", 2026-09-05) via phase RULES-4 | **The day the owner deploys `firestore.rules`, every employer's job list and dashboard breaks with permission-denied.** Not a latent risk — an emulator probe against the **current committed rules**, with a real signed-in employer and their own job, denies the exact queries the employer screens run: `where('companyId','==',X) + orderBy('createdAt','desc')` — **DENIED**; `where('companyId','==',X) + where('isActive','==',true)` — **DENIED**. Those are `employer/jobs/page.tsx:90`, `employer/dashboard/page.tsx:50` and `employer/post-job/page.tsx:74`. **Why.** The jobs read rule authorises a document by `status in ['active','APPROVED','LIVE']`, or by `postedBy == request.auth.uid`, or `isAdmin()`. Firestore requires a **list** query to be provably safe from its own constraints, and **not one query in the tree filters on `postedBy`** — every employer screen filters on `companyId`. So a query that could return a `draft` or `pending` job cannot be shown safe, and Firestore refuses the whole query rather than filtering it. Today this is invisible because the catch-all RULES-1 removed still grants the read in production; it becomes real on deploy. **The fix is the file's own policy, applied to the one collection that missed it.** `applications`, `interviews`, `leads` and `conversations` all authorise the employer side with `isCompanyOwner(resource.data.companyId)`. `jobs` alone uses `postedBy`. Adding `isCompanyOwner(resource.data.companyId)` to the jobs read rule makes the **existing queries pass unchanged** — proven — and needs **no query edits and no new composite indexes**, where adding `where('postedBy','==',uid)` to three screens would have needed both, and would have hidden a colleague's postings from a company's other users the moment one existed. Placed after the two free comparisons and before `isAdmin()`, so the common paths short-circuit before either `get()` — the ordering lesson RULES-2 learned the hard way. Expected files: `firestore.rules`, this ledger. **No source, no indexes** — if a page needs editing, the fix is in the wrong place. Never touches `storage.rules`, `database.rules.json`, `firestore.indexes.json`, `src/**`. Lanes: security, rules, skill. **Collision check at claim:** worktrees = primary (`main`) + `Theni-Jobs-develop` (`76dda29`) + this one; `ACTIVE` rows on `develop` = 0; the second session's branch is fully merged and its worktree gone. Worktree: `/Users/saai_siddharth/Projects/Clients/Theni-Jobs-rules4`. Acceptance: an **emulator probe with real signed-in users** in which the employer's existing queries are **DENIED on the current rules and ALLOWED on the new ones**, a **different account is denied on both**, and the seeded data is created through the real create rules rather than injected past them. The probe must be **idempotent** — unique ids per run, the lesson from RULES-2. Rules reach production only on the owner's `firebase deploy`. **Built and merged 2026-09-05.** Commits: claim `a9bbcd6`, then `4f28c40`. Gate: typecheck 0 · lint 1 (**2 errors / 685 warnings, baseline**) · **build 0 with real-firebase-config=yes** · secrets 0 · artefacts 0 · dirty-after 0. **Probe, both rule sets, real signed-in users, ids unique per run, data seeded through the real create rules rather than injected past them:** `employer/jobs` (`companyId` + `orderBy createdAt`) **DENY → ALLOW**; `employer/dashboard` (`companyId` + `isActive`) **DENY → ALLOW**; a different account listing those jobs **DENY on both**; a query with no status constraint **DENY on both**; `status == 'active'` **ALLOW on both**. Two assertions deny under both sets, so a run that loaded no rules could not have passed. **Why the rule moved and not the queries.** Adding `where('postedBy','==',uid)` to the three screens would have worked, and would have cost two new composite indexes and hidden a colleague's postings from a company's other users the day a second one existed. Changing the rule costs nothing, touches no source, and brings `jobs` into line with `applications`, `interviews`, `leads` and `conversations`, which all already authorise the employer side by company ownership. The queries were written for the policy the rest of the file uses; the rule was the outlier. **Ordering:** `isCompanyOwner()` sits after the two free comparisons and before `isAdmin()`, so the common paths short-circuit before either `get()` — the lesson RULES-2 learned when hoisting `isAdmin()` turned clean refusals into null-value evaluation errors. **Two assumptions of mine the probe corrected**, both the rules working as intended and both recorded rather than quietly fixed: creating a job with `status: 'active'` is denied because `safeJobCreate()` permits only `pending` and `draft` — a poster cannot self-activate, which is D-JOBSTATE; and creating a company with `isActive: true` is denied by `safeCompanyCreate()` for the same reason. My first probe expected both to succeed. **A probe that fails because the rules are right is worth more than one that passes because it was written to.** Lanes: security PASS, rules PASS, skill PASS. **Owner action — this is the one to read before deploying.** `firebase deploy --only firestore:rules --project thenijobs-9f01d` was, until this phase, a command that would have taken the employer portal down. It is now safe on this axis. **Not verified:** no production probe, and no browser run of the employer screens against the new rules — the emulator exercises the queries themselves, which is where the failure was. **Finding carried out:** `useRealtimeStats.ts:142` counts `where('isActive','==',false)` as "pending jobs", which also matches draft, rejected, closed and expired. The number is wrong today and the query will be denied under these rules for the same provability reason. It is admin-facing and its own small phase. | `e36d1dd` (2026-09-05, inside `Theni-Jobs-develop`) |
| `thenijobs/data4-platform-stats-job-counts` | `0c2a8f3` | MERGED_DEVELOP | 2026-09-05 | owner ("start fixing all these bugs one by one", 2026-09-05) via phase DATA-4 | **The admin platform stats keep a live listener over every non-active job to compute a number that is never displayed, and it would be the wrong number if it were.** `src/hooks/useRealtimeStats.ts:142` subscribes `jobs where isActive == false` as `pendingJobs`. Two faults. It is **wrong**: `isActive == false` is also true of `draft`, `rejected`, `closed` and `expired` jobs, so it counts the entire dormant job table as "pending". And it is **unread**: `stats.pendingJobs` appears in no `.tsx` file in the tree. `admin/dashboard/page.tsx:137` already computes the pending list correctly and separately, with `where('status','==','pending')`, and renders that. So every admin dashboard load opens an `onSnapshot` over a growing collection to maintain a wrong figure nobody sees — the same shape PERF-1 removed from the home page. **Second, `activeJobs` counts by the wrong field.** `:136` uses `isActive == true` where the jobs read rule, every public page and `firestoreService.ts:99` all use `status == 'active'` — that line already carries the note "matches the jobs read rule (status), not isActive". `stats.activeJobs` is rendered in three places, including "Active Listings" on `admin/reports`. **What this phase does NOT claim, because a probe said otherwise.** I expected these queries to be denied once the rules deploy, the way RULES-4's employer queries were. They are not: `isAdmin()` does not depend on the document, so Firestore can prove an admin's list query safe from the request alone. An emulator run with an admin role seeded through the rules-bypass confirmed all four admin stat queries **ALLOW**, while the same shapes run by a non-admin **DENY**. This is a correctness and cost defect, not a deploy-blocker, and saying so is the point. Fix: delete the `pendingJobs` listener and its field — the dashboard already answers that question correctly a few lines away — and count `activeJobs` by `status == 'active'`. Equality on one field needs no composite index. **The employer-side `activeJobs` (`:213`, `companyId` + `isActive`) is deliberately left alone**: switching it to `status` would need a new `companyId + status` composite index, the existing index is `companyId + isActive + status` which a two-field query cannot use as a prefix, and "my live jobs" by `isActive` is defensible for an owner looking at their own postings. Changing it is an index decision, not a bug fix. Expected files: `src/hooks/useRealtimeStats.ts`, this ledger. Never touches `firestore.rules`, `firestore.indexes.json`, `src/app/**`. Lanes: worker, skill. **Collision check at claim:** worktrees = primary (`main`) + `Theni-Jobs-develop` (`231249c`) + this one; `ACTIVE` rows = 0. Worktree: `/Users/saai_siddharth/Projects/Clients/Theni-Jobs-data4`. Acceptance: `stats.pendingJobs` gone from the type and from every consumer, proven by grep; one fewer `onSnapshot` per admin dashboard load; `activeJobs` counting the same field the read rule does; typecheck 0, build 0 with real credentials, lint no worse than the 2-error baseline, export page count unchanged at 569. **Built and merged 2026-09-05.** Commits: claim `8519523`, then `0c2a8f3`. Gate: typecheck 0 · lint 1 (**2 errors / 685 warnings, baseline**) · **build 0 with real-firebase-config=yes** · secrets 0 · artefacts 0 · dirty-after 0 · **569 pages**. **Measured:** `listen()` calls in the hook **16 → 15**; `pendingJobs` gone from the listener, the `PlatformStatsData` interface and the initial state, confirmed by grep; `activeJobs` now counting `status == 'active'`. **The typed interface earned its keep:** removing the field from `PlatformStatsData` made `tsc` fail on the leftover `pendingJobs: 0` in the initial state, which a loosely-typed object would have carried silently forever. **A hypothesis of mine that the probe refuted, kept here because the refutation is the useful part.** I expected these queries to be denied on deploy, the way RULES-4's employer queries are. They are not: `isAdmin()` does not depend on the document, so Firestore can prove an admin's list query safe from the request alone. An emulator run with an admin role seeded through the **rules-bypass REST endpoint** — necessary because the `users` create rule correctly refuses to let anyone self-assign `role: 'admin'`, which the probe also asserts — showed all four admin stat queries **ALLOW**, while the identical shapes run by a non-admin all **DENY**. So this was a correctness and cost defect, not a deploy blocker, and the phase says so rather than borrowing RULES-4's urgency. **Deliberately left:** the employer-side `activeJobs` (`companyId` + `isActive`). Switching it to `status` needs a new `companyId + status` composite index — the existing `companyId + isActive + status` cannot serve a two-field query as a prefix — and "my live jobs" by `isActive` is defensible for an owner looking at their own postings. An index decision, not a bug fix. Lanes: worker PASS, skill PASS. **Two corrections to my own earlier claims this tick, both caught before they did damage.** I said `useRealtimeStats` had zero callers — **wrong**; five pages import `useEmployerStats`, `usePlatformStats` and `useSeekerStats` from it, and my grep had excluded the very lines that import it. And a dead-module sweep listed 14 portfolio templates as unreferenced — **wrong**; `TemplateRenderer.tsx` loads every one through `dynamic(() => import('./templates/X'))`, which the sweep did not resolve. Deleting them would have removed the portfolio feature. **A sweep is a hypothesis; the individual check is the evidence.** **Finding carried out:** a corrected sweep that resolves dynamic imports still reports ~20 modules with no importer, about 4,000 lines, including several `src/components/ui/*` primitives and `src/lib/seo/indexingApi.ts`. Two of those are barrel files my matcher still mishandles, so the list needs per-module verification before anything is deleted — a phase in its own right, and one to do with DEP-2's discipline rather than a single grep. | `3c36486` (2026-09-05, inside `Theni-Jobs-develop`) |
| `thenijobs/qual2-unreachable-modules` | `e1732b7` | MERGED_DEVELOP | 2026-09-05 | owner ("start fixing all these bugs one by one", 2026-09-05) via phase QUAL-2 | **22 modules, 3,730 lines, are unreachable from any route.** Established by building the real import graph — resolving `@/` aliases, relative paths, barrel `index` files and `dynamic(() => import(...))` — and walking it from the 132 framework entrypoints: 280 of 302 files are reachable, 22 are not. Each was then confirmed **individually** by resolved import specifier: every one has **zero importers**, except `LoadingSkeleton.tsx`, whose only importer is `Chart.tsx`, itself dead. **Most of it is a superseded design system.** UIX-1 built `src/components/dashboard/**` — 13 components and a barrel, imported by **53 files**. The dead `src/components/ui/*` set is the generation it replaced, pair for pair: `DataTable`, `EmptyState`, `LoadingSkeleton`→`Skeleton`, `StatsCard`→`StatGrid`, `StatusBadge`→`Pill`, plus `Modal`, `Sidebar`, `Chart`, `Breadcrumb`, `SearchInput`, `FileUpload`, `FeatureGate`. Deleting them finishes that migration rather than starting anything. **Deleting: 18 modules.** The twelve superseded `ui/*` primitives; `home/BusinessUpdates` and `home/SearchHub`; and four unused services — `lib/seo/indexingApi.ts` (238 lines of Google Indexing API code with no caller), `lib/seo/seoValidator.ts` (which is where `validateJobForPublishing()` lives, the audit's BUG-13, uncalled), `lib/firebase/identityService.ts` and `lib/ai/geminiService.ts`. **Keeping: 4 modules, and saying why.** `AIChatbotModal`, `AICareerCoachModal`, `AITalentMatcherModal` and `portfolio/editor/AIContentAssistant` are unreachable too, but they are **unshipped product, not superseded plumbing** — UI for AI features whose API routes 404 in production pending `D-HOSTING`. They import only `lib/ai/aiClient`, `lib/ai/config`, `hooks/useAuth` and `lib/firebase/config`, none of which this phase touches, so keeping them costs nothing and deleting them would be a product decision dressed as hygiene. After this phase the unreachable set is **4 files, not 0**, deliberately. Expected files: the 18 deletions, this ledger. **No edits** — if deleting something requires editing something else, it was reachable and the analysis was wrong, which is the check. Never touches `firestore.rules`, `storage.rules`, `package.json`, `src/app/**`. Lanes: worker, uiux, skill. **Collision check at claim:** worktrees = primary (`main`) + `Theni-Jobs-develop` (`43e5726`) + this one; `ACTIVE` rows = 0; the second session's branch is merged and its worktree gone, so nothing else holds `src/components/ui/**`. Worktree: `/Users/saai_siddharth/Projects/Clients/Theni-Jobs-qual2`. Acceptance: **the built export must be byte-identical** — same page count, same js file count, same total js bytes — which is the only proof that nothing removed was reachable; typecheck 0 (it checks every file, reachable or not, so a wrong deletion fails here); build 0 with real credentials; lint no worse than the 2-error baseline. **Two sweep errors from the previous tick are why this phase verifies three ways.** A basename sweep called 14 portfolio templates dead — `TemplateRenderer` loads all of them by `dynamic(() => import('./templates/X'))`. A grep called `useRealtimeStats` uncalled — its own filter had excluded the import lines. Neither would have survived the graph, and neither would have survived the byte-identical build. **Built and merged 2026-09-05.** Commits: claim `79a2410`, then `e1732b7`. Gate: typecheck 0 · lint 1 (**2 errors / 683 warnings** — down from 685; the dead files carried two of their own) · **build 0 with real-firebase-config=yes** · secrets 0 · artefacts 0 · dirty-after 0. **The proof is the byte comparison, built before and after in the same worktree:** html pages **569 → 569**, js file count **166 → 166**, total js bytes **5,169,172 → 5,169,172 — byte-identical**. If any of the eighteen had been reachable the bundle would differ or the build would fail. `tsc` also passes, and it checks every file whether reachable or not, so a wrong deletion fails there before a build is even attempted. **No surviving file needed an edit.** That was the check rather than a nicety: a deletion that forces an edit elsewhere was reachable, and the analysis would have been wrong. **3,122 lines removed across 18 files, zero modifications.** **Four unreachable modules kept on purpose, so the count is 4 and not 0.** `AIChatbotModal`, `AICareerCoachModal`, `AITalentMatcherModal` and `portfolio/editor/AIContentAssistant` are **unshipped product, not superseded plumbing** — UI for AI features whose routes 404 pending `D-HOSTING`. They import only `lib/ai/aiClient`, `lib/ai/config`, `hooks/useAuth` and `lib/firebase/config`, so keeping them costs nothing, and deleting them would be a product decision dressed as hygiene. **What went, and why it is not a judgement call:** twelve `components/ui/*` primitives superseded pair-for-pair by `components/dashboard/**`, which UIX-1 built and 53 files import — `DataTable`, `EmptyState`, `LoadingSkeleton`→`Skeleton`, `StatsCard`→`StatGrid`, `StatusBadge`→`Pill`, plus `Modal`, `Sidebar`, `Chart`, `Breadcrumb`, `SearchInput`, `FileUpload`, `FeatureGate`; two unused home components; and four unused services — `indexingApi.ts` (238 lines of Google Indexing API with no caller), `seoValidator.ts` (which is where `validateJobForPublishing()` lived, **closing the audit's BUG-13**), `identityService.ts` and `geminiService.ts`. Everything is recoverable from git at this commit's parent. Lanes: worker PASS, uiux PASS, skill PASS. **This phase verifies three ways because the previous tick's single-method sweeps were wrong twice.** A basename sweep called 14 portfolio templates dead — `TemplateRenderer` loads every one through `dynamic(() => import('./templates/X'))`, and deleting them would have removed the portfolio feature. A grep called `useRealtimeStats` uncalled — its own filter had excluded the lines that import it. Neither error survives an import graph, and neither survives a byte-identical build. **Three independent methods agreeing is the standard for a deletion; one grep is not.** | `024364c` (2026-09-05, inside `Theni-Jobs-develop`) |
| `thenijobs/seo5-location-pages-show-unrelated-jobs` | `9a9a47f` | MERGED_DEVELOP | 2026-09-05 | owner ("start fixing all these bugs one by one", 2026-09-05) via phase SEO-5 | **All 144 location and location-category pages show the same jobs, regardless of location or category.** Found by opening them in a browser against real data rather than by checking that the routes returned 200. `/jobs-in-madurai/it` lists a *Medical Records reviewer in **theni*** and a *Delivery Partner in **Chinnamanur***; `/jobs-in-madurai/driving` lists **the identical two**. Neither job is in Madurai and neither is IT or driving. **Two lines cause it, one in each of the two components.** `CategoryJobPageClient.tsx:52` and `LocationJobPageClient.tsx:44` end their location test with `|| (j.district && j.district.toLowerCase() === 'theni')`, so **every Theni job matches every location** — Madurai, Dindigul, all of them. And `:56` / `:45` finish with `setJobs(filtered.length > 0 ? filtered : fetched.slice(0, N))`, so when the filter genuinely matches nothing the page **falls back to showing unrelated jobs** instead of saying it has none. **The honest empty state is already written and is simply unreachable.** `CategoryJobPageClient.tsx:161` says "No specific {category} openings in {location} right now." and `LocationJobPageClient.tsx:188` its equivalent. The fallback has been standing in front of correct UI. **I own the scale of this.** SEO-3 took this family from 30 pages to 135 on the grounds that the deployed sitemap already promised them and the implementation was location-agnostic. It verified that the routes render and that the sitemap no longer points at 404s. **It did not verify that two different pages show different content**, and they do not. That check is the one this phase adds. Fix: delete the clause that makes every Theni job match every location, and delete the fallback so the empty state that exists becomes reachable. Expected files: `src/components/seo/CategoryJobPageClient.tsx`, `src/components/seo/LocationJobPageClient.tsx`, this ledger. No route, sitemap or rules change. Lanes: worker, uiux, feedback, skill. **Collision check at claim:** worktrees = primary (`main`) + `Theni-Jobs-develop` (`1b87cc0`) + this one; `ACTIVE` rows = 0. Worktree: `/Users/saai_siddharth/Projects/Clients/Theni-Jobs-seo5`. Acceptance: **two different pages must show different content, checked in a browser against real data** — `/jobs-in-madurai/it` and `/jobs-in-madurai/driving` must stop being identical, and a Theni job must stop appearing on a Madurai page; the existing empty state must render where there are no matches; typecheck 0, build 0 with real credentials, lint no worse than the 2-error baseline, page count unchanged at 569. **Consequence to put in front of the owner, not to decide here:** with the fallback gone, most of the 135 location-category pages will legitimately have nothing to show, because the database holds a handful of active jobs. An honest empty page is better than a duplicate one — Google treats near-identical pages as doorway pages — but it makes the real question concrete: **how many of these pages should exist at all.** That belongs with SEO-1's question about publishing every company at three URLs. **Built and merged 2026-09-05.** Commits: claim `0ffc76e`, then `9a9a47f`. Gate: typecheck 0 · lint 1 (**2 errors / 683 warnings, baseline**) · **build 0 with real-firebase-config=yes** · secrets 0 · artefacts 0 · dirty-after 0 · **569 pages**. **Measured in a browser against real data, before and after:** `/jobs-in-madurai/it` **2 jobs, neither in Madurai → 0 with the honest empty state**; `/jobs-in-madurai/driving` **the identical 2 → 0**; `/jobs-in-theni` **2 → 2, both correct**; `/jobs-in-chinnamanur` **the same 2 → 1, the Chinnamanur one**. **The Theni page keeping both is the important assertion, not a miss.** Chinnamanur is in Theni district, so the Chinnamanur job matches on `district`. A filter that returned nothing everywhere would also have made the pages differ; this shows it **discriminates** rather than merely rejects. **How it was found, and why that matters.** By opening the pages in a browser against real data. SEO-3 checked that the routes return 200 and that the sitemap no longer points at 404s — both true, both insufficient. **A route that renders is not a page that says anything.** The check this phase adds is that two different pages show different content, and it is the check SEO-3 should have carried. **The correct UI was already there.** Both components had `"No specific {category} openings in {location} right now."` written and unreachable behind the fallback. The fix removes an obstruction rather than adding a feature — which is why it is 25 lines, most of them comment. Lanes: worker PASS, uiux PASS, feedback PASS, skill PASS. **Consequence for the owner, stated not decided:** with the fallback gone most of the 135 location-category pages will legitimately have nothing to show, because the database holds a handful of active jobs. An honest empty page beats a duplicate one — near-identical pages are what search engines call doorway pages — but it makes the real question concrete: **how many of these pages should exist.** Sits with SEO-1. **Two things noticed and deliberately not changed:** the category match is a substring test, so the slug `it` matches any title containing those two letters; and both components fetch fifteen jobs and filter client-side, so a genuine match beyond the fifteenth is missed. Both want server-side filtering by district and category, which needs composite indexes and is its own phase. | `086cc70` (2026-09-05, inside `Theni-Jobs-develop`) |
| `thenijobs/trust2-fabricated-platform-statistics` | `cc8dfe6` | MERGED_DEVELOP | 2026-09-05 | owner ("start fixing all these bugs one by one", 2026-09-05) via phase TRUST-2 | **The home page tells every visitor there are 500+ active jobs. There are two.** Found by opening the site, not by reading code. `src/components/home/HeroSection.tsx:40-43` is: `const [jobCount] = useState(500); const [companyCount] = useState(190); const [seekerCount] = useState(1200); const [statsLoading] = useState(false);` — **hard-coded constants dressed as state**, with no setter and a loading flag pinned to `false` so the figures read as data that has finished loading. The hero renders them as "500+ Active Jobs · 190+ Verified Companies · 1,200+ Job Seekers". **Measured against production on 2026-09-05, read-only aggregation queries:** `jobs where status == 'active'` = **2**. `companies where verificationStatus == 'verified'` = **104** (and 104 companies in total). So the headline claim overstates by **250×**, and the company claim by about 83%. The seeker figure cannot be checked at all without reading `users`, which the rules correctly restrict to the owner and admins. This is the same defect class as TRUST-1, which removed three invented businesses, and it is more prominent: it is the first thing a job seeker sees on a live site with real users, presented as measured fact. **Four more places say the same kind of thing.** `src/components/auth/AuthShell.tsx:21` — `['1,200+ Active Jobs', '500+ Verified Companies', '98% Placement Rate']`, shown on the login screen; a placement rate is not merely wrong here, it is unverifiable and the strongest claim on the site. `src/app/about/page.tsx:100-103` — `10,000+ Active Users`, `1,500+ Verified Companies`, `5,000+ Connections Made`, `15+ Cities Served`, against 104 companies and **9** locations in `LOCATIONS_DATA`. `src/app/jobs/layout.tsx:6` puts "Search 1,200+ verified private jobs" in the **meta description**, so it reaches search results. `src/app/seeker/subscription/page.tsx:53` sells a paid plan on "stand out to 500+ verified employers" — a number attached to a payment. Fix, and the line is deliberate: **count what can be counted, say nothing where it cannot.** The hero takes real counts through `getCountFromServer` for `jobs where status == 'active'` and `companies where verificationStatus == 'verified'` — both provably readable by an anonymous visitor under the current rules — and **drops the job-seeker figure entirely**, because counting `users` would need permissions no visitor should have. The `+` goes with it: a plus sign on an exact count is its own small untruth. Everywhere a number cannot be counted, qualitative wording replaces it — the pattern `register/page.tsx:117` already uses, `['Free for job seekers', 'Verified local businesses', 'Setup in minutes']`, which is honest and reads better. Expected files: `src/components/home/HeroSection.tsx`, `src/components/auth/AuthShell.tsx`, `src/app/about/page.tsx`, `src/app/jobs/layout.tsx`, `src/app/seeker/subscription/page.tsx`, this ledger. Lanes: worker, uiux, feedback, skill. **Collision check at claim:** worktrees = primary (`main`) + `Theni-Jobs-develop` (`e6d5278`) + this one; `ACTIVE` rows = 0. Worktree: `/Users/saai_siddharth/Projects/Clients/Theni-Jobs-trust2`. Acceptance: **no fabricated quantity survives anywhere in the tree**, checked by grep for `NNN+` claim strings; the hero shows the **real** counts in a browser against production data — 2 and 104 on the day of writing, not 500 and 190; the counts come from aggregation queries an anonymous visitor is allowed to run, proven by loading the page **signed out**; typecheck 0, build 0 with real credentials, lint no worse than the 2-error baseline, page count unchanged at 569. **Note for the owner rather than a decision taken here:** the honest number is small today and will grow. If the front page should not show a live count while it is small, the fix is to show no count — not to show a made-up one. **Built and merged 2026-09-05.** Commits: claim `544ab29`, then `cc8dfe6`. Gate: typecheck 0 · lint 1 (**2 errors / 683 warnings, baseline**) · **build 0 with real-firebase-config=yes** · secrets 0 · artefacts 0 · dirty-after 0 · **569 pages**. **Verified in a browser, signed out** — which is how the page is usually seen, and the state in which the counts have to be permitted: the hero renders **"2 Active Jobs · 104 Verified Companies"**. No `500+`, no `190+`, no `1,200+`, and no Job Seekers card. The counts loading at all for an anonymous visitor is the proof that each aggregation query carries the constraint its read rule needs. **How it was found:** by opening the site. Twenty-seven phases of reading this code had not surfaced it, because nothing about `useState(500)` is a *bug* to a compiler, a linter or a security probe — it is a lie to a person, and only a person looking at the page can see it. Two phases in a row have now been found this way. **The line applied throughout: count what can be counted, say nothing where it cannot.** Jobs and companies are counted with `getCountFromServer`, which bills one read per thousand documents rather than reading them. The seeker figure is **removed rather than corrected**, because counting `users` needs a read the rules restrict to the owner and admins — correctly — so there is no honest number available to a visitor. Where nothing is countable, qualitative wording replaces the figure, following the pattern `register/page.tsx:117` already passes. The About page keeps exactly one number, **nine locations**, because `LOCATIONS_DATA` lists nine. **The trailing `+` was dropped deliberately.** These are exact counts, and a plus sign on an exact number is its own small untruth. **`tsc` earned its keep again:** this component already had a `query` state for the search box, and importing Firestore's `query` unaliased shadowed it. It surfaced as "Type 'String' has no call signatures" rather than reaching a browser. Lanes: worker PASS, uiux PASS, feedback PASS, skill PASS. **The most consequential of the five was not the home page.** `seeker/subscription/page.tsx` sold a paid plan on "stand out to **500+ verified employers**" against 104, and `AuthShell`'s **98% placement rate** is a claim about outcomes that nothing in this system records — there is no figure it could be a wrong version of. A wrong number is a defect; a fabricated outcome statistic attached to a payment is a different kind of problem. **Note for the owner, not a decision taken here:** the honest number is small today and will grow. If the front page should not show a live count while it is small, the answer is to show **no** count. | `368fc24` (2026-09-05, inside `Theni-Jobs-develop`) |

| `thenijobs/trust3-generated-sites-assert-unverified-claims` | `c6a6ac5` | MERGED_DEVELOP | 2026-09-05 | owner ("start fixing all these bugs one by one", 2026-09-05) via phase TRUST-3 | **The generated "Official Website" for each real business asserts credentials, availability and opening hours the business never supplied.** Found by opening one: `/uthayam-air-travels`, a **travel agency**, is advertised with **"On-Demand Repairs & Trusted Service Guarantee"**, **"Express Service & Technician Booking"**, **"On-site Visits • Certified Technicians • Service Warranty"**, careers headed **"Electrician, Plumber & Technician Jobs"**, and an FAQ stating as fact that it is **"open Monday through Saturday from 9:00 AM to 8:00 PM"**. The page is badged **"Verified Official Website · Powered by THENIJOBS"**, so a visitor reads all of it as the business's own words. **Three causes, all in `src/components/company/CompanyLandingWebsite.tsx`.** **(1) The preset matcher picks the wrong industry.** `:231` is `catLower.includes(m.toLowerCase())` over `Object.keys(INDUSTRY_THEMES)` **in declaration order**. The `service-business` preset lists the bare token `'Services'` and is declared before `corporate-professional`, so the category *"Professional & Business Services"* matches `'Services'` first and a travel agency is dressed as a repair shop. Any category containing a generic word is captured by whichever preset claims that word earliest. **(2) Every preset asserts specifics whichever one is chosen.** `healthcare-hospital` announces **"24/7 Medical Care"** — a clinic open nine-to-five advertised as always available, which is the kind of claim someone acts on in an emergency. `manufacturing-industry` announces **"ISO Certified Production"**, a certification the business may not hold. `education-college` claims **"accredited degree programs"**, `corporate-professional` **"certified financial auditing"**, `service-business` a **"verified warranty on all repair services"**. These are regulated, checkable claims made on behalf of named third parties by a directory they did not write. **(3) The hours are invented outright.** `:278` answers "What are the operating hours?" with `company.businessHours || '...open Monday through Saturday from 9:00 AM to 8:00 PM'`, and `:1191` prints the same fallback in the location panel. Where the business supplied nothing, the page states hours anyway — someone can travel to a shop that is shut because this page said it was open. This completes a family: TRUST-1 removed **invented businesses**, TRUST-2 removed **invented statistics**, this removes **invented claims about real businesses** — the most exposed of the three, because each one names a real company. Fix: make the matcher prefer the most specific category token rather than the first declared; replace every badge and tagline asserting a credential, certification or availability with wording describing what the directory actually knows; and state opening hours **only** when the business supplied them. Expected files: `src/components/company/CompanyLandingWebsite.tsx`, this ledger. Lanes: worker, uiux, feedback, skill. **Collision check at claim:** worktrees = primary (`main`) + `Theni-Jobs-develop` (`fdb4e08`) + this one. A second session merged **PRIV-1** (`f324bf8`) into `develop` during my previous phase; its files are `firestore.rules`, `SeekerPortfolioClient.tsx`, `seeker/profile/page.tsx`, `firestoreServer.ts` and `firestoreService.ts` — **none of which this phase touches**, and it never touches `CompanyLandingWebsite.tsx`. `ACTIVE` rows on `develop` = 0 and its worktree is gone. Worktree: `/Users/saai_siddharth/Projects/Clients/Theni-Jobs-trust3`. Acceptance: **`/uthayam-air-travels` must stop being a repair shop**, verified in a browser; no page may state opening hours the business did not supply; a grep must find no surviving assertion of ISO certification, accreditation, 24/7 availability or warranty in the preset table; the matcher must be shown to pick `corporate-professional` for *"Professional & Business Services"* while still picking `service-business` for a genuine repair category; typecheck 0, build 0 with real credentials, lint no worse than the 2-error baseline, page count unchanged at 569. **Built and merged 2026-09-05.** Commits: claim `0114639`, then `c6a6ac5`. Gate: typecheck 0 · lint 1 (**2 errors / 683 warnings, baseline**) · **build 0 with real-firebase-config=yes** · secrets 0 · artefacts 0 · dirty-after 0 · **569 pages**. **Matcher proven by running both versions over real categories**, not by reading the diff: `Professional & Business Services` **service-business → corporate-professional**; `Automobile & Transport`, `Healthcare & Hospital`, `Education & Training`, `Retail, Shop & Supermarket` and a bare `Services` **all unchanged**. Only the broken case moves, which is what distinguishes a fix from a rewrite. **Verified in a browser on `/uthayam-air-travels`:** no repairs copy, no invented hours, no ISO claim, no 24/7 claim, no advisory claim; the hours line reads *"Uthayam air travels has not listed its opening hours here. Please call before visiting to confirm."* **The rule is written into the file, above the preset table**, so the next person adding a preset inherits it: a badge names the category and nothing more — no credential, no availability, no guarantee. The three categories are not arbitrary, they are what was actually there: `ISO Certified`, `accredited degree programs` and `certified financial auditing` are **credentials**; `24/7 Medical Care`, `Open Daily` and `Admissions Open` are **availability**; `Service Warranty` and `verified warranty on all repair services` are **guarantees**. **The healthcare one deserves naming separately.** `24/7 Medical Care`, plus a body line reading `General Medicine • Pediatrics • Orthopedics • 24/7 Pharmacy`, was applied to every business in the healthcare category. A clinic that closes at five was advertised as always open, on a page carrying its own name. That is a claim someone acts on in an emergency. **Completes a family.** TRUST-1 removed **invented businesses**, TRUST-2 removed **invented statistics**, TRUST-3 removes **invented claims about real businesses** — the most exposed of the three, because each page names a real company and several of the claims were regulated ones. Lanes: worker PASS, uiux PASS, feedback PASS, skill PASS. **A second session merged PRIV-1 (`f324bf8`) into `develop` during the previous phase.** Its files are `firestore.rules`, `SeekerPortfolioClient.tsx`, `seeker/profile/page.tsx`, `firestoreServer.ts` and `firestoreService.ts`; this phase touches only `CompanyLandingWebsite.tsx`, so the two do not meet. Checked by diffing that merge's first parent rather than by reading its ledger row. **Findings carried out:** (a) the business in the example is a **travel agency filed under "Professional & Business Services"**, so no preset could have been right — the template can only be as accurate as the category, and category quality is a data problem this phase does not touch. (b) `jobCtaText` still names job families per category ("Consultant, Executive & Associate Careers"), which is softer because the section beneath it states the truth about vacancies, but it is the same kind of guess. | `4c47ee7` (2026-09-05, inside `Theni-Jobs-develop`) |
| `thenijobs/trust4-fabricated-category-job-counts` | `c3708f8` | MERGED_DEVELOP | 2026-09-05 | owner ("start fixing all these bugs one by one", 2026-09-05) via phase TRUST-4 | **Every `/jobs-in-<location>` page shows fabricated per-category job counts, and after SEO-5's fix they now contradict the honest number on the same page.** `src/components/seo/locationData.ts:76-90` — `CATEGORIES_LIST` — carries a hard-coded `count` string on each of the 15 categories: `'120+'`, `'95+'`, `'60+'`, down to `'25+'`. Rendered at exactly one site, `LocationJobPageClient.tsx:234`, in the "Jobs by Category in {location}" sidebar on all 9 location pages. **Verified live on 2026-09-05:** `/jobs-in-madurai` reads **"0 Active Listings"** at the top of the page — the honest count SEO-5 restored — while the sidebar six inches below it reads **"IT & Software Development in Madurai · 60+"**, **"Sales & Marketing in Madurai · 95+"**, **"Work From Home & Remote in Madurai · 30+"**, for every one of the 15 categories, on every one of the 9 locations. Before SEO-5 this was merely false; now it is **visibly self-contradictory on one screen** — the platform states 0 jobs and then lists dozens per category a few lines later. **The honest version already exists two lines away, in the sibling file.** `CategoryJobPageClient.tsx`'s own "Other Categories in {location}" list (`:191-199`) renders `{c.name}` and a chevron — no number. `LocationJobPageClient.tsx`'s "Jobs by Category" list is the only one of the two that attaches a fabricated quantity to the same kind of link. This is the fourth in the family: TRUST-1 invented businesses, TRUST-2 invented platform statistics, TRUST-3 invented claims about real businesses, TRUST-4 invents per-category counts that are not merely wrong but now actively contradict a real number rendered on the same page. Fix: remove the `count` field from `CATEGORIES_LIST` and stop rendering it — match the honest pattern the sibling component already uses. No live per-category-per-location count is computed anywhere else in the app, and adding one would mean 15 aggregation queries per page load for a sidebar link list; that is disproportionate to what it is for, and not what this phase does. Expected files: `src/components/seo/locationData.ts`, `src/components/seo/LocationJobPageClient.tsx`, this ledger. Lanes: worker, uiux, feedback, skill. **Collision check at claim:** worktrees = primary (`main`) + `Theni-Jobs-develop` (`5a7687b`) + this one; `ACTIVE` rows = 0. Worktree: `/Users/saai_siddharth/Projects/Clients/Theni-Jobs-trust4`. Acceptance: no `+`-suffixed fabricated count survives in `CATEGORIES_LIST` or its render site, checked by grep; `/jobs-in-madurai` (and one other location) verified in a browser to no longer contradict its own "Active Listings" figure; typecheck 0, build 0 with real credentials, lint no worse than the 2-error baseline, page count unchanged at 569. **Built and merged 2026-09-05.** Commits: claim `9d05078`, then `c3708f8`. Gate: typecheck 0 · lint 1 (**2 errors / 683 warnings, baseline**) · **build 0 with real-firebase-config=yes** · secrets 0 · artefacts 0 · dirty-after 0 · **569 pages**. **Verified in a browser on `/jobs-in-madurai`:** "0 Active Listings" at the top, and the category sidebar now lists **names with no attached number** — no more contradiction between the top of the page and the sidebar six inches below it. **Found while scoping SEO-6** (client-side category filtering, still PLANNED): reading `CATEGORIES_LIST` to understand the substring-match risk surfaced the `count` field sitting right next to the `slug`/`name` fields SEO-6 cares about. Kept as its own TRUST-class phase rather than folded into SEO-6, because the two are unrelated defects that happen to live in the same file — one is a data-integrity lie, the other is a filtering-precision bug. Lanes: worker PASS, uiux PASS, feedback PASS, skill PASS. **Not verified:** the other eight location pages by individual browser check — only Madurai was opened. The fix is a field removal with no per-location branching, so the same result is expected everywhere `CATEGORIES_LIST` is mapped, but it was not clicked through one by one. **Finding carried out, sharpening SEO-6's own scope:** the `it` category slug is a substring of `security` and of `hospitality` (`secur-**it**-y`, `hosp**it**al**it**y`), so the existing `.includes(categorySlug)` match on `CategoryJobPageClient`/`LocationJobPageClient` would put a Security Guard or a Hospitality job on the **IT & Software** category page. Concrete evidence for the fix SEO-6 already names. | `71a72e4` (2026-09-05, inside `Theni-Jobs-develop`) |
| `thenijobs/seo6-category-match-precision` | `d822f9f` | MERGED_DEVELOP | 2026-09-05 | owner ("start fixing all these bugs one by one", 2026-09-05) via phase SEO-6 | **Three matching-precision bugs in the location/category job pages, found while scoping and confirmed against live data and unit tests rather than eyeballed.** **(1) A two-letter category slug matched inside unrelated words.** `CategoryJobPageClient.tsx`'s `matchCat` used `.toLowerCase().includes(categorySlug)` against `title`/`category`/`jobType`. Checked programmatically against the real 15-slug category list: `'it'` is a substring of `'hospitality'` and of `'security'`, and of the common job word `'kitchen'`. A Hospitality, Security or Kitchen job could be shown on the **IT & Software** category page. Side-by-side, old vs new, on the same four titles: `Hospitality Manager` **true -> false**, `Security Guard` **true -> false**, `Kitchen Staff` **true -> false**, `IT Support Executive` **true -> true** — the one genuine match survives, the three accidents do not. Fixed with a word-boundary regex, which is **strictly a subset** of what `.includes()` matched — anything that matched as a whole word before still matches; only mid-word accidents are excluded. It does not add recall (a title with no shared substring, like 'Driver Required' against slug `driving`, still will not match) — that is a synonym-expansion problem, the kind `/jobs/page.tsx` solves with `RULE_BASED_SYNONYMS`, and a different, larger fix than the false-positive bug closed here. **(2) `full-time`/`part-time` could never match by jobType at all.** Read directly from production: both real job documents store `jobType: 'full_time'` — underscore. The slugs are `'full-time'`/`'part-time'` — hyphen. `.includes('full-time')` against `'full_time'` never matches, confirmed live, so `/jobs-in-*/full-time` and `/jobs-in-*/part-time` could only ever show a job by an exact literal hyphen appearing in its title, which no real title has. Fixed by comparing on the normalised form. **(3) `work-from-home` shares no substring with its real value at all.** The app's own jobType vocabulary (`src/app/jobs/page.tsx:371-372`) stores this as `'remote'` or `'wfh'` — words with nothing in common with the slug. No hyphen/underscore swap fixes that, so an explicit two-entry alias map closes it, grounded in the vocabulary the rest of the app already uses rather than invented. **(4) Both components fetch 15 jobs and filter client-side, so a genuine match past the 15th was silently dropped.** Raised to `PUBLIC_LIST_LIMIT = 500`, the same cap and the same name PERF-3 set on `/jobs` and `/businesses` for the identical reason. **Deliberately not done, and said so rather than left silent:** unlike `/jobs` and `/businesses`, these two components do not surface a 'showing the first 500' notice when the cap is hit. At the current job volume the cap cannot bind, and adding pagination-style chrome to a landing page whose job is to funnel traffic to `/jobs` is scope beyond fixing the three matching bugs above; recorded as a gap rather than silently matched to PERF-3's UI as well as its limit. Also not done: multi-word slugs whose only difference from the real field is a space vs. a hyphen (`'customer service'` vs `'customer-service'`) still do not match — the same limitation the old code had, not a regression, and out of scope for a precision fix. Expected files: `src/components/seo/CategoryJobPageClient.tsx`, `src/components/seo/LocationJobPageClient.tsx`, this ledger. Lanes: worker, uiux, skill. **Collision check at claim:** worktrees = primary (`main`) + `Theni-Jobs-develop` (`64d2689`) + this one; `ACTIVE` rows = 0. Worktree: `/Users/saai_siddharth/Projects/Clients/Theni-Jobs-seo6`. Acceptance: the word-boundary fix demonstrated old-vs-new on the same inputs, showing the exact false positives eliminated and the true positive preserved; the jobType normalisation checked against the live database's actual `full_time` value; typecheck 0, build 0 with real credentials, lint no worse than the 2-error baseline, page count unchanged at 569. **Built and merged 2026-09-05.** Commits: claim `6f5d633`, then `d822f9f`. Gate: typecheck 0 · lint 1 (**2 errors / 683 warnings, baseline**) · **build 0 with real-firebase-config=yes** · secrets 0 · artefacts 0 · dirty-after 0 · **569 pages**. **The word-boundary fix was demonstrated, not asserted:** the exact matcher logic run outside the app against the same four titles gave `Hospitality Manager` **true -> false**, `Security Guard` **true -> false**, `Kitchen Staff` **true -> false**, `IT Support Executive` **true -> true** — every accidental match removed, the one real match kept. **Confirmed `PUBLIC_LIST_LIMIT` and both `fbLimit(15)` removals are in the merged tree:** `grep` finds `PUBLIC_LIST_LIMIT` three times in each of the two files and no `fbLimit(15)` anywhere under `src/components/seo/`. Lanes: worker PASS, uiux PASS, skill PASS. **Not verified:** no browser click-through, because the current database (2 jobs, neither hospitality/security/kitchen-titled) cannot demonstrate the fix live — the same reason this phase relied on unit-level evidence for the matcher instead. What is verified live is the `jobType: 'full_time'` value the second bug is built around. **Findings carried out, both stated as gaps rather than silently left:** no 'showing the first 500' capped notice on either component, unlike `/jobs`/`/businesses`; and a slug that differs from its field only by a space vs. a hyphen (`customer service` vs `customer-service`) still does not match — a pre-existing limitation, not a regression from this phase. | `b0f2e5e` (2026-09-05, inside `Theni-Jobs-develop`) |
| `thenijobs/trust5-stale-price-in-pricing-meta-description` | `b8ae100` | MERGED_DEVELOP | 2026-09-05 | owner ("start fixing all these bugs one by one", 2026-09-05) via phase TRUST-5 | **The pricing page's own meta description advertises a price that does not exist.** `src/app/pricing/layout.tsx:6` — the META DESCRIPTION, which reaches Google search results the same way `jobs/layout.tsx`'s did before TRUST-2 fixed it — reads: 'Starting at just ₹2.74/day (₹999/yr) for 15 job postings, company portfolio, digital ID card, and priority candidate access.' **Every number in that sentence is wrong, checked against `SUBSCRIPTION_PLANS` in `src/lib/constants.ts`, the source PAY-1 already established as ground truth.** The entry paid plan (`standard`) costs **₹480/yr**, not ₹999 — a 108% overstatement. Its daily equivalent is **₹1.31**, not ₹2.74 — more than double. Its feature list says **'10 Active Job Postings'**, not 15. `₹999` is not a coincidence: it is the exact stale `basic` price PAY-1 found hard-coded in `verify/route.ts`'s dead price table before that phase derived it from `SUBSCRIPTION_PLANS` — this file carries the same abandoned number in prose, in the one place it reaches search engines before a visitor ever opens the page. This is the fifth in the family, and the first to be a **wrong price** rather than a wrong count or an invented credential: TRUST-1 invented businesses, TRUST-2 invented statistics, TRUST-3 invented claims about real businesses, TRUST-4 invented per-category counts, TRUST-5 quotes a subscription price that would set the wrong expectation for a business owner before they ever see the real one. Fix: derive the description from `SUBSCRIPTION_PLANS` itself rather than replace one hard-coded number with another. The `standard` plan's `price`, `dailyEquivalent` and the job-posting count parsed from its own `features` array (`'10 Active Job Postings'`) all come from the single source every other phase this session has pointed the rest of the app at, so a future price change updates this sentence automatically instead of leaving a sixth stale copy for someone else to find. Expected files: `src/app/pricing/layout.tsx`, this ledger. **No change to `SUBSCRIPTION_PLANS` itself** — the prices are the owner's numbers, read here rather than edited. Lanes: worker, uiux, feedback, skill. **Collision check at claim:** worktrees = primary (`main`) + `Theni-Jobs-develop` (`0110269`) + this one; `ACTIVE` rows = 0. Worktree: `/Users/saai_siddharth/Projects/Clients/Theni-Jobs-trust5`. Acceptance: the rendered meta description on `/pricing` must state the real `standard`-plan numbers, verified against the live `SUBSCRIPTION_PLANS` values and against what the page's own visible pricing card already shows; no leftover `₹999`/`₹2.74`/`15 job postings` string anywhere in the file; typecheck 0, build 0 with real credentials, lint no worse than the 2-error baseline, page count unchanged at 569. **Built and merged 2026-09-05.** Commits: claim `3f839d4`, then `b8ae100`. Gate: typecheck 0 · lint 1 (**2 errors / 683 warnings, baseline**) · **build 0 with real-firebase-config=yes** · secrets 0 · artefacts 0 · dirty-after 0 · **569 pages**. **Verified against the actual built static export, not just the dev server:** `out/pricing.html`'s `<meta name="description">` reads **'Starting at just Rs 1.31/day (Rs 480/yr) for 10 job postings'** — exactly the `standard` plan's real `dailyEquivalent`, `price`, and the job-posting count parsed from its own `features` array, matching the visible price card on the same page. **The independent-discovery detail is worth keeping:** Rs 999 was not copied from one bug to the next. PAY-1 found and removed it from `verify/route.ts`'s dead `PLAN_PRICES` table, unrelated to this file, in an earlier phase this same session. Finding the identical abandoned number surface a second time, in prose rather than code, in a file neither phase had touched, is a small sample of how a real number drifts out of every place it was ever pasted once nothing points back at the source. Lanes: worker PASS, uiux PASS, feedback PASS, skill PASS. **Not verified:** no change to `SUBSCRIPTION_PLANS` itself and none intended — the prices are the owner's numbers, read here rather than edited. | `2414d6c` (2026-09-05, inside `Theni-Jobs-develop`) |
| `thenijobs/qual3-public-pages-no-title` | `986e325` | MERGED_DEVELOP | 2026-09-05 | owner ("start fixing all these bugs one by one", 2026-09-05) via phase QUAL-3 | **Five public, marketing-facing pages carry no page-specific browser title at all** — they inherit the site-wide default ('THENIJOBS – Jobs in Theni, Tamil Nadu | Local Job Portal') because SEO-2 removed the title template that used to paper over this, and none of these five ever had their own metadata to begin with. Confirmed live: `/register-business`'s tab title is the homepage's, not its own, verified in a browser after SEO-2 landed. **Scope is deliberately five files, not the sixty-three a repository sweep found.** A `page.tsx`-with-no-metadata-and-no-layout.tsx-sibling scan turned up 63 client components. **Fifty-eight are authenticated `/admin/**`, `/employer/**` and `/seeker/**` dashboard screens** — low search value since a signed-in visitor is not finding them through a search engine, and the second session's `uiux1-canonical-dashboard-system` phase already reworked their visual chrome; giving them titles in this phase would be guessing at a decision that phase may already have made deliberately, or may make next. The five kept are the ones a stranger reaches directly — from an ad, a shared link, or a search result — before ever signing in: `/register`, `/register-business`, `/company/register`, `/login`, `/forgot-password`. **Two of the five are separately-linked, functionally-overlapping registration routes** — `/company/register` (linked from `/businesses`, `/pricing`, company profile pages and the footer — the more heavily-used one) and `/register-business` (linked once, from the home hero). Whether the site should have two business-registration entry points is a routing decision this phase does not make, the same way SEO-1 did not decide whether a company should live at one URL or three; each page gets an honest title describing what it actually is, and the duplication itself is recorded as a finding. Fix: a `layout.tsx` sibling for each of the five, following the convention every other public page in this tree already uses (`pricing/layout.tsx`, `jobs/layout.tsx`, `businesses/layout.tsx`), with a title and description drawn from each page's own visible heading — no invented numbers, no superlatives, matching the discipline this session's TRUST phases established. Expected files: five new `layout.tsx` files under `src/app/{register,register-business,company/register,login,forgot-password}/`, this ledger. No existing file is modified. Lanes: uiux, feedback, skill. **Collision check at claim:** worktrees = primary (`main`) + `Theni-Jobs-develop` (`9d8ef46`) + this one; `ACTIVE` rows = 0. Worktree: `/Users/saai_siddharth/Projects/Clients/Theni-Jobs-qual3`. Acceptance: each of the five pages renders its own `<title>` in the built export, verified by reading the actual HTML rather than the dev server; none reuse the site-wide default; no fabricated claim in any description; typecheck 0, build 0 with real credentials, lint no worse than the 2-error baseline; **page count stays at 569** — a `layout.tsx` wraps an existing route's `page.tsx`, it does not add a new one, so five new files should move zero pages, and that is itself worth checking rather than assuming. **Built and merged 2026-09-05.** Commits: claim `1f773c7`, a correction `faa53ef`, then the fix `986e325`. Gate: typecheck 0 · lint 1 (**2 errors / 683 warnings, baseline**) · **build 0 with real-firebase-config=yes** · secrets 0 · artefacts 0 · dirty-after 0 · **569 pages, unchanged, as corrected in the claim and confirmed rather than assumed** — a `layout.tsx` wraps an existing route's `page.tsx`, it does not add one. **Verified against the built export directly**, reading each HTML file's own `<title>` rather than trusting the dev server: `/register` -> 'Create Your Account | THENIJOBS', `/register-business` and `/company/register` -> 'Register Your Business | THENIJOBS', `/login` -> 'Sign In | THENIJOBS', `/forgot-password` -> 'Reset Your Password | THENIJOBS'. A control page, `/jobs`, checked in the same pass and confirmed unaffected. **A self-correction worth keeping.** The claim row originally stated an acceptance criterion of 'page count changes from 569 to 574' — wrong, and caught before a single line of the fix was written, with its own commit (`faa53ef`) explaining why: adding a `layout.tsx` does not create a new route. The build then confirmed 569, matching the corrected prediction rather than the original wrong one. **Scope discipline:** 58 of the 63 pages a repository-wide sweep found are authenticated `/admin/**`, `/employer/**`, `/seeker/**` dashboards, deliberately left out. They carry low search value on their own account, and the second session's `uiux1-canonical-dashboard-system` phase already reworked their chrome — giving them titles here would guess at a decision that belongs to that phase, not this one. **One duplication surfaced, not resolved.** `/company/register` and `/register-business` are two live, separately-linked routes doing the same job — the former reached from `/businesses`, `/pricing`, company profile pages and the footer, the latter only from the home hero. Neither is made canonical here; each gets an honest title for what it is, and the overlap itself is recorded as the finding, the same posture SEO-1 already takes toward the three-URL company duplication. Lanes: uiux PASS, feedback PASS, skill PASS. **Not verified:** no click-through of the five pages in a live browser session beyond reading their `<title>` from the export — the pages themselves were not modified, only wrapped, so their own rendering was never at risk. | `86402f9` (2026-09-05, inside `Theni-Jobs-develop`) |
| `thenijobs/rules5-draft-portfolio-sites-are-public` | `734fc24` | MERGED_DEVELOP | 2026-09-05 | owner ("start fixing all these bugs one by one", 2026-09-05) via phase RULES-5 | **A business's unpublished company website is fully readable by anyone, in production, right now.** `firestore.rules`'s `portfolioSites` block is `allow read: if true;` with a comment reading 'Public site viewing (filtering by status handled in query/client)' — the exact anti-pattern RULES-2 and RULES-3 already closed elsewhere in this file: a security boundary that exists only in the client's good behaviour, not in the rule that actually decides who may read. **Demonstrated against production, read-only, on 2026-09-05.** A plain unauthenticated REST read against `portfolioSites` — no ID token, only the public web API key every page already ships — returned both real documents in the collection in full: their `sections`, `branding`, `theme` and `seo` fields. One of the two has `status: 'draft'`. The app's own client, `PublicPortfolioPageClient.tsx:106`, checks `site.status !== 'published'` and shows 'This portfolio website is currently in draft mode' instead of the content — but that check runs in the browser, after the data has already been sent. A request that skips the app entirely — a REST call, a browser devtools query, anything that is not this specific React component — receives the full draft. **The static export is not the exposure.** `getAllPublishedPortfolioUsernamesServer()` (`firestoreServer.ts:378-390`) filters `status == 'published'` in the query itself at build time, so no draft is ever baked into a static HTML page. The gap is purely at the database layer: anyone who queries `portfolioSites` directly, bypassing the app's UI and its build-time filtering both, reads what the owner has not chosen to publish. Fix: `allow read` requires `status == 'published'`, or the requester is authenticated as the site's own `ownerId`, or an admin — the same shape every other status-gated collection in this file already uses (`companies`, `jobs`), and compatible with the one legitimate authenticated case this app has, the owner previewing their own unpublished site (`isDraftPreview` in `CompanyLandingWebsite.tsx`). No unauthenticated draft-preview-link feature exists to break — checked before assuming. Expected files: `firestore.rules`, this ledger. Never touches `storage.rules`, `database.rules.json`, `src/**`. Lanes: security, rules, skill. **Collision check at claim:** worktrees = primary (`main`) + `Theni-Jobs-develop` (`5a96c02`) + this one; `ACTIVE` rows = 0. Worktree: `/Users/saai_siddharth/Projects/Clients/Theni-Jobs-rules5`. Acceptance: an **emulator probe with real signed-in users** proving a draft site is denied to a stranger and to a signed-out request, allowed to its owner and to an admin, and a published site stays readable by anyone; **revert-and-watch** showing the old rule allowing exactly what the new one denies; at least one assertion denied under **both** rule sets. Rules reach production only on the owner's `firebase deploy`. **Built and merged 2026-09-05.** Commits: claim `b992dd9`, then `734fc24`. Gate: typecheck 0 · lint 1 (**2 errors / 683 warnings, baseline**) · **build 0 with real-firebase-config=yes** · secrets 0 · artefacts 0 · dirty-after 0. **Probe, both rule sets, real signed-in users, ids unique per run, data seeded through the real create rule:** a stranger reading a draft site goes **ALLOW -> DENY**; the owner, an admin, and anyone reading a published site all stay **ALLOW**; two write-side guards **DENY under both rule sets**. **A probe-design mistake, corrected before it was trusted.** The first version of this probe put its non-vacuity guard on the READ side — 'a signed-out client reads the draft, want DENY' — and it **failed under the old rules**, correctly: `allow read: if true` denies nothing at all on the read path, which is the entire finding this phase closes. A guard needs a case that was already gated before the phase touched anything, and the read side had none to offer. Moved to the write side, where create/update already required `ownerId` to match in both rule sets, unchanged by this phase. **Building the guard on the wrong side would have made a real vulnerability look like a broken probe** — worth keeping as a general lesson: a rule this permissive cannot supply its own read-side guard. **Discovered while sweeping for new bugs after the queue emptied of unblocked work**, not from a planned audit item — found by re-checking a rule comment ("filtering by status handled in query/client") against the pattern RULES-2 and RULES-3 had already named as the thing to distrust in this file. Lanes: security PASS, rules PASS, skill PASS. **Not verified:** no production probe — demonstrating the read against `thenijobs-9f01d` directly would mean reading a real business's actual unpublished content, which is exactly the thing being closed. The single read-only REST check that opened this phase already showed the live exposure without printing the draft's own content, and the emulator with real users stands in for the rest. **Owner action required, and urgent relative to the other rules already queued:** `firebase deploy --only firestore:rules --project thenijobs-9f01d` closes this alongside RULES-1 through RULES-4, all still undeployed. Until then, any business that has started building its website but not yet published it has that draft fully readable by anyone who asks. | `4d64c81` (2026-09-05, inside `Theni-Jobs-develop`) |
| `thenijobs/rules6-company-follows-and-dead-collections` | `0301812` | MERGED_DEVELOP | 2026-09-05 | owner ("start fixing all these bugs one by one", 2026-09-05) via phase RULES-6 | **A full inventory of every read rule in `firestore.rules`, following RULES-5's discovery that this file had more than one instance of the same anti-pattern.** Seven collections read `if true`: `reviews`, `services`, `advertisements`, `usernames`, `companyFollows`, `seoAnalytics`, `companySlugs`. Five are public-by-design and correct — reviews, services, ads and slugs are meant to be discoverable, and `companySlugs`'s public read is a **recorded owner decision (D-SLUG, RULES-1)** for duplicate-slug detection, not touched here even though a related concern about it is logged separately below. Two are not. **(1) `companyFollows` is the one user-relationship collection in this file that is not scoped to its own user.** `savedJobs`, `jobAlerts`, `notifications` and `subscriptions` all gate read with `resource.data.userId == request.auth.uid || isAdmin()`. `companyFollows` alone reads `if true`, so any stranger can enumerate exactly which real user follows which real company — a behavioural fact about a specific person, openly readable by anyone with no ownership check at all. Confirmed live: one real `{companyId, createdAt, userId}` document exists in production today, and it was read back with a plain unauthenticated request. Every one of the four call sites in `firestoreService.ts` (`followCompany`, `unfollowCompany`, `isFollowingCompany`) already queries by the caller's own `userId`, and the one UI caller (`JobDetailPageClient.tsx:267`) only runs when a `uid` is present — so scoping read to the owner changes nothing a signed-in user can already do and closes what a stranger could. **(2) `usernames` and `seoAnalytics` have zero references anywhere in `src/`** — no read, no write, in either direction. The same shape RULES-2 found in `whatsappApplications` and RULES-3 found in `certificates`/`uploads`: a rule block written for a feature that was never built, left exactly as open as the day it was drafted because nothing exercises it and nothing complains. Closed to default-deny, which is provably behaviour-preserving since nothing in the tree depends on the current openness. **Recorded, not fixed: `companySlugs` stores a business owner's plaintext email inline** in a document whose read rule the owner already decided should be public, for slug-duplicate checking. The stated purpose only needs slug *existence* to be checkable; the current implementation also lets anyone enumerate registered owners' email addresses by paginating the collection or querying `where('email','==',...)` directly — the exact query `register-business/page.tsx:99` runs client-side. This is not touched here: it sits inside a decision the owner already made (D-SLUG), and reversing part of a recorded decision without being asked is a different kind of change than closing an unexamined default. Logged as its own item for the owner rather than silently altered. Fix: `companyFollows` read scoped to the follower or an admin; `usernames` and `seoAnalytics` default-denied entirely. Expected files: `firestore.rules`, this ledger. Never touches `storage.rules`, `database.rules.json`, `src/**`, and does not touch `companySlugs`. Lanes: security, rules, skill. **Collision check at claim:** worktrees = primary (`main`) + `Theni-Jobs-develop` (`a55247b`) + this one; `ACTIVE` rows = 0. Worktree: `/Users/saai_siddharth/Projects/Clients/Theni-Jobs-rules6`. Acceptance: an **emulator probe with real signed-in users** proving a stranger cannot read another user's follow record while the follower and an admin still can; `usernames`/`seoAnalytics` denied to everyone including their own would-be creator, since nothing in the app ever creates one; **revert-and-watch** with the old rules allowing what the new ones deny; guards denied under both rule sets placed on the write side, following the lesson RULES-5 recorded about read-side guards being meaningless against an `if true` rule. Rules reach production only on the owner's `firebase deploy`. **Built and merged 2026-09-05.** Commits: claim `669f917`, then `0301812`. Gate: typecheck 0 · lint 1 (**2 errors / 683 warnings, baseline**) · **build 0 with real-firebase-config=yes** · secrets 0 · artefacts 0 · dirty-after 0. **Probe, both rule sets, real signed-in users, ids unique per run:** stranger reading another user's `companyFollows` record **ALLOW -> DENY**; the follower and an admin unchanged at **ALLOW**; a plain signed-in user reading `usernames` or `seoAnalytics` **ALLOW -> DENY**; an admin reading `seoAnalytics` unchanged at **ALLOW**, proving the admin-only closure rather than a full one; two write-side guards **DENY under both rule sets**. **A methodology note carried forward from RULES-5 and confirmed useful again:** both non-vacuity guards were placed on the write side deliberately — this file's `if true` reads deny nothing at all, so a read-side guard against an unconditionally-open rule proves nothing about the probe's validity. **The one item explicitly not touched is worth restating plainly.** `companySlugs` stores a business owner's plaintext email inline, in a document whose public read is `RULES-1`'s own recorded decision (`D-SLUG`) — and that decision's own comment names `email` as one of the stored fields, which is evidence the tradeoff was considered rather than an oversight. This phase logs, rather than reverses, that the implementation lets anyone enumerate registered owners' emails by paginating the collection or by the exact `where('email','==',...)` query `register-business/page.tsx` already runs client-side. A later phase or the owner directly can decide whether the stated purpose — checking whether a *slug* is taken — actually requires the *email* to travel in the same publicly-readable document. Lanes: security PASS, rules PASS, skill PASS. **Not verified:** no production probe for the same reason RULES-5 recorded — demonstrating either abuse against `thenijobs-9f01d` would mean reading a real user's actual data, which is the thing being closed. The read-only single-document check that opened this phase already showed the live exposure without printing anyone's identity. **Owner action:** this rules file now has six phases queued behind one `firebase deploy --only firestore:rules --project thenijobs-9f01d` — RULES-1 through RULES-6 — none of them live yet. | `f654ba9` (2026-09-05, inside `Theni-Jobs-develop`) |
| `thenijobs/pay2-job-posting-limit-not-enforced` | `14bf218` | MERGED_DEVELOP | 2026-09-05 | owner ("start fixing all these bugs one by one", 2026-09-05) via phase PAY-2 | **The Free plan's "1 Active Job Posting" limit, and every paid tier's higher limit, is decorative — nothing in the app stops an employer from posting past it.** `employer/post-job/page.tsx:82` computes `planCheck = canPostNewJob(planSlug, activeJobsCount)`, and `planCheck.allowed` drives exactly one thing: which banner renders at the top of the page — an amber "Job Posting Limit Reached" warning with an "Upgrade Plan" link when `false`, a plain blue counter when `true`. **`planCheck.allowed` is referenced nowhere else in the file.** The submit button's `disabled` prop (`:759`) is `loading || (step === 1 && !isStep1Valid)` — no reference to the plan check at all. `handlePost()` (`:141-160`) checks `!companyId` and an empty title, and nothing else, before calling `createDocument('jobs', jobPayload)`. **An employer who has already hit their plan's limit can read the warning banner, click through all four wizard steps, and successfully create another job.** The banner tells the truth about the limit; the button and the handler do not act on it. `firestore.rules`'s `safeJobCreate()` does not enforce a count either — it checks `status`, `isActive`, `isFeatured` and `rejectionReason`, never how many jobs the poster already has. A Firestore rule cannot cheaply count existing documents against a request without a `get()`/aggregation the way this file avoids elsewhere for cost reasons, so the natural enforcement point is the one this phase fixes, not the rule. **This is the only count-based plan limit found anywhere in the app** — a search for every file referencing `canPostNewJob`, `PLAN_FEATURE_MATRIX` or `hasFeaturePermission` finds exactly two: this page, and `employer/company-profile/page.tsx`, which only uses `hasFeaturePermission` for **boolean** feature gates (whether a whole tab is shown) rather than a numeric count. Whether other numeric limits named in `SUBSCRIPTION_PLANS.features` strings — 3 products, 6 gallery images, 5 team members — are enforced anywhere they are actually created is a separate question this phase does not answer; recorded as a finding rather than assumed either way. Fix: `handlePost()` refuses with the same early-return shape as its existing `!companyId` and empty-title guards when `!planCheck.allowed`; the submit button additionally disables on step 4 when `!planCheck.allowed`, so the flow visibly stops at the point that actually creates the job rather than after the fact. Steps 1-3 are left open — the warning banner already runs the whole time, and blocking earlier prevents nothing a blocked step 4 does not already prevent. Expected files: `src/app/employer/post-job/page.tsx`, this ledger. `src/lib/plans.ts`'s `canPostNewJob` itself is correct and untouched — the bug is that its result was computed and displayed but never acted on. Lanes: worker, feedback, skill. **Collision check at claim:** worktrees = primary (`main`) + `Theni-Jobs-develop` (`b3b1a81`) + this one; `ACTIVE` rows = 0. Worktree: `/Users/saai_siddharth/Projects/Clients/Theni-Jobs-pay2`. Acceptance: the exact `disabled` boolean expression and `handlePost`'s guard clause proven, old vs new, against the same inputs — an over-limit employer denied, an under-limit employer unaffected; typecheck 0, build 0 with real credentials, lint no worse than the 2-error baseline; **page count checked before and after in this same worktree, not against a remembered figure** — the claim originally assumed a stale absolute number (569) left over from several ticks earlier in this session; a real baseline build taken in this worktree at the exact claimed commit, before any edit, returned 572, and 572 is also what the built export shows with the fix applied. The comparison that matters — before and after, same worktree, same commit — passed; the number quoted at claim time did not. **Not claimed:** an authenticated end-to-end click-through, which would require a real employer account already at their plan's limit and would write to `thenijobs-9f01d`. **Built and merged 2026-09-05.** Commits: claim `6f2b2d7`, ledger self-correction `74714af`, fix `14bf218`, merge `b62cacc`. Gate after the last phase-branch commit: typecheck 0 · lint 1 (**2 errors / 683 warnings, unchanged from the standing baseline, neither in the touched file**) · **build 0 with real-firebase-config=yes** · secrets:bundle 0 · secrets:src-literals 0 · tracked-artefacts 0. **Revert-and-watch, in the phase worktree:** `git revert --no-commit 14bf218` removed both the guard clause and the disabled-button change; a standalone comparison of the button's `disabled` expression against an over-limit employer at step 4 returned to `false` (bug reproduced); `git revert --abort` restored the fix, `git status --porcelain` empty. **Old-vs-new logic table** (four representative inputs against the exact `disabled` expression): over-limit at step 4 `false -> true`; under-limit at step 4 unchanged (`false`); first post ever unchanged (`false`); over-limit but mid-wizard at step 2 unchanged (`false`) — only the intended case changes. **Page-count correction carried through, not silently dropped:** the claim's own acceptance line originally asserted a remembered figure of 569; a fresh build taken in the phase worktree at the claimed base commit (`b3b1a81`), with the fix stashed out, returned 572; the same worktree with the fix re-applied also returned 572. 572 is recorded here as the current verified baseline, not 569 — the fix itself changes zero pages, exactly as expected for a client-side guard clause with no new route. Lanes: worker PASS, feedback PASS (the two new toasts follow the file's own existing `toast.warning` pattern for the sibling `!companyId` and empty-title guards), skill PASS. **Not verified:** an authenticated browser click-through by a real employer already at their plan's limit — doing so would require writing a job document to `thenijobs-9f01d`, which this session does not do. The logic proof (old-vs-new comparison of the exact boolean expression executed by the button and the guard clause) is the verification standard used for pure client-side logic elsewhere this session (SEO-6); a rules Playground or emulator probe is not applicable here because no `firestore.rules` line changed. | `b62cacc` (2026-09-05, inside `Theni-Jobs-develop`) |
| `thenijobs/priv1-portfolio-contact-exposure` | `1b87cc0` | MERGED_DEVELOP | 2026-09-05 | owner ("For all the 3 do the recommended", 2026-09-05, answering a CTO-scoped phase contract) via phase PRIV-1 | **A seeker's phone and email render unconditionally on their public portfolio, and the toggle that looks like it controls this does nothing.** `firestore.rules` `seekerProfiles` block grants document-level `read` to anyone once `isPortfolioPublic == true`; phone/email live in that same document (`seeker/profile/page.tsx:61-62,102-103,230-231,606-607`). `SeekerPortfolioClient.tsx:55` declares `privacySettings?: {hidePhone, hideEmail, hideAddress, hideResume, hideContact}` and reads it at `:703,716-718` to decide whether to render phone/email — but `grep -rl privacySettings src/` finds exactly one file, that same client component: nothing anywhere writes it. No settings UI exists. It is always `undefined`, so phone/email always render for any opted-in-public seeker. The rule's own comment (from a prior session, added during the `main`/`develop` reconciliation `bc8014e`) already documents that even a working toggle would be presentation-only against a document-level Firestore rule. Production is a static export with no working `/api/**` layer to redact server-side, so the only real enforcement point is the rule itself. Owner decisions (`For all the 3 do the recommended`, 2026-09-05): ship the render-level mitigation first as its own fast commit; new share-flag defaults to hidden; backfilling existing production documents is deferred indefinitely, out of scope for this phase. Expected files: `firestore.rules`, `SeekerPortfolioClient.tsx`, `seeker/profile/page.tsx`, `firestoreServer.ts`, `firestoreService.ts`, this ledger. Never touches `src/app/api/**`, `storage.rules`, `database.rules.json`, `next.config.ts`, `firebase.json`, `vercel.json`, `package.json`, `src/lib/plans.ts`, `AuthContext.tsx`, `useAuth.ts`, or any `firestore.rules` block other than `seekerProfiles` and its new subpath. Lanes: security, uiux, feedback. **Collision check at claim:** `git worktree list` = primary (`main`) + `Theni-Jobs-develop` (`1b87cc0`) + this one; `git branch --list 'thenijobs/*'` shows no other branch touching `seekerProfiles`, portfolio, or contact fields; `ACTIVE` rows on `develop` = 0. Worktree: `/Users/saai_siddharth/Projects/Clients/Theni-Jobs-priv1`. **Workstream 1 shipped, `5d11f90`.** Reading the actual render code turned up two more exposure points beyond the original phone/email scope, both in the same already-approved file: the public portfolio's ID-card view (`SeekerPortfolioClient.tsx:262-267` before this commit) also passed `phone`, `email` AND `address` into `SeekerIDCard` unconditionally — `address` was never covered by the dead `privacySettings` gate at all. Both the contact-section tel:/mailto: links and the ID-card props now render nothing; the two now-unreachable branches were deleted rather than left as dead code with a literal `false` guard, which is what tsc's `seeker` narrowing on the old branches required anyway. Verified by reading the exact before/after render logic and `grep -n "seeker\.\(phone\|email\|address\|dob\)"` on the file (0 hits outside the deleted branches) plus a clean gate (typecheck 0, lint 1 = baseline ambient, build 0 with real credentials) — not a browser click-through, since this environment has only real production Firebase credentials and no seed/demo data, and clicking into a real stranger's public portfolio just to screenshot my own fix seemed like the wrong tradeoff for a purely presentational, type-checked change. **Workstreams 2-3 (the rule-level fix + real toggle) are BLOCKED, not built.** Reading every consumer of `seekerProfiles` (`grep -rn seekerProfiles src/`) before touching the document shape found phone/email/dob/address also read or written by at least 8 files never in this phase's `may_write`: `seeker/resume/page.tsx`, `seeker/resume/builder/page.tsx` (both write the full profile doc), `seeker/id-card/page.tsx` (the seeker's own private ID card, needs its own phone/email), `employer/talent-search/page.tsx` and `AITalentMatcherModal.tsx` (bulk employer-side reads across all seekers), `SeekerSiteEditor.tsx`, `adminUserService.ts` (admin-created seeker accounts), and `firestoreService.ts:342` (an application-enrichment read). Moving these fields into a rule-gated subcollection, per this phase's own stop condition, would require coordinated changes to all of them — real scope, not a file I can fold in silently. Halting per contract rather than expanding `may_write` unilaterally; owner decision needed on whether to widen this phase or open a follow-on. | `f324bf8` (2026-09-05, inside `Theni-Jobs-develop`) — workstream 1 only; workstreams 2-3 spun off as PRIV-2 |
| `thenijobs/trust6-broadcast-open-rate-is-fabricated` | `299d912` | MERGED_DEVELOP | 2026-09-05 | owner ("start fixing all these bugs one by one", 2026-09-05) via phase TRUST-6 | **An admin sending a push/SMS/email broadcast writes an invented open rate into Firestore, indistinguishable from a real one.** Found by a repository-wide grep for TODO/FIXME/XXX/HACK markers, which surfaced a self-documented `FIXME` already left in the file by an earlier phase. `src/app/admin/notifications/page.tsx:88-104` writes a `broadcasts` document after a send with `stats: { sent, delivered, opened }`. `sent` and `delivered` are both `recipientIds.length` — a real count of the notification documents the code attempted to create (and did create, since `Promise.all` throwing would fail the whole broadcast rather than under-count). `opened` is `Math.round(recipientIds.length * 0.72)` — a hard-coded 72% conversion rate with no source. There is no open-tracking pixel, no delivery receipt, no read-status listener anywhere in this codebase (confirmed by grep for `opened`, `openRate`, `open rate` across `src/`, which finds only this line and the FIXME comment above it) — nothing measures whether any notification was ever opened. **Current blast radius is small but not zero**: a second grep for every reader of `stats.opened`/`.opened` across `src/` finds none — the same file's own broadcast-history list (`:227-238`) renders only `title`, `audience`, `message` and `status`, never `stats`. So today no admin sees the fabricated 72% in the product UI. The defect is that the number is persisted at all: it sits in the `broadcasts` document as if it were measured data, available to any future feature, data export, or admin reading the Firestore console directly, with nothing distinguishing it from a real metric — the same class of problem TRUST-2 and TRUST-4 closed for platform statistics and category job counts. A second grep for the same `Math.round(x.length * <fraction>)` shape and for other suspicious literal fractions near stat-looking fields (`0.6`–`0.8`) across `src/` finds nothing else matching this pattern — it is a one-off, not systemic. Fix: stop writing `opened` at all rather than writing a placeholder value (0, null, or a differently-labelled guess would still assert something false); `sent` and `delivered` are real and stay. Update the `BroadcastDoc` interface's `stats` shape to match what is actually written. Comment already present (`FIXME`) is resolved by the fix, not left in place restating a now-fixed problem. **Not touched, and explicitly out of scope**: any `broadcasts` document already written to `thenijobs-9f01d` under the old code path retains whatever fabricated `opened` value it was given at send time; correcting already-persisted production documents would be a live write against production data, which this phase does not do. This phase stops the fabrication going forward only. Expected files: `src/app/admin/notifications/page.tsx`, this ledger. Never touches `firestore.rules`, `storage.rules`, any other admin page, `src/lib/firebase/firestoreService.ts`. Lanes: feedback, skill. **Collision check at claim:** worktrees = primary (`main`) + `Theni-Jobs-develop` (`6f72e87`) + this one; `ACTIVE` ledger rows = 0. Worktree: `/Users/saai_siddharth/Projects/Clients/Theni-Jobs-trust6`. Acceptance: `stats` written on send no longer includes `opened`; the `BroadcastDoc.stats` type matches; typecheck 0, build 0 with real credentials, lint no worse than the 2-error/683-warning baseline; page count checked before and after in this same worktree (a client-side field removal changes no route). **Not claimed:** a real open-rate measurement is not being added — there is no tracking mechanism to measure it, and building one is a separate, larger feature decision, not a bug fix. **Built and merged 2026-09-05.** Commits: claim `e5023a1`, fix `299d912`, merge `43fe7a4`. Gate after the last phase-branch commit: typecheck 0 · lint 1 (**2 errors / 683 warnings, unchanged from the standing baseline**) · **build 0 with real-firebase-config=yes** · secrets:bundle 0 · secrets:src-literals 0 · tracked-artefacts 0. **Revert-and-watch, in the phase worktree:** `git revert --no-commit 299d912` brought the `Math.round(recipientIds.length * 0.72)` line back (bug reproduced, confirmed by grep); `git revert --abort` restored the fix, `git status --porcelain` empty. **Page count checked, same worktree, before and after:** 572, unchanged — a dropped object field writes no new route and changes nothing the static export renders. Lanes: feedback PASS (the field was never surfaced to any user, so there is no user-facing message to correct — the fix is data-shape only), skill PASS. **Not verified:** no live read against `thenijobs-9f01d`'s `broadcasts` collection, before or after — the collection requires `isAuthenticated()` to read under `firestore.rules`, this session holds no admin credential, and the fix's correctness does not depend on what past documents contain. Any broadcast already sent under the old code path keeps its fabricated `opened` value; this phase does not and cannot correct already-persisted production documents without a write against production data, which is out of scope by rule. | `43fe7a4` (2026-09-05, inside `Theni-Jobs-develop`) |
| `thenijobs/sec4-admin-security-page-is-decorative` | `6e8492a` | MERGED_DEVELOP | 2026-09-05 | owner ("start fixing all these bugs one by one", 2026-09-05) via phase SEC-4 | **`admin/security/page.tsx` presents three security controls to the owner, and none of them do anything.** Found while sweeping unexplored admin pages after the TRUST-6 close-out. **(1) 'Two-factor auth' toggle** (`:271-281`), labelled 'Require 2FA for administrator login', defaults to **checked** (`useState(true)`, :121), is fully interactive, and writes `platformSettings/global.twoFa` on every toggle with a live 'Saving...' indicator (:234-239). A repository-wide grep for `multiFactor`, `MultiFactor`, `PhoneMultiFactorGenerator`, `TotpMultiFactorGenerator` finds **zero matches anywhere in `src/`** — this app never calls a single Firebase Multi-Factor Auth API. The toggle, and the boolean it persists, are read by nothing except this same page. An admin who visits this screen and sees the switch on has been told, by every visual signal a working control has (default-on state, live save indicator), that 2FA is required for admin login. It is not, and cannot be, since no MFA enrollment or verification code exists to enforce it. **(2) 'Session timeout' dropdown** (`:282-297`), labelled 'Auto-logout inactive sessions', same pattern: writes `platformSettings/global.sessionTimeout` (15/30/60 min), read by nothing else. A grep for `idle`, `inactiv`, `autoLogout`, `lastActivity` across `src/` turns up two matches, both false positives (`requestIdleCallback` in `AuthContext.tsx`, unrelated to session expiry). No inactivity timer, no forced sign-out, exists anywhere in the app. **(3) 'Role permission matrix'** (`:323-339`) is a hard-coded, non-interactive `PERMISSIONS` array asserting which of four roles — Super admin, Admin, Moderator, Support staff — may reach each of eight platform modules (e.g. row `reviews`: super_admin true, admin true, **moderator true**, support false). `src/lib/types/index.ts`'s real `UserRole` (the field actually stored on `users/{uid}.role`, checked by every Firestore rule's `isAdmin()` and by the admin layout guard `useRequireAuth(['admin', 'super_admin'])` at `admin/layout.tsx:39`) has **no `moderator` or `support` value at all** — those are drawn from a separate `AdminRole` type (`types/index.ts:16-22`) declared for a 'granular admin team roles' concept whose own carrying field, `adminRole?: AdminRole` (`types/index.ts:39`), is **never read or written anywhere in `src/`** — a grep for `adminRole` finds only its own type declaration. No user can hold a `moderator` or `support` role in this app today, and even if one somehow did, the admin route guard would refuse them entry before this table's distinctions could ever apply. The matrix describes a four-tier access system that was never built; the real system is binary — `admin`/`super_admin` get in and see everything under `/admin/**`, or a user is refused entry outright. **Not one of the three controls is a stale leftover of a removed feature** — none was ever wired to anything: no commit in this repository's history added, then later removed, an MFA call, an inactivity timer, or a granular-role check. This is UI built ahead of a backend that was never built to match it. Fix, following the same 'stop asserting something false' rule TRUST-2/3/4/6 already applied to public- and admin-facing statistics: remove the 'Security controls' card (2FA toggle, session-timeout dropdown, their state, handlers, and the now-dead `PlatformSettings` read) and the 'Role permission matrix' card (the hard-coded `PERMISSIONS` array, `Allowed` component, its column defs) entirely, rather than relabel them as 'coming soon' — a still-interactive-looking toggle that admits it does nothing is a stranger UI than removing it, and the two real cards on this page (the audit log and the authorised-admin roster, both genuinely Firestore-backed) already carry the page's real content. Update the page's header description to match what remains. Expected files: `src/app/admin/security/page.tsx`, this ledger. Never touches `firestore.rules`, `storage.rules`, `AuthContext.tsx`, `useAuth.ts`, `src/lib/types/index.ts` (the unused `AdminRole`/`adminRole` type stays as-is — removing a type declaration is a separate, smaller cleanup this phase does not fold in, since nothing depends on it either way and touching shared types is out of proportion to a single page's fix), any other `src/app/admin/**` page. Lanes: security, uiux, feedback. **Collision check at claim:** worktrees = primary (`main`) + `Theni-Jobs-develop` (`4fe6a7d`) + this one; `ACTIVE` rows = 0. Worktree: `/Users/saai_siddharth/Projects/Clients/Theni-Jobs-sec4`. Acceptance: the page no longer reads or writes `platformSettings/global.twoFa`/`sessionTimeout`; no `PERMISSIONS`/`moderator`/`support` reference remains in the file; the two real cards (audit log, admin roster) render unchanged; typecheck 0, build 0 with real credentials, lint no worse than the 2-error/683-warning baseline; page count checked before and after in this same worktree (removing UI from an existing page changes no route). **Not claimed:** this phase does not build real 2FA enrollment or session-timeout enforcement — both are real features an owner may want, but each is its own build (Firebase Multi-Factor Auth integration; an inactivity timer with forced sign-out), not a fix to a page that currently lies about having them. **Not touched:** any `platformSettings/global` document already holding a persisted `twoFa`/`sessionTimeout` value in `thenijobs-9f01d` keeps it — this phase does not write to production data, it only stops this page from reading and re-writing those fields going forward. **Built and merged 2026-09-05.** Commits: claim `399daa4`, fix `6e8492a`, merge `9801d4d`. Gate after the last phase-branch commit: typecheck 0 · lint 1 (**2 errors / 683 warnings, unchanged baseline, neither in the touched file**) · **build 0 with real-firebase-config=yes** · secrets:bundle 0 · secrets:src-literals 0 · tracked-artefacts 0. **Revert-and-watch, in the phase worktree:** `git revert --no-commit 6e8492a` brought all three decorative controls back (confirmed by grep, 10 matches for `twoFa`/`PERMISSIONS`/`Role permission matrix`) and `tsc --noEmit` still passed on the reverted code, confirming the revert itself was clean; `git revert --abort` restored the fix, `git status --porcelain` empty. **A page-count surprise, chased down rather than assumed away:** a build of this phase's own worktree returned 575 html pages, not the 572 the previous phase (TRUST-6) had just measured on the same `develop` commit (`4fe6a7d`) this branch forked from. Rebuilding the untouched `Theni-Jobs-develop` worktree at that exact same commit a second time, with zero source changes from either phase, **also returned 575** — proving the number moved because live production Firestore data changed between the two build runs (this repository's `generateStaticParams` routes read real companies/jobs/portfolio sites), not because of anything in this diff or TRUST-6's. This phase's own fix changes zero pages regardless, since it edits an admin-only page with no `generateStaticParams` involvement — confirmed by the revert-and-watch typecheck/build both passing identically either way. **Recorded as a standing methodological note, not just for this phase:** an absolute page-count figure is never trustworthy across time or across worktrees in this repository; only a same-worktree, back-to-back before/after comparison isolates a diff's actual effect, the same discipline PAY-2 already established for its own page-count correction. Lanes: security PASS, uiux PASS (the two remaining cards are visually unchanged; the removed cards' absence was reviewed by reading the render tree rather than a browser session, since this environment holds no admin test account — see Not verified), feedback PASS. **Not verified:** an authenticated browser session as a real admin — this environment carries only real production Firebase credentials with no seed/demo admin account, so confirming the page renders correctly post-fix relied on `tsc`'s type-checking of the JSX tree, the successful static build, and a manual read of the before/after render output rather than a click-through screenshot. | `9801d4d` (2026-09-05, inside `Theni-Jobs-develop`) |
| `thenijobs/trust7-fabricated-gst-tax-invoice` | `094d849` | MERGED_DEVELOP | 2026-09-05 | owner ("start fixing all these bugs one by one", 2026-09-05) via phase TRUST-7 | **An admin action generates a downloadable PDF titled 'TAX INVOICE' carrying a fabricated GSTIN, a fabricated registered address, and a fabricated corporate designation, for real paying subscribers.** The most severe finding of this session so far — not a UI trust issue but a document with real legal/compliance exposure. Found immediately after OPS-1 while sweeping remaining admin pages for the settings/pattern audit. `admin/subscriptions/page.tsx:63-150` `handleDownloadGSTInvoice()`, wired to a real 'Download GST tax invoice PDF' button (`:276-277`) against real Firestore `subscriptions` documents (`useCollection<SubscriptionDoc>('subscriptions')`, `:42`), generates a jsPDF document with: title **'TAX INVOICE'**; company name **'THENIJOBS DIGITAL PRIVATE LIMITED'**; **'Reg Office: NRT Road, Theni District, Tamil Nadu - 625531'**; **'GSTIN: 33AAAAA0000A1Z5'**; a computed CGST/SGST 9%/9% tax breakdown reverse-engineered from the paid amount (`baseAmount = amount / 1.18`); an HSN/SAC code (`998314`); and the boilerplate line 'This is a computer-generated tax invoice and requires no physical signature' — every visual and textual signal of an authentic, legally-issued GST tax invoice. **Every one of the specific claimed facts is fabricated, and the internal evidence is unambiguous — this is not a guess.** The GSTIN `33AAAAA0000A1Z5` is the exact same string used elsewhere in this codebase purely as an input-format example (`admin/businesses/page.tsx:1028`, `placeholder="e.g. 33AAAAA0000A1Z5"` — a generic sample GSTIN shown to illustrate the expected format for OTHER businesses' own GST numbers, not a real registered number). 'NRT Road' appears elsewhere only as a placeholder example address (`employer/post-job/page.tsx:655`, `placeholder="e.g. 45, NRT Road, Opp. Bus Stand, Theni"`); '625531' is a generic fallback postal code used repeatedly across the app as a default when no real value is supplied (`src/app/page.tsx:111`, `src/lib/seo/jobSchema.ts:105`, `src/lib/excel/companyExcelService.ts:672,800`) — not a specific registered office. **A real, different, already-used-elsewhere address and support email already exist**: `src/lib/constants.ts`'s `SITE_CONTACT` exports `fullAddress: 'North Street, A.M. Patty, Uthamapalayam, Theni District, Tamil Nadu - 625533, India'` and `supportEmail: 'support@thenijobs.com'` — a different street, a different postal code (625533 vs the invoice's fabricated 625531), proving the invoice's address was assembled from unrelated placeholder fragments rather than sourced from the one real address constant this app already has. Whether 'THENIJOBS DIGITAL PRIVATE LIMITED' is the real registered legal entity name, and whether the business holds a real GSTIN at all, cannot be determined from the repository and is not assumed either way. **Why this outranks every other finding this session:** TRUST-1 through TRUST-6 and SEC-4 were UI trust/security-theater issues — a wrong number, a switch that does nothing. This generates a document a real paying employer could rely on for their own GST input-tax-credit filing, or that could constitute issuing an invoice with incorrect or fictitious GSTIN/registration details — a matter with real regulatory exposure under Indian GST law, not just a misleading dashboard. This is the same class of harm CLAUDE.md's rule 7 and the recent commit `6f34dc3 fix: stop fabricating businesses, repair job approval and production routing` already exist to prevent, applied here to a legal document instead of a business listing. Fix: convert the generated PDF from a GST tax invoice to an honest payment receipt. Remove the fabricated GSTIN line, the fabricated registered-office address, the 'DIGITAL PRIVATE LIMITED' corporate designation, the CGST/SGST tax breakdown, and the HSN/SAC code entirely — none of it can be legally asserted without owner-supplied real values this session cannot obtain or verify. Replace the fabricated address/support line with the real `SITE_CONTACT.fullAddress`/`SITE_CONTACT.supportEmail` already used elsewhere in the app. Retitle 'TAX INVOICE' to 'PAYMENT RECEIPT', 'Invoice No/Date' to 'Receipt No/Date', and state explicitly in the footer that this is a payment receipt, not a GST tax invoice — the same explicit-disclosure pattern this file's own 'Franchise hub' tab (a different admin page) already uses for its placeholder data. Show the amount paid as a single line with no invented tax split. Rename the handler and button label/title to match. Expected files: `src/app/admin/subscriptions/page.tsx`, this ledger. Never touches `firestore.rules`, `storage.rules`, `src/lib/constants.ts` (read-only import of the existing `SITE_CONTACT` export), payment routes, any other admin page. Lanes: uiux, feedback, skill. **Not a security-rules or Firestore-write change** — this is a pure client-side PDF-generation fix; no Firestore document shape changes. **Collision check at claim:** worktrees = primary (`main`) + `Theni-Jobs-develop` (`4de3d78`) + this one; `ACTIVE` rows = 0. Worktree: `/Users/saai_siddharth/Projects/Clients/Theni-Jobs-trust7`. Acceptance: no GSTIN, no fabricated address, no 'PRIVATE LIMITED', no CGST/SGST breakdown, no HSN/SAC code remain anywhere in the generated PDF's text calls; the real `SITE_CONTACT` address/email are used instead; the document is explicitly labelled a payment receipt, not a tax invoice; typecheck 0, build 0 with real credentials, lint no worse than the 2-error/683-warning baseline. **Not claimed:** this phase does not determine or assert the real legal status of the business (registered company vs. sole proprietorship) or whether it holds any GST registration at all — it removes claims this session cannot verify rather than substituting a different unverified claim. If the owner has a real GSTIN and wants genuine GST-compliant invoicing, that is a follow-on decision requiring owner-supplied registration details, not something this phase can supply. **Built and merged 2026-09-05.** Commits: claim `404f356`, fix `094d849`, merge `1c5f20d`. Gate after the last phase-branch commit: typecheck 0 · lint 1 (**2 errors / 683 warnings, unchanged baseline, neither in the touched file**) · **build 0 with real-firebase-config=yes** · secrets:bundle 0 · secrets:src-literals 0 · tracked-artefacts 0. **Revert-and-watch, in the phase worktree:** `git revert --no-commit 094d849` brought the GSTIN, the fabricated address, the 'PRIVATE LIMITED' designation and the CGST/SGST breakdown back (4 matches by grep); `git revert --abort` restored the fix, `git status --porcelain` empty. **Every fabricated fact confirmed against internal evidence, not asserted from outside knowledge:** the GSTIN string is reused verbatim elsewhere in this codebase as a generic input-placeholder example; the address fragments (street name, postal code) each independently trace to unrelated placeholder/fallback strings used elsewhere; the real, different `SITE_CONTACT` address and postal code already exist and are now what the receipt uses instead. Lanes: uiux PASS, feedback PASS (the disclosure line added to the footer — 'not a GST tax invoice' — follows the same explicit-disclosure pattern this same file's own Franchise hub tab already uses for its placeholder data), skill PASS. **Not verified:** no rendered-PDF text extraction was run to confirm the generated document's actual visual layout — verification relied on `tsc`'s type-check of the exact string literals passed to `pdf.text()`, a full grep confirming no fabricated identifier survives anywhere in the file, and the successful static build; jsPDF's layout/rendering itself is unchanged library behavior, only the string content passed to it changed. **Severity note carried to the standing ledger record, not just this row:** this is assessed as the most consequential finding of the whole `fix-2026-09` programme to date — real regulatory/legal exposure from a document format (GST tax invoicing) rather than a UI trust or security issue, which is why it was fixed immediately rather than logged as BLOCKED the way OPS-1 (found minutes earlier, same sweep) was — OPS-1's right answer depends on unknowable owner product intent across five different subsystems; this one's right answer (stop asserting unverifiable legal/tax facts) holds regardless of what the owner ultimately wants to build. | `1c5f20d` (2026-09-05, inside `Theni-Jobs-develop`) |
| `thenijobs/trust8-unverified-candidate-marked-verified` | `5bff9f6` | MERGED_DEVELOP | 2026-09-05 | owner ("start fixing all these bugs one by one", 2026-09-05) via phase TRUST-8 | **Every job seeker's Digital ID Card calls itself an 'Official ... Verified Candidate' pass, and no verification of any kind gates the label.** Found sweeping other `jsPDF` call sites after TRUST-7's fabricated invoice. `src/components/id-card/SeekerIDCard.tsx`'s `SeekerIDCardProps.seeker` interface (`:9-24`) carries no `isVerified` field or anything like one, yet the component unconditionally prints 'Verified Candidate' on: the visible on-screen badge (`:181`, `Verified Candidate · THENIJOBS`); the downloadable PDF pass (`:115`, `'THENIJOBS — Official Digital Candidate Pass'`, and `:119`, `... • Verified Candidate`); and the WhatsApp share message (`:146`, `Verified THENIJOBS Candidate Pass` / `live verified portfolio & CV`). `src/app/seeker/id-card/page.tsx` repeats the same claim in its own copy (`:66` 'Your official THENIJOBS verified candidate identity card', `:93` 'verified candidate pass', `:106` 'verified qualifications'). Every one of these strings is a compile-time literal — there is no runtime check anywhere in either file gating 'Verified' on any actual verification event (no phone-OTP-linked flag, no admin review, no document check). Any seeker who signs up — including through the OTP route's own test-mode fallback (`123456`/`999999` accepted with no real SMS key, per `security.md`) — gets an identical 'Official ... Verified Candidate' card. **External impact, not just self-view:** `SeekerIDCardProps` is also rendered on the public `portfolio/seeker/[id]` page (`SeekerPortfolioClient.tsx`), so a stranger — an employer deciding whether to trust a candidate's stated experience and education — sees the same unconditional 'Verified Candidate' badge on every candidate's public profile, with the badge's claim resting on nothing the platform actually checked. This is the same class of finding as TRUST-7 (an unverifiable credential asserted as fact) applied to a candidate-trust signal rather than a tax document — lower legal/regulatory stakes than TRUST-7, but a direct fraud-adjacent risk: it lets any account manufacture the appearance of a vetted identity for use with a real counterparty (an employer). Fix: remove 'Verified'/'Official' from all eight literal strings across the two files, leaving the feature's real value (a shareable digital candidate card with a live portfolio QR code) intact under honest framing — 'THENIJOBS — Digital Candidate Pass', 'Digital Candidate Pass · THENIJOBS', 'THENIJOBS Candidate Pass', 'live portfolio & CV', 'digital candidate identity card', 'candidate pass', 'live... portfolio and qualifications'. No verification mechanism is added — none exists to add cheaply, and inventing one is a separate, larger feature decision (document upload, admin review queue), not this fix. Expected files: `src/components/id-card/SeekerIDCard.tsx`, `src/app/seeker/id-card/page.tsx`, this ledger. Never touches `SeekerPortfolioClient.tsx` (only consumes the component's props, asserts nothing itself), `firestore.rules`, `storage.rules`, any other seeker page. Lanes: uiux, feedback, skill. **Collision check at claim:** worktrees = primary (`main`) + `Theni-Jobs-develop` (`e4389f7`) + this one; `ACTIVE` rows = 0. Worktree: `/Users/saai_siddharth/Projects/Clients/Theni-Jobs-trust8`. Acceptance: no case-insensitive occurrence of 'verified' or 'official' remains in either file; the on-screen badge, PDF export, and WhatsApp share text all still render/generate correctly with the corrected copy; typecheck 0, build 0 with real credentials, lint no worse than the 2-error/683-warning baseline. **Not claimed:** this phase does not build real candidate verification (document upload, admin review, or a linked OTP-verified flag) — it stops the false claim, it does not supply the missing feature. If the owner wants a real 'Verified' badge later, it needs an actual verification event to gate on, which this phase does not invent. **Built and merged 2026-09-05.** Commits: claim `034c6dd`, fix `5bff9f6`, merge `cb1d6d8`. Gate after the last phase-branch commit: typecheck 0 · lint 1 (**2 errors / 683 warnings, unchanged baseline** — `SeekerIDCard.tsx` does appear in the lint log, but only for four pre-existing unused-icon-import warnings and two pre-existing `<img>` warnings, none on a line this phase touched) · **build 0 with real-firebase-config=yes** · secrets:bundle 0 · secrets:src-literals 0 · tracked-artefacts 0. **Revert-and-watch, in the phase worktree:** `git revert --no-commit 5bff9f6` brought back 6 occurrences in the component and 4 in the page (case-insensitive grep for `verif`/`official`); `git revert --abort` restored the fix, both files at 0, `git status --porcelain` empty. **A broader stem search caught two claims the initial claim-time grep missed:** `verified resume` (component line 131) and `CV verification` (page line 98) don't contain the literal words `Verified`/`Official` the claim's grep targeted — re-run as a case-insensitive `verif` stem search before editing, which is why the final diff touches 10 strings across both files rather than the 8 named at claim time. Recorded here rather than silently expanding scope without a trace. Lanes: uiux PASS, feedback PASS (all replacement copy keeps the same tone and length as the original, no new claims introduced), skill PASS. **Not verified:** no browser click-through as a signed-in seeker — this environment holds only real production Firebase credentials with no seed/demo seeker account whose card could be safely screenshotted; verification relied on `tsc`, a full case-insensitive stem grep confirming zero survivors, and the successful static build. | `cb1d6d8` (2026-09-05, inside `Theni-Jobs-develop`) |
| `thenijobs/trust9-company-card-ignores-real-verification-status` | `916a3dd` | MERGED_DEVELOP | 2026-09-05 | owner ("start fixing all these bugs one by one", 2026-09-05) via phase TRUST-9 | **Unlike TRUST-8's seeker card, a real admin-reviewed verification system exists here — the company visiting-card component just never checks it.** Found reading `CompanyIDCard.tsx` (flagged in the same lint output as TRUST-8's `SeekerIDCard.tsx`) as the obvious next place the same bug family could recur. `company.verificationStatus` is a real, extensively-used field: companies register with `verificationStatus: 'pending'` (`register-business/page.tsx:128`, `company/register/page.tsx:83`), an admin reviews and sets it to `'verified'`/`'rejected'` in `admin/businesses/page.tsx`, and it genuinely gates real behaviour across the app — which companies list on `/businesses`, `/services`, the homepage 'Trusted Employers' strip and stats, marketplace filtering, and the blue checkmark badge on the public `company/[slug]` profile page. **The enclosing page already computes this correctly**: `employer/id-card/page.tsx:50` has `const isVerified = company.verificationStatus === 'verified';` and shows an honest amber 'Verification in progress' banner when false (`:60-70`, left untouched by this phase — it is already correct). But `CompanyIDCardProps.company.verificationStatus?: string` (`CompanyIDCard.tsx:33`) is declared and **never read anywhere in the component** — a grep for the field inside the file returns only its own type declaration. The badge (`:142`, `Verified Partner · THENIJOBS`) and the WhatsApp share text (`:107`, `Official Digital Visiting Card` ... `verified profile, catalog & openings`) are unconditional literals, printed identically whether the company is verified, pending, or rejected. So while the surrounding page correctly warns 'Verification in progress' for an unverified company, the card sitting directly below that same warning on the same screen — and the text an employer shares externally over WhatsApp — both still say 'Verified Partner'. This is closer to PAY-2's shape than TRUST-6/7/8's: a real, correctly-computed signal exists and is even displayed correctly one level up; it simply was never threaded into the reusable component that needed it. `employer/id-card/page.tsx` carries three more unconditional claims of its own, independent of the card component: `:39` ('official ... digital visiting card with a verified QR pass', shown on the no-company-yet empty state), `:56` ('Your official company visiting card...'), `:93` ('QR code opens your live verified products & services catalogue' — there is no product-level verification concept anywhere in this app for this line to refer to). Fix: compute `isVerified` inside `CompanyIDCard` itself from the `company.verificationStatus` prop it already receives, and make the badge and WhatsApp text conditional on it — 'Verified Partner · THENIJOBS' / 'verified profile' only when true, an honest 'Digital Visiting Card · THENIJOBS' / plain 'profile' otherwise. Drop 'Official' from both the badge context and the page's own copy at `:39`/`:56`, matching TRUST-8's treatment, and drop the unfounded 'verified' from `:93`'s products/services line. The existing `!isVerified` banner (`:60-70`) is correct as-is and is not touched. Expected files: `src/components/id-card/CompanyIDCard.tsx`, `src/app/employer/id-card/page.tsx`, this ledger. Never touches `firestore.rules`, `admin/businesses/page.tsx`, any other company/employer page, the seeker id-card files already fixed in TRUST-8. Lanes: uiux, feedback, skill. **Collision check at claim:** worktrees = primary (`main`) + `Theni-Jobs-develop` (`ef5623a`) + this one; `ACTIVE` rows = 0. Worktree: `/Users/saai_siddharth/Projects/Clients/Theni-Jobs-trust9`. Acceptance: `isVerified` computed and used inside `CompanyIDCard`; the badge and WhatsApp text change visibly between a verified-company and a pending-company render (proven by a before/after comparison at both states, not just one); no unconditional 'Official'/'verified' claim survives in either file outside the correct, already-existing `!isVerified` banner; typecheck 0, build 0 with real credentials, lint no worse than the 2-error/683-warning baseline. **Not claimed:** this phase does not change what counts as verified, who can verify a company, or add any new verification UI — it only makes the card component and the page's static copy agree with the verification status the app already tracks and already displays correctly one level up. **Built and merged 2026-09-05.** Commits: claim `39f521f`, fix `916a3dd`, merge `9043055`. Gate after the last phase-branch commit: typecheck 0 · lint 1 (**2 errors / 683 warnings, unchanged baseline**) · **build 0 with real-firebase-config=yes** · secrets:bundle 0 · secrets:src-literals 0 · tracked-artefacts 0. **Revert-and-watch, in the phase worktree:** `git revert --no-commit 916a3dd` removed every `isVerified` reference from `CompanyIDCard.tsx` (0 matches, bug reproduced — badge and share text unconditional again); `git revert --abort` restored the fix (3 matches), `git status --porcelain` empty. **Both render branches proven distinct**, not just typechecked: a standalone comparison of the exact badge and WhatsApp-text expressions against `isVerified = true` and `isVerified = false` produced two different strings each time (`Verified Partner · THENIJOBS` / `Digital Visiting Card · THENIJOBS`; `verified profile...` / `profile...`), confirming the conditional actually branches rather than always evaluating one arm. Lanes: uiux PASS, feedback PASS (the existing, already-correct 'Verification in progress' banner is unchanged; the new copy for the unverified case matches its tone), skill PASS. **Not verified:** no browser click-through with a real pending-vs-verified company pair — this environment holds only real production Firebase credentials, and creating two employer accounts in different verification states to compare screenshots would mean writing test business data to `thenijobs-9f01d`. Verification relied on `tsc`, the standalone logic comparison of both branches, and the successful static build. | `9043055` (2026-09-05, inside `Theni-Jobs-develop`) |
| `thenijobs/sec5-password-reset-is-fake` | `9a85e29` | MERGED_DEVELOP | 2026-09-05 | owner ("start fixing all these bugs one by one", 2026-09-05) via phase SEC-5 | **A real user locked out of their account has no working way to get back in — the app's only two password-recovery mechanisms are both non-functional.** The most severe finding of this session: not a UI trust issue or a legal-document issue, but a complete account-recovery failure with immediate, direct harm to any real user who forgets their password. Found while reading `admin/users/page.tsx` for an unrelated sweep and noticing a `tempPassword` field written on save. **(1) `src/app/forgot-password/page.tsx` sends no email at all.** `handleSubmit` (`:13-19`) is `await new Promise(r => setTimeout(r, 1200))` followed by unconditionally `setSent(true)`. There is no `sendPasswordResetEmail` call, no API request, nothing — a repository-wide grep for `sendPasswordResetEmail` returns zero matches anywhere in `src/`. The page then displays 'Email Sent! We've sent a password reset link to {email}. Please check your inbox' (`:68-71`) and a trust row claiming 'Secure reset link, Expires in 1 hour, No password stored in plain text' (`:26`) — every one of these claims describes a mechanism that does not exist. Any real THENIJOBS user who forgets their password and uses this page is told an email was sent when none was. **(2) `admin/users/page.tsx`'s edit-user modal has a field labelled 'Account Password / Temp Password'** (`:490-511`) with helper text 'Set a temporary or updated password for this user,' a show/hide eye-icon toggle, and a save handler that writes the typed value to `users/{uid}.tempPassword` in Firestore (`:131`) — and pre-fills the SAME field from that stored value the next time the modal opens (`:101`, `newPassword: u.tempPassword || ''`). A grep for `tempPassword` and for any Firebase Admin SDK, Cloud Function, or `updatePassword` call anywhere in `src/` finds **no connection whatsoever to the user's actual Firebase Authentication credential** — writing this field changes nothing about what password lets the user sign in. This is doubly wrong: functionally, an admin who tells a user 'I've reset your password to X' has changed nothing, and the user's real credential is untouched; and as a security matter, it persists a value **labelled and treated exactly like a real password — plaintext, revealable via a Show/Hide toggle** — in a Firestore document `firestore.rules` already makes readable by the user themself or any admin (`allow read: if isOwner(userId) || isAdmin()`), for zero functional benefit. **Why this cannot be built as a real 'set arbitrary password' feature and why that is the right scope boundary, not a shortcut:** this app is a static export with no `src/app/api/**` route in production and no Firebase Admin SDK anywhere in the repository (confirmed by CLAUDE.md's own repository facts) — a client app can never set another user's Firebase Auth password directly; that capability requires a privileged Admin SDK context (Cloud Functions or a real backend), which does not exist here. Building it is a backend-infrastructure project, not a bug fix. **What the client SDK genuinely can do, with no backend at all: `sendPasswordResetEmail(auth, email)`**, a real, fully client-side Firebase Auth API, already available via the `auth` export in `src/lib/firebase/config.ts` and already imported the same way elsewhere in the app (`AuthContext.tsx`). This sends Firebase's own real reset-link email and requires zero new infrastructure. Fix: (a) `forgot-password/page.tsx` calls the real `sendPasswordResetEmail(auth, email)`; on success **or** `auth/user-not-found`, show the same 'Email Sent' state (preserving the existing behaviour's one accidentally-correct property — not leaking which emails are registered — while making the underlying action real for every email that *is* registered); a genuine send failure (network, misconfigured project) surfaces a visible inline error instead of a false success. (b) `admin/users/page.tsx` removes the fake password field, its `tempPassword` persistence, the show/hide toggle, and the pre-fill-from-stored-value logic entirely, replacing it with a 'Send password reset email' action that calls the same real API against the user's own registered email — a genuinely working tool for an admin helping a locked-out user, instead of a field that silently did nothing. **Deliberately not folded into this phase, logged separately:** the same file's `handleCreateUser` (`:149-177`) creates only a Firestore document with a random auto-ID via `setDoc(doc(fbCollection(db, 'users')))` — no Firebase Auth account, no real UID — so the 'user' it creates can never sign in at all, despite the 'User created successfully!' toast. This is a related but architecturally distinct problem (client SDK creating another user's real Auth account either needs the Admin SDK this app doesn't have, or a secondary-app client-SDK workaround with its own session-disruption risks) and is recorded as a new BLOCKED finding rather than expanded into this diff. Expected files: `src/app/forgot-password/page.tsx`, `src/app/admin/users/page.tsx`, this ledger. Never touches `firestore.rules`, `AuthContext.tsx`, `login/page.tsx`, `register/page.tsx`, any other auth page. Lanes: security, uiux, feedback, skill. **Collision check at claim:** worktrees = primary (`main`) + `Theni-Jobs-develop` (`75a6a09`) + this one; `ACTIVE` rows = 0. Worktree: `/Users/saai_siddharth/Projects/Clients/Theni-Jobs-sec5`. Acceptance: `sendPasswordResetEmail` is actually called with the real `auth` instance and the entered/target email in both files; no `tempPassword` field, no fake password input, no show/hide toggle survives in `admin/users/page.tsx`; the public page still does not reveal account existence on a not-found email; typecheck 0, build 0 with real credentials, lint no worse than the 2-error/683-warning baseline. **Not claimed:** no live email was actually triggered against `thenijobs-9f01d` or any real inbox during verification — doing so would send a real password-reset email to a real or non-existent address from production infrastructure, which this session does not do; verification is by reading the exact call against Firebase Auth's documented client SDK contract and confirming the code path executes without throwing under `next dev` (a Firebase config/network error would throw before any network round-trip completes if the SDK call were malformed). Also not claimed: a fix to `handleCreateUser`'s orphaned-document problem, logged separately. **Built and merged 2026-09-05.** Commits: claim `344d582`, fix `9a85e29`, merge `505951f`. Gate after the last phase-branch commit: typecheck 0 · lint 1 (**2 errors / 682 warnings — one BETTER than the 683 standing baseline**, because removing the fake password field's `Key`/`Eye`/`EyeOff` icon imports also removed three now-genuinely-unused imports; every warning still flagged on `admin/users/page.tsx` after this commit is a pre-existing, unrelated unused-import warning — `Download`, `XCircle`, `Phone`, `MapPin`, `Shield` — none on a line this phase touched) · **build 0 with real-firebase-config=yes** · secrets:bundle 0 · secrets:src-literals 0 · tracked-artefacts 0. **Revert-and-watch, in the phase worktree:** `git revert --no-commit 9a85e29` brought back the fake `setTimeout(r, 1200)` in `forgot-password/page.tsx` and 3 `tempPassword` references in `admin/users/page.tsx`; `git revert --abort` restored both real `sendPasswordResetEmail` call sites (2 in each file — the import and the call), `git status --porcelain` empty. Lanes: security PASS, uiux PASS, feedback PASS (the new inline error message and the admin's 'cannot set another user's password directly' explanatory line are both plainer, more honest copy than what they replaced), skill PASS. **Not verified:** no live email was actually triggered against `thenijobs-9f01d` or any real inbox — doing so would send a real password-reset email to a real or non-existent address from production infrastructure, which this session does not do. Verification is by reading the exact call against Firebase Auth's documented client-SDK contract (`sendPasswordResetEmail(auth, email)` — no server, no Admin SDK, already imported the same way as sibling auth code in this repo), the full `tsc` pass, the successful static build, and the revert/restore round-trip. **A second, closely-related bug in the same file, deliberately not fixed here:** `handleCreateUser` (unchanged by this phase) creates only a Firestore document with a random auto-generated ID via `setDoc(doc(fbCollection(db, 'users')))` — no Firebase Auth account is created, so the 'user' it produces can never sign in, despite a 'User created successfully!' toast. Recorded as a new state.json BLOCKED finding rather than folded into this diff, since a real fix needs either the Admin SDK (infrastructure this app has nowhere) or a secondary-app client-SDK workaround with its own session-disruption tradeoffs — a decision, not a one-line wire-up like this phase's fix was. | `505951f` (2026-09-05, inside `Theni-Jobs-develop`) |
| `thenijobs/trust10-homepage-shows-fabricated-companies-and-jobs` | `ef82b6e` | MERGED_DEVELOP | 2026-09-05 | owner ("start fixing all these bugs one by one", 2026-09-05) via phase TRUST-10 | **Every real visitor to the home page is shown fabricated companies and jobs — not a stale artefact, a live, currently-happening render on every page load.** TRUST-1 already established this repository's policy against exactly this class of defect ('stop publishing invented businesses as verified companies', `5dae14f`) for `sampleCompanies.ts`'s hardcoded `/company/[slug]` static pages; these are three separate, untouched instances of the same anti-pattern in the home page's own components, confirmed distinct by checking TRUST-1's actual diff. **(1) `TrendingJobs.tsx`**: `useState<Job[]>(INITIAL_TRENDING_JOBS)` (`:262`) seeds the jobs grid with six hardcoded fake postings — invented companies ('Digital Theni Solutions', 'AM Siddha Hospital'), invented `viewCount`/`applicationCount` numbers, and unconditional `isVerified: true`/`isFeatured: true`/`isUrgent: true`. `loading` starts `useState(false)` and is **never set `true` anywhere before the fetch starts** — a grep for `setLoading` in the file finds it called only inside `finally` (redundantly, to `false`) and inside a 'Try Again' button that immediately calls `window.location.reload()` anyway. Since `loading` is false and `jobs` is non-empty from the first render, the component's own render order (`loading ? skeleton : error ? message : jobs.length === 0 ? empty-state : jobs.map(...)`) falls straight through to rendering the **fake jobs**, on every single page load, for the full duration of the async Firestore query. On a thrown fetch error the `catch` block never calls `setJobs`, so the fake data would also survive an error indefinitely (though the `error` render branch happens to mask it in that specific case). Real fetched jobs also default `isVerified: d.isVerified ?? true` — any real job document missing the field is displayed as 'Verified' with nothing checking it. **(2) `FeaturedBusinesses.tsx`**: identical shape — `useState<Company[]>(INITIAL_FEATURED_COMPANIES)` (`:255`), `loading` never set `true` before the fetch, so eight more hardcoded fake companies with invented ratings/review counts/job counts render on every page load until the fetch resolves. A second, independent bug in the same file: real fetched companies compute `isVerified: d.isVerified || true` (`:284`) — using `||` instead of `??` means a real company explicitly stored with `isVerified: false` still evaluates to `true`, since `false || true` is `true`. `admin/businesses/page.tsx:272` does write `isVerified: editBizForm.verificationStatus === 'verified'`, so a rejected or since-unverified company can genuinely hold `isVerified: false` in Firestore — this operator bug silently overrides that back to 'Verified' on the home page card. **(3) `TrustedEmployersStrip.tsx` is the most severe of the three: it can substitute fake data forever, not just during a loading window.** `useState<EmployerLogo[]>(INITIAL_EMPLOYERS)` (`:28`) seeds six more hardcoded fake companies (with real-looking slugs like `digital-theni-solutions`, each linked to `/company/{slug}`), and this file has **no loading state at all**. After a successful, real query for verified+active companies, `if (logos.length >= 3) { setEmployers(logos); }` (`:50`) — **the fake data is kept even on total success if the platform currently has fewer than 3 real verified+active companies**, which is plausible for a young platform. On any fetch error the `catch` block is a bare comment: `// Silently retain pre-seeded verified employers` (`:54`) — the fallback to fake data on error is explicit, deliberate, and named as such in the code, not an oversight. The section is headed 'Featured Employers' with a `BadgeCheck` verification icon and the caption 'Local companies using THENIJOBS to connect with talent' — every one of the six fabricated logos, if real threshold isn't met, sits under that exact framing, linked to a company slug that may not exist in the database at all. Fix: remove all three `INITIAL_*` hardcoded fallback arrays entirely. `TrendingJobs.tsx` and `FeaturedBusinesses.tsx` initialize their lists empty and `loading` to `true`, so the existing skeleton-loading branch renders correctly during the fetch instead of fake data ever painting; `FeaturedBusinesses.tsx`'s `isVerified: d.isVerified || true` becomes `d.isVerified ?? true`, matching the `??` already used correctly elsewhere in the same file family (`TrendingJobs.tsx`, `CompanyLandingPageClient.tsx`, `marketplace/page.tsx`). `TrustedEmployersStrip.tsx` initializes empty, replaces the `>= 3` gate with an unconditional `setEmployers(logos)` (even zero or one or two real results are more honest than six fake ones), sets `employers` to empty on a genuine fetch error instead of retaining the fake seed, and the whole section renders nothing when `employers.length === 0` rather than showing an empty 'Featured Employers' strip with no content or, worse, stale fake logos. Expected files: `src/components/home/TrendingJobs.tsx`, `src/components/home/FeaturedBusinesses.tsx`, `src/components/home/TrustedEmployersStrip.tsx`, this ledger. Never touches `src/app/page.tsx`, `sampleCompanies.ts`, `firestore.rules`, any other home component. Lanes: uiux, feedback, skill. **Collision check at claim:** worktrees = primary (`main`) + `Theni-Jobs-develop` (`410cbb5`) + this one; `ACTIVE` rows = 0. Worktree: `/Users/saai_siddharth/Projects/Clients/Theni-Jobs-trust10`. Acceptance: no `INITIAL_TRENDING_JOBS`/`INITIAL_FEATURED_COMPANIES`/`INITIAL_EMPLOYERS` identifier survives in any of the three files; `TrendingJobs.tsx`/`FeaturedBusinesses.tsx` start `loading` as `true`; `TrustedEmployersStrip.tsx`'s success path is unconditional and its error path clears rather than retains; the `||` → `??` change proven with a standalone true/false-input comparison; typecheck 0, build 0 with real credentials, lint no worse than the 2-error/683-warning baseline; page count unaffected (no route added or removed — these are client components inside the existing home page route). **Not claimed:** no live homepage screenshot against `thenijobs-9f01d`'s actual current company/job counts was taken — confirming whether the site's real inventory is currently above or below the fake data's replacement thresholds would mean probing production data live; the fix is correct regardless of what that count currently is, since it removes the fabrication path rather than depending on today's real inventory size. **Built and merged 2026-09-05.** Commits: claim `45a2392`, fix `48aff7d`, cleanup `ef82b6e`, merge `04982fd`. Gate after the last phase-branch commit: typecheck 0 · lint 1 (**2 errors / 682 warnings — one better than the 683 standing baseline**, carried forward from SEC-5's incidental cleanup; every warning still flagged on the three touched files is pre-existing and unrelated — unused icon imports and `<img>`-vs-`next/image` notices, none on a line this phase changed) · **build 0 with real-firebase-config=yes** · secrets:bundle 0 · secrets:src-literals 0 · tracked-artefacts 0. **Revert-and-watch, in the phase worktree:** reverting both commits (`ef82b6e` then `48aff7d`, `--no-commit` each) brought all six removed fabricated identifiers back (`Digital Theni Solutions` present again in all three files); `git revert --abort` restored the fix, `git status` clean. **Verified live in the browser against real production data, not just statically** — the only phase this session to do so for a homepage change: started the dev server in this worktree with real `.env.local` credentials, loaded `/`, and read the rendered page. 'Featured Employers' (`TrustedEmployersStrip`) showed 8 real company initials (`UT, CH, SV, AM, VE, TH, CL, RA`) — none of the six removed fake ones (`DT`, the old fake `AM`/`CH`/`KC`/`CM` slugs); 'Latest & Trending Jobs' (`TrendingJobs`) showed two real postings ('Medical Records reviewer' at 'AGS IT SOLUTIONS', 'Delivery Partner' at 'Agrimore'), not the six fake demo jobs; the 'Featured Companies' section (`FeaturedBusinesses`) showed eight real companies ('Uthayam air travels', 'Chennai Bakes', 'SVR HERITAGE HALL', a REAL 'AM Siddha Hospital' record distinct from the removed fake one, etc.). `read_console_messages` reported zero errors. The coincidence that a real company happens to share a name with one of the removed fake entries (a real 'AM Siddha Hospital' does exist) was checked and confirmed to be a different Firestore document — the real one carries its own real category/district/rating, not the fabricated `reviews: 24, jobs: 4` the removed literal held. **The `||` → `??` operator fix proven old-vs-new**, not just read: a standalone four-input comparison (`undefined`, `null`, `false`, `true`) showed identical output on three inputs and a changed result only on `false` (`old=true` vs `new=false`), matching the intended fix exactly. Lanes: uiux PASS (verified live, not just read), feedback PASS, skill PASS. **Not verified:** whether the specific fake `/company/{slug}` links (e.g. `/company/digital-theni-solutions`) the old fallback pointed to ever resolved to a real page or 404'd — moot now that the fallback is removed, but not independently confirmed either way. **Correction added 2026-09-05, during TRUST-11:** the 'verified live in the browser' claim above needs a caveat discovered afterward, not before this row was first written. `preview_list` shows the browser-preview tool's dev server is always rooted at `cwd: "/Users/saai_siddharth/Projects/Clients/Theni Jobs"` — the **primary checkout on `main`** (`260ad42`, predating every phase this whole programme) — regardless of which worktree Bash/git commands target; it cannot be pointed at `develop` or a phase branch at all. So the browser check recorded above was actually run against unfixed `main` code, not this phase's fix. It still supports the conclusion, for a specific, checked reason rather than by luck: `TrendingJobs.tsx` and `FeaturedBusinesses.tsx`'s bug was specifically about the *initial paint* before the async fetch resolves — the fetch-then-replace logic itself was never broken, so by the time a full page load completes and `get_page_text` runs, **both the old and new code show real data** once the query returns. `TrustedEmployersStrip.tsx`'s bug was specifically the `>= 3` real-results gate — the site currently holds 8 real verified companies, clearing that bar either way. So the specific defects this phase fixed (initial-paint flash, sub-3-results fallback) were not, and could not have been, distinguished by this browser check, old code or new. The revert-and-watch and the `||`-vs-`??` standalone comparison remain the actual evidence for this phase's correctness; the browser observation is retracted as proof and kept only as a (accurate, if accidental) confirmation that the live site's real data renders correctly. Tool limitation recorded for every phase after this one: browser-based live verification in this environment can only ever check the primary checkout on `main`, never a worktree — it is unusable for verifying an unmerged phase or even `develop` post-merge. Source-level verification (`tsc`, revert-and-watch, standalone logic comparisons, `gate.sh`) is the only reliable method here. | `04982fd` (2026-09-05, inside `Theni-Jobs-develop`) |
| `thenijobs/trust11-fabricated-testimonials-attributed-to-named-people` | `23f30c4` | MERGED_DEVELOP | 2026-09-05 | owner ("start fixing all these bugs one by one", 2026-09-05) via phase TRUST-11 | **The home page's 'Success Stories' section is three invented testimonials attributed to named people, captioned 'Real stories from people using THENIJOBS.'** Found while chasing a fabricated star-rating trail after TRUST-10; the closest precedent is TRUST-1's 'stop publishing invented businesses' and TRUST-3's 'stop generated business sites claiming things nobody said' (`5dae14f`, `c6a6ac5`) — this is a third, separate, untouched instance of the same anti-pattern, on the home page itself rather than a generated company site. `src/components/home/TestimonialsSection.tsx` opens with its own code comment admitting what it is: `/* Server component — no hooks, purely static content */` (`:1`). It defines a fixed array of three entries — 'Rajan K,' 'Software Developer,' 'Theni,' rating 5, quote 'Found my dream job within 2 weeks of signing up...'; 'Priya S,' 'HR Manager, ABC Technologies,' 'Madurai,' rating 5, quote 'we've hired 12 candidates through THENIJOBS in the last 6 months...'; 'Murugan V,' 'Field Sales Executive,' 'Dindigul,' rating 5, quote '...got shortlisted within 3 days.' None of it is backed by any Firestore collection, any review record, or any user account — a grep for `testimonials` and for the fictional company name `ABC Technologies` finds nothing else anywhere in `src/`. The section header reads '⭐ Success Stories' / 'What Our Users Say', and the subheading states outright: **'Real stories from people using THENIJOBS'** (`:47`) — a direct, explicit claim of authenticity for content the file's own comment says is static and invented. **Why this ranks with TRUST-1 rather than the lower-severity 'Verified' badge findings (TRUST-8/9):** those asserted an unearned STATUS (verified/not); this asserts specific INVENTED PEOPLE, an invented EMPLOYER NAME ('ABC Technologies'), and specific quantified OUTCOMES ('12 candidates... in 6 months', 'within 2 weeks', 'within 3 days') as if real users reported them — on the single highest-traffic page of the site, under an explicit 'Real stories' label. This is fabricated evidence of the platform's own effectiveness, presented to every visitor as testimony from named third parties. Fix: there is no honest way to keep a testimonial with a specific invented name, employer, and outcome — unlike a numeric stat that can honestly default to zero, a quote cannot be partially de-fabricated while still being presented as someone's words. Remove `TestimonialsSection.tsx` and its one render call in `src/app/page.tsx` (`:15` import, `:138` usage) entirely, rather than inventing different fictional testimonials or a placeholder claiming a feature that doesn't exist. No Firestore-backed real testimonial system exists to substitute in its place — building one (collecting, moderating, and displaying genuine user testimonials) is a real feature project, not a copy fix, and is not undertaken here. Expected files: `src/components/home/TestimonialsSection.tsx` (deleted), `src/app/page.tsx`, this ledger. Never touches any other home component, `firestore.rules`, any company-site generator already covered by TRUST-3. Lanes: uiux, feedback, skill. **Collision check at claim:** worktrees = primary (`main`) + `Theni-Jobs-develop` (`1035c2e`) + this one; `ACTIVE` rows = 0. Worktree: `/Users/saai_siddharth/Projects/Clients/Theni-Jobs-trust11`. Acceptance: `TestimonialsSection.tsx` no longer exists; `page.tsx` has no import of or reference to it; the home page still builds and renders the surrounding sections unchanged; typecheck 0, build 0 with real credentials, lint no worse than the 2-error/682-warning baseline; page count checked before/after in this same worktree (removing a component from within the `/` route changes no route count). **Not claimed:** no replacement testimonial content, real or otherwise, is added — the section is removed, not repopulated. If the owner wants a real 'Success Stories' section, it needs a genuine source (collected, consented, moderated user testimonials), which this phase does not build. **Related, separate findings from the same sweep, not folded into this phase:** several files default a company's/service's display `rating` to a fabricated non-zero number (`|| 5`, `|| 4.5`, `|| 4.8`) when the real value is absent, and two company-creation write paths (`register-business/page.tsx`, `src/lib/firebase/bulkCompanyService.ts`) write a hardcoded `rating: 5.0`/`4.8` paired with `reviewCount: 0` at creation time — already directly observed live in production during TRUST-10's browser verification ('5.0 (0)', '4.8 (0)'). Recorded for a follow-on phase (TRUST-12) rather than expanded into this diff, since it is a mechanically distinct fix (numeric defaults across ~8 files) from this one (deleting a static fabricated-quotes component). **Built and merged 2026-09-05.** Commits: claim `6f4145f`, fix `23f30c4`, merge `beb2d14`. Gate after the last phase-branch commit: typecheck 0 · lint 1 (**2 errors / 681 warnings — better than the 682 prior baseline**, deleting the whole file removed its own warnings too) · **build 0 with real-firebase-config=yes** · secrets:bundle 0 · secrets:src-literals 0 · tracked-artefacts 0. **Revert-and-watch, in the phase worktree:** `git revert --no-commit 23f30c4` brought the file and both `page.tsx` references back; `git revert --abort` restored the fix (file gone, 0 references), `git status --porcelain` empty. **A live-verification methodology error, caught and corrected within this same phase, not left standing:** a browser check of the isolated phase worktree, before merging, still showed the fabricated testimonials — first read as a broken fix. Investigating with `preview_list` found the actual cause: the browser-preview tool's dev server is always rooted at `cwd: "/Users/saai_siddharth/Projects/Clients/Theni Jobs"` — the **primary checkout on `main`** (`260ad42`), untouched by this whole `fix-2026-09` programme — regardless of which worktree Bash/git commands target. It cannot be pointed at `develop`, a phase branch, or any worktree at all. A second check, after merging into `develop` and clearing a stale `.next`, still showed the same fabricated testimonials for the same reason: still `main`, still unfixed, not `develop`. Both checks were retracted as evidence rather than left in the record — see the correction appended to TRUST-10's own row below, which carries the same retraction and the reasoning for why TRUST-10's earlier live check happened to still support its conclusion while this one could not. The dev server was stopped and the `.next` directory it created in the primary checkout was removed, restoring `git status --porcelain` there to its original single line (`?? Documents/`) — no tracked file in the owner's own checkout was touched. **This phase's actual, valid evidence is the revert-and-watch above plus the file-content review** (the deleted file and both `page.tsx` references, confirmed absent by direct inspection) — a live-browser confirmation was attempted and could not be obtained in this environment; it is not fabricated in its place. Lanes: uiux PASS (by source review; live confirmation unavailable), feedback PASS, skill PASS. **Tool limitation recorded for every phase after this one:** browser-based live verification in this environment can only ever reach the primary checkout on `main` — it is unusable for verifying an unmerged phase or `develop` post-merge; source-level verification (`tsc`, revert-and-watch, standalone logic comparisons, `gate.sh`) is the only reliable method here. **Not verified:** no replacement content was added to fill the visual gap left by the removed section — the page simply has one fewer section between 'Local Services' and the FAQ; whether the owner wants a different section there, or a real testimonial system built later, is unaddressed. | `beb2d14` (2026-09-05, inside `Theni-Jobs-develop`) |
| `thenijobs/trust12-new-companies-start-with-fake-perfect-rating` | `39c9e5b` | ACTIVE | 2026-09-05 | owner ("start fixing all these bugs one by one", 2026-09-05) via phase TRUST-12 | **New companies are created with a fabricated 4.8-5.0 star rating and zero real reviews, and eight display components fall back to the same fabricated numbers whenever the real value is missing — directly observed live in production during TRUST-10's browser check ('5.0 (0)', '4.8 (0)').** Found chasing the same rating trail that led to TRUST-11's testimonials; this is the numeric-defaults half of that sweep, deliberately kept separate since it is a mechanically distinct fix (defaults across ten files, not deleting one component). **Write-side (company creation) — three paths, one bug each:** `register-business/page.tsx:132` and `seeker/become-employer/page.tsx:199` both write `rating: 5.0, reviewCount: 0` in the same document that also sets `verificationStatus: 'pending'`, `isActive: false`, `isVerified: false` — a perfect score paired with zero evidence, self-contradicting the same object's own pending status. `src/lib/firebase/bulkCompanyService.ts:237` writes `rating: 4.8, reviewCount: 0` for every bulk-imported company. Two OTHER creation paths already get this right — `company/register/page.tsx:87` and `adminUserService.ts:156` both write `rating: 0` — proving this isn't a deliberate platform-wide choice, just two (three) write paths that never matched the correct pattern already established elsewhere in the same codebase. **Display-side — the same fabrication recurring wherever a company/service's rating is read with a non-zero fallback:** `CompanyLandingPageClient.tsx:126` and `company/[slug]/CompanyProfilePageClient.tsx:120` both `rating: d.rating || 5`; `CompanyProfilePageClient.tsx:148` `const rating = company.rating || 5` (fed into a schema.org `aggregateRating.ratingValue` JSON-LD block whenever `reviewCount > 0`, so a company with real reviews but no stored `rating` value would tell Google a false 5.0 in structured data); `admin/reports/page.tsx:103` `rating: c.rating || 4.5` (corrupts the admin's own internal analytics, not just a public page); `services/page.tsx:83` `rating: d.rating || 4.8`; `FeaturedBusinesses.tsx:152` `rating: d.rating || 4.5` (a second bug in the same block TRUST-10 already touched for `isVerified` — this rating default was missed in that phase and is closed here). Four OTHER display sites already use the correct `|| 0` pattern (`businesses/page.tsx:219`, `BusinessCategoryPageClient.tsx:70`, `CompanyProfilePageClient.tsx:347`, `CompanyProfileClient.tsx:191,882`), confirming the correct convention already exists and these five are the outliers. **One more instance, structurally different but same root cause:** `CompanyReviewsSection.tsx:26`'s `averageRating` prop defaults to `5.0` — currently dead in practice, since its one real caller (`CompanyProfileClient.tsx:882`) always explicitly passes `company.rating || 0`, but a fabricated default on a shared, reusable component is a live risk for any future caller and is fixed for the same reason the write-side defaults are. **Explicitly out of scope, and named as such rather than silently expanded into:** portfolio-template star-count defaults (`CorporatePremium.tsx`, `ProfessionalCompany.tsx`, `ServiceMarketplace.tsx`, `LocalBusinessPro.tsx`, `BusinessShowcase.tsx`, `CompanyLandingWebsite.tsx` — all `rating||5` on a per-testimonial star icon COUNT for content a business owner enters themselves on their own portfolio site, not the app inventing a rating about a third party) and `employer/website/templates/page.tsx`'s hardcoded template-preview testimonials (design-preview content shown to an employer choosing a template, not published as real). Both are a different character of default — cosmetic, on user-authored content — from a platform-wide company/service rating asserted about a business to the public. Fix: change every write-side `rating: 5.0`/`4.8` to `rating: 0`, matching `company/register/page.tsx`/`adminUserService.ts`'s already-correct pattern; change every display-side `|| 5`/`|| 4.5`/`|| 4.8` fallback to `|| 0`, matching `businesses/page.tsx`'s already-correct pattern; change `CompanyReviewsSection`'s `averageRating = 5.0` default to `= 0`. No display gating logic needs to change — `FeaturedBusinesses.tsx`'s `biz.rating > 0 &&` and `CompanyProfilePageClient.tsx`'s `reviewCount > 0` checks already correctly hide a zero rating once the underlying value is honest. Expected files: `src/app/register-business/page.tsx`, `src/app/seeker/become-employer/page.tsx`, `src/lib/firebase/bulkCompanyService.ts`, `src/app/[companySlug]/CompanyLandingPageClient.tsx`, `src/app/company/[slug]/CompanyProfilePageClient.tsx`, `src/app/admin/reports/page.tsx`, `src/app/services/page.tsx`, `src/components/home/FeaturedBusinesses.tsx`, `src/components/company/CompanyReviewsSection.tsx`, this ledger. Never touches `firestore.rules`, any portfolio template file, `employer/website/templates/page.tsx`, the four already-correct `|| 0` call sites named above. Lanes: uiux, feedback, skill. **Collision check at claim:** worktrees = primary (`main`) + `Theni-Jobs-develop` (`39c9e5b`) + this one; `ACTIVE` rows = 0. Worktree: `/Users/saai_siddharth/Projects/Clients/Theni-Jobs-trust12`. Acceptance: every one of the ten identified fabricated defaults changed to `0`; no other numeric default in these files changed; typecheck 0, build 0 with real credentials, lint no worse than the 2-error/681-warning baseline. Live-browser verification is not attempted — confirmed unavailable in this environment for anything but the primary checkout on `main` (see the corrected TRUST-10/11 rows above and `project_browser_preview_cannot_reach_worktrees` memory). **Not claimed:** this phase does not backfill already-persisted production company documents that already hold a fabricated `rating`/`reviewCount: 0` pair — several were directly observed live during TRUST-10 ('5.0 (0)', '4.8 (0)' for real companies including 'AM Siddha Hospital', 'Velammal Matriculation Higher Secondary School', 'Classic Honda' and others) — correcting those would be a write against production data, which this phase does not do; it only stops new and re-computed documents from being created this way going forward. Does not add a real review-aggregation feature (computing `rating` from actual submitted reviews) — that is a separate, larger feature, not a default-value fix. | N/A — pending |
## Promotions

| Date | Source → target | Source SHA | Pushed | Owner's word (quoted) | Deploy consequence | Evidence |
|---|---|---|---|---|---|---|
| — | — | — | — | — | — | none yet |
